#pragma once

#define TEST_V100_L1 \
    test_l1<dtype, 1, num_iters>(f); \
    test_l1<dtype, 2, num_iters>(f); \
    test_l1<dtype, 4, num_iters>(f); \
    test_l1<dtype, 8, num_iters>(f); \
    test_l1<dtype, 16, num_iters>(f); \
    test_l1<dtype, 24, num_iters>(f); \
    test_l1<dtype, 32, num_iters>(f); \
    test_l1<dtype, 40, num_iters>(f); \
    test_l1<dtype, 48, num_iters>(f); \
    test_l1<dtype, 56, num_iters>(f); \
    test_l1<dtype, 64, num_iters>(f); \
    test_l1<dtype, 72, num_iters>(f); \
    test_l1<dtype, 80, num_iters>(f); \
    test_l1<dtype, 88, num_iters>(f); \
    test_l1<dtype, 96, num_iters>(f); \
    test_l1<dtype, 104, num_iters>(f); \
    test_l1<dtype, 112, num_iters>(f); \
    test_l1<dtype, 120, num_iters>(f); \
    test_l1<dtype, 128, num_iters>(f); \
    test_l1<dtype, 136, num_iters>(f); \
    test_l1<dtype, 144, num_iters>(f); \
    test_l1<dtype, 152, num_iters>(f); \
    test_l1<dtype, 160, num_iters>(f); \
    test_l1<dtype, 168, num_iters>(f); \
    test_l1<dtype, 176, num_iters>(f); \
    test_l1<dtype, 184, num_iters>(f); \
    test_l1<dtype, 192, num_iters>(f); \
    test_l1<dtype, 200, num_iters>(f); \
    test_l1<dtype, 208, num_iters>(f); \
    test_l1<dtype, 216, num_iters>(f); \
    test_l1<dtype, 224, num_iters>(f); \
    test_l1<dtype, 232, num_iters>(f); \
    test_l1<dtype, 240, num_iters>(f); \
    test_l1<dtype, 248, num_iters>(f); \
    test_l1<dtype, 256, num_iters>(f); \
    test_l1<dtype, 264, num_iters>(f); \
    test_l1<dtype, 272, num_iters>(f); \
    test_l1<dtype, 280, num_iters>(f); \
    test_l1<dtype, 288, num_iters>(f); \
    test_l1<dtype, 296, num_iters>(f); \
    test_l1<dtype, 304, num_iters>(f); \
    test_l1<dtype, 312, num_iters>(f); \
    test_l1<dtype, 320, num_iters>(f); \
    test_l1<dtype, 328, num_iters>(f); \
    test_l1<dtype, 336, num_iters>(f); \
    test_l1<dtype, 344, num_iters>(f); \
    test_l1<dtype, 352, num_iters>(f); \
    test_l1<dtype, 360, num_iters>(f); \
    test_l1<dtype, 368, num_iters>(f); \
    test_l1<dtype, 376, num_iters>(f); \
    test_l1<dtype, 384, num_iters>(f); \
    test_l1<dtype, 392, num_iters>(f); \
    test_l1<dtype, 400, num_iters>(f); \
    test_l1<dtype, 408, num_iters>(f); \
    test_l1<dtype, 416, num_iters>(f); \
    test_l1<dtype, 424, num_iters>(f); \
    test_l1<dtype, 432, num_iters>(f); \
    test_l1<dtype, 440, num_iters>(f); \
    test_l1<dtype, 448, num_iters>(f); \
    test_l1<dtype, 456, num_iters>(f); \
    test_l1<dtype, 464, num_iters>(f); \
    test_l1<dtype, 472, num_iters>(f); \
    test_l1<dtype, 480, num_iters>(f); \
    test_l1<dtype, 488, num_iters>(f); \
    test_l1<dtype, 496, num_iters>(f); \
    test_l1<dtype, 504, num_iters>(f); \
    test_l1<dtype, 512, num_iters>(f); \
    test_l1<dtype, 520, num_iters>(f); \
    test_l1<dtype, 528, num_iters>(f); \
    test_l1<dtype, 536, num_iters>(f); \
    test_l1<dtype, 544, num_iters>(f); \
    test_l1<dtype, 552, num_iters>(f); \
    test_l1<dtype, 560, num_iters>(f); \
    test_l1<dtype, 568, num_iters>(f); \
    test_l1<dtype, 576, num_iters>(f); \
    test_l1<dtype, 584, num_iters>(f); \
    test_l1<dtype, 592, num_iters>(f); \
    test_l1<dtype, 600, num_iters>(f); \
    test_l1<dtype, 608, num_iters>(f); \
    test_l1<dtype, 616, num_iters>(f); \
    test_l1<dtype, 624, num_iters>(f); \
    test_l1<dtype, 632, num_iters>(f); \
    test_l1<dtype, 640, num_iters>(f); \
    test_l1<dtype, 648, num_iters>(f); \
    test_l1<dtype, 656, num_iters>(f); \
    test_l1<dtype, 664, num_iters>(f); \
    test_l1<dtype, 672, num_iters>(f); \
    test_l1<dtype, 680, num_iters>(f); \
    test_l1<dtype, 688, num_iters>(f); \
    test_l1<dtype, 696, num_iters>(f); \
    test_l1<dtype, 704, num_iters>(f); \
    test_l1<dtype, 712, num_iters>(f); \
    test_l1<dtype, 720, num_iters>(f); \
    test_l1<dtype, 728, num_iters>(f); \
    test_l1<dtype, 736, num_iters>(f); \
    test_l1<dtype, 744, num_iters>(f); \
    test_l1<dtype, 752, num_iters>(f); \
    test_l1<dtype, 760, num_iters>(f); \
    test_l1<dtype, 768, num_iters>(f); \
    test_l1<dtype, 776, num_iters>(f); \
    test_l1<dtype, 784, num_iters>(f); \
    test_l1<dtype, 792, num_iters>(f); \
    test_l1<dtype, 800, num_iters>(f); \
    test_l1<dtype, 808, num_iters>(f); \
    test_l1<dtype, 816, num_iters>(f); \
    test_l1<dtype, 824, num_iters>(f); \
    test_l1<dtype, 832, num_iters>(f); \
    test_l1<dtype, 840, num_iters>(f); \
    test_l1<dtype, 848, num_iters>(f); \
    test_l1<dtype, 856, num_iters>(f); \
    test_l1<dtype, 864, num_iters>(f); \
    test_l1<dtype, 872, num_iters>(f); \
    test_l1<dtype, 880, num_iters>(f); \
    test_l1<dtype, 888, num_iters>(f); \
    test_l1<dtype, 896, num_iters>(f); \
    test_l1<dtype, 904, num_iters>(f); \
    test_l1<dtype, 912, num_iters>(f); \
    test_l1<dtype, 920, num_iters>(f); \
    test_l1<dtype, 928, num_iters>(f); \
    test_l1<dtype, 936, num_iters>(f); \
    test_l1<dtype, 944, num_iters>(f); \
    test_l1<dtype, 952, num_iters>(f); \
    test_l1<dtype, 960, num_iters>(f); \
    test_l1<dtype, 968, num_iters>(f); \
    test_l1<dtype, 976, num_iters>(f); \
    test_l1<dtype, 984, num_iters>(f); \
    test_l1<dtype, 992, num_iters>(f); \
    test_l1<dtype, 1000, num_iters>(f); \
    test_l1<dtype, 1008, num_iters>(f); \
    test_l1<dtype, 1016, num_iters>(f); \
    test_l1<dtype, 1024, num_iters>(f); \
    test_l1<dtype, 1032, num_iters>(f); \
    test_l1<dtype, 1040, num_iters>(f); \
    test_l1<dtype, 1048, num_iters>(f); \
    test_l1<dtype, 1056, num_iters>(f); \
    test_l1<dtype, 1064, num_iters>(f); \
    test_l1<dtype, 1072, num_iters>(f); \
    test_l1<dtype, 1080, num_iters>(f); \
    test_l1<dtype, 1088, num_iters>(f); \
    test_l1<dtype, 1096, num_iters>(f); \
    test_l1<dtype, 1104, num_iters>(f); \
    test_l1<dtype, 1112, num_iters>(f); \
    test_l1<dtype, 1120, num_iters>(f); \
    test_l1<dtype, 1128, num_iters>(f); \
    test_l1<dtype, 1136, num_iters>(f); \
    test_l1<dtype, 1144, num_iters>(f); \
    test_l1<dtype, 1152, num_iters>(f); \
    test_l1<dtype, 1160, num_iters>(f); \
    test_l1<dtype, 1168, num_iters>(f); \
    test_l1<dtype, 1176, num_iters>(f); \
    test_l1<dtype, 1184, num_iters>(f); \
    test_l1<dtype, 1192, num_iters>(f); \
    test_l1<dtype, 1200, num_iters>(f); \
    test_l1<dtype, 1208, num_iters>(f); \
    test_l1<dtype, 1216, num_iters>(f); \
    test_l1<dtype, 1224, num_iters>(f); \
    test_l1<dtype, 1232, num_iters>(f); \
    test_l1<dtype, 1240, num_iters>(f); \
    test_l1<dtype, 1248, num_iters>(f); \
    test_l1<dtype, 1256, num_iters>(f); \
    test_l1<dtype, 1264, num_iters>(f); \
    test_l1<dtype, 1272, num_iters>(f); \
    test_l1<dtype, 1280, num_iters>(f); \
    test_l1<dtype, 1288, num_iters>(f); \
    test_l1<dtype, 1296, num_iters>(f); \
    test_l1<dtype, 1304, num_iters>(f); \
    test_l1<dtype, 1312, num_iters>(f); \
    test_l1<dtype, 1320, num_iters>(f); \
    test_l1<dtype, 1328, num_iters>(f); \
    test_l1<dtype, 1336, num_iters>(f); \
    test_l1<dtype, 1344, num_iters>(f); \
    test_l1<dtype, 1352, num_iters>(f); \
    test_l1<dtype, 1360, num_iters>(f); \
    test_l1<dtype, 1368, num_iters>(f); \
    test_l1<dtype, 1376, num_iters>(f); \
    test_l1<dtype, 1384, num_iters>(f); \
    test_l1<dtype, 1392, num_iters>(f); \
    test_l1<dtype, 1400, num_iters>(f); \
    test_l1<dtype, 1408, num_iters>(f); \
    test_l1<dtype, 1416, num_iters>(f); \
    test_l1<dtype, 1424, num_iters>(f); \
    test_l1<dtype, 1432, num_iters>(f); \
    test_l1<dtype, 1440, num_iters>(f); \
    test_l1<dtype, 1448, num_iters>(f); \
    test_l1<dtype, 1456, num_iters>(f); \
    test_l1<dtype, 1464, num_iters>(f); \
    test_l1<dtype, 1472, num_iters>(f); \
    test_l1<dtype, 1480, num_iters>(f); \
    test_l1<dtype, 1488, num_iters>(f); \
    test_l1<dtype, 1496, num_iters>(f); \
    test_l1<dtype, 1504, num_iters>(f); \
    test_l1<dtype, 1512, num_iters>(f); \
    test_l1<dtype, 1520, num_iters>(f); \
    test_l1<dtype, 1528, num_iters>(f); \
    test_l1<dtype, 1536, num_iters>(f); \
    test_l1<dtype, 1544, num_iters>(f); \
    test_l1<dtype, 1552, num_iters>(f); \
    test_l1<dtype, 1560, num_iters>(f); \
    test_l1<dtype, 1568, num_iters>(f); \
    test_l1<dtype, 1576, num_iters>(f); \
    test_l1<dtype, 1584, num_iters>(f); \
    test_l1<dtype, 1592, num_iters>(f); \
    test_l1<dtype, 1600, num_iters>(f); \
    test_l1<dtype, 1608, num_iters>(f); \
    test_l1<dtype, 1616, num_iters>(f); \
    test_l1<dtype, 1624, num_iters>(f); \
    test_l1<dtype, 1632, num_iters>(f); \
    test_l1<dtype, 1640, num_iters>(f); \
    test_l1<dtype, 1648, num_iters>(f); \
    test_l1<dtype, 1656, num_iters>(f); \
    test_l1<dtype, 1664, num_iters>(f); \
    test_l1<dtype, 1672, num_iters>(f); \
    test_l1<dtype, 1680, num_iters>(f); \
    test_l1<dtype, 1688, num_iters>(f); \
    test_l1<dtype, 1696, num_iters>(f); \
    test_l1<dtype, 1704, num_iters>(f); \
    test_l1<dtype, 1712, num_iters>(f); \
    test_l1<dtype, 1720, num_iters>(f); \
    test_l1<dtype, 1728, num_iters>(f); \
    test_l1<dtype, 1736, num_iters>(f); \
    test_l1<dtype, 1744, num_iters>(f); \
    test_l1<dtype, 1752, num_iters>(f); \
    test_l1<dtype, 1760, num_iters>(f); \
    test_l1<dtype, 1768, num_iters>(f); \
    test_l1<dtype, 1776, num_iters>(f); \
    test_l1<dtype, 1784, num_iters>(f); \
    test_l1<dtype, 1792, num_iters>(f); \
    test_l1<dtype, 1800, num_iters>(f); \
    test_l1<dtype, 1808, num_iters>(f); \
    test_l1<dtype, 1816, num_iters>(f); \
    test_l1<dtype, 1824, num_iters>(f); \
    test_l1<dtype, 1832, num_iters>(f); \
    test_l1<dtype, 1840, num_iters>(f); \
    test_l1<dtype, 1848, num_iters>(f); \
    test_l1<dtype, 1856, num_iters>(f); \
    test_l1<dtype, 1864, num_iters>(f); \
    test_l1<dtype, 1872, num_iters>(f); \
    test_l1<dtype, 1880, num_iters>(f); \
    test_l1<dtype, 1888, num_iters>(f); \
    test_l1<dtype, 1896, num_iters>(f); \
    test_l1<dtype, 1904, num_iters>(f); \
    test_l1<dtype, 1912, num_iters>(f); \
    test_l1<dtype, 1920, num_iters>(f); \
    test_l1<dtype, 1928, num_iters>(f); \
    test_l1<dtype, 1936, num_iters>(f); \
    test_l1<dtype, 1944, num_iters>(f); \
    test_l1<dtype, 1952, num_iters>(f); \
    test_l1<dtype, 1960, num_iters>(f); \
    test_l1<dtype, 1968, num_iters>(f); \
    test_l1<dtype, 1976, num_iters>(f); \
    test_l1<dtype, 1984, num_iters>(f); \
    test_l1<dtype, 1992, num_iters>(f); \
    test_l1<dtype, 2000, num_iters>(f); \
    test_l1<dtype, 2008, num_iters>(f); \
    test_l1<dtype, 2016, num_iters>(f); \
    test_l1<dtype, 2024, num_iters>(f); \
    test_l1<dtype, 2032, num_iters>(f); \
    test_l1<dtype, 2040, num_iters>(f); \
    test_l1<dtype, 2048, num_iters>(f); \
    test_l1<dtype, 2056, num_iters>(f); \
    test_l1<dtype, 2064, num_iters>(f); \
    test_l1<dtype, 2072, num_iters>(f); \
    test_l1<dtype, 2080, num_iters>(f); \
    test_l1<dtype, 2088, num_iters>(f); \
    test_l1<dtype, 2096, num_iters>(f); \
    test_l1<dtype, 2104, num_iters>(f); \
    test_l1<dtype, 2112, num_iters>(f); \
    test_l1<dtype, 2120, num_iters>(f); \
    test_l1<dtype, 2128, num_iters>(f); \
    test_l1<dtype, 2136, num_iters>(f); \
    test_l1<dtype, 2144, num_iters>(f); \
    test_l1<dtype, 2152, num_iters>(f); \
    test_l1<dtype, 2160, num_iters>(f); \
    test_l1<dtype, 2168, num_iters>(f); \
    test_l1<dtype, 2176, num_iters>(f); \
    test_l1<dtype, 2184, num_iters>(f); \
    test_l1<dtype, 2192, num_iters>(f); \
    test_l1<dtype, 2200, num_iters>(f); \
    test_l1<dtype, 2208, num_iters>(f); \
    test_l1<dtype, 2216, num_iters>(f); \
    test_l1<dtype, 2224, num_iters>(f); \
    test_l1<dtype, 2232, num_iters>(f); \
    test_l1<dtype, 2240, num_iters>(f); \
    test_l1<dtype, 2248, num_iters>(f); \
    test_l1<dtype, 2256, num_iters>(f); \
    test_l1<dtype, 2264, num_iters>(f); \
    test_l1<dtype, 2272, num_iters>(f); \
    test_l1<dtype, 2280, num_iters>(f); \
    test_l1<dtype, 2288, num_iters>(f); \
    test_l1<dtype, 2296, num_iters>(f); \
    test_l1<dtype, 2304, num_iters>(f); \
    test_l1<dtype, 2312, num_iters>(f); \
    test_l1<dtype, 2320, num_iters>(f); \
    test_l1<dtype, 2328, num_iters>(f); \
    test_l1<dtype, 2336, num_iters>(f); \
    test_l1<dtype, 2344, num_iters>(f); \
    test_l1<dtype, 2352, num_iters>(f); \
    test_l1<dtype, 2360, num_iters>(f); \
    test_l1<dtype, 2368, num_iters>(f); \
    test_l1<dtype, 2376, num_iters>(f); \
    test_l1<dtype, 2384, num_iters>(f); \
    test_l1<dtype, 2392, num_iters>(f); \
    test_l1<dtype, 2400, num_iters>(f); \
    test_l1<dtype, 2408, num_iters>(f); \
    test_l1<dtype, 2416, num_iters>(f); \
    test_l1<dtype, 2424, num_iters>(f); \
    test_l1<dtype, 2432, num_iters>(f); \
    test_l1<dtype, 2440, num_iters>(f); \
    test_l1<dtype, 2448, num_iters>(f); \
    test_l1<dtype, 2456, num_iters>(f); \
    test_l1<dtype, 2464, num_iters>(f); \
    test_l1<dtype, 2472, num_iters>(f); \
    test_l1<dtype, 2480, num_iters>(f); \
    test_l1<dtype, 2488, num_iters>(f); \
    test_l1<dtype, 2496, num_iters>(f); \
    test_l1<dtype, 2504, num_iters>(f); \
    test_l1<dtype, 2512, num_iters>(f); \
    test_l1<dtype, 2520, num_iters>(f); \
    test_l1<dtype, 2528, num_iters>(f); \
    test_l1<dtype, 2536, num_iters>(f); \
    test_l1<dtype, 2544, num_iters>(f); \
    test_l1<dtype, 2552, num_iters>(f); \
    test_l1<dtype, 2560, num_iters>(f); \
    test_l1<dtype, 2568, num_iters>(f); \
    test_l1<dtype, 2576, num_iters>(f); \
    test_l1<dtype, 2584, num_iters>(f); \
    test_l1<dtype, 2592, num_iters>(f); \
    test_l1<dtype, 2600, num_iters>(f); \
    test_l1<dtype, 2608, num_iters>(f); \
    test_l1<dtype, 2616, num_iters>(f); \
    test_l1<dtype, 2624, num_iters>(f); \
    test_l1<dtype, 2632, num_iters>(f); \
    test_l1<dtype, 2640, num_iters>(f); \
    test_l1<dtype, 2648, num_iters>(f); \
    test_l1<dtype, 2656, num_iters>(f); \
    test_l1<dtype, 2664, num_iters>(f); \
    test_l1<dtype, 2672, num_iters>(f); \
    test_l1<dtype, 2680, num_iters>(f); \
    test_l1<dtype, 2688, num_iters>(f); \
    test_l1<dtype, 2696, num_iters>(f); \
    test_l1<dtype, 2704, num_iters>(f); \
    test_l1<dtype, 2712, num_iters>(f); \
    test_l1<dtype, 2720, num_iters>(f); \
    test_l1<dtype, 2728, num_iters>(f); \
    test_l1<dtype, 2736, num_iters>(f); \
    test_l1<dtype, 2744, num_iters>(f); \
    test_l1<dtype, 2752, num_iters>(f); \
    test_l1<dtype, 2760, num_iters>(f); \
    test_l1<dtype, 2768, num_iters>(f); \
    test_l1<dtype, 2776, num_iters>(f); \
    test_l1<dtype, 2784, num_iters>(f); \
    test_l1<dtype, 2792, num_iters>(f); \
    test_l1<dtype, 2800, num_iters>(f); \
    test_l1<dtype, 2808, num_iters>(f); \
    test_l1<dtype, 2816, num_iters>(f); \
    test_l1<dtype, 2824, num_iters>(f); \
    test_l1<dtype, 2832, num_iters>(f); \
    test_l1<dtype, 2840, num_iters>(f); \
    test_l1<dtype, 2848, num_iters>(f); \
    test_l1<dtype, 2856, num_iters>(f); \
    test_l1<dtype, 2864, num_iters>(f); \
    test_l1<dtype, 2872, num_iters>(f); \
    test_l1<dtype, 2880, num_iters>(f); \
    test_l1<dtype, 2888, num_iters>(f); \
    test_l1<dtype, 2896, num_iters>(f); \
    test_l1<dtype, 2904, num_iters>(f); \
    test_l1<dtype, 2912, num_iters>(f); \
    test_l1<dtype, 2920, num_iters>(f); \
    test_l1<dtype, 2928, num_iters>(f); \
    test_l1<dtype, 2936, num_iters>(f); \
    test_l1<dtype, 2944, num_iters>(f); \
    test_l1<dtype, 2952, num_iters>(f); \
    test_l1<dtype, 2960, num_iters>(f); \
    test_l1<dtype, 2968, num_iters>(f); \
    test_l1<dtype, 2976, num_iters>(f); \
    test_l1<dtype, 2984, num_iters>(f); \
    test_l1<dtype, 2992, num_iters>(f); \
    test_l1<dtype, 3000, num_iters>(f); \
    test_l1<dtype, 3008, num_iters>(f); \
    test_l1<dtype, 3016, num_iters>(f); \
    test_l1<dtype, 3024, num_iters>(f); \
    test_l1<dtype, 3032, num_iters>(f); \
    test_l1<dtype, 3040, num_iters>(f); \
    test_l1<dtype, 3048, num_iters>(f); \
    test_l1<dtype, 3056, num_iters>(f); \
    test_l1<dtype, 3064, num_iters>(f); \
    test_l1<dtype, 3072, num_iters>(f); \
    test_l1<dtype, 3080, num_iters>(f); \
    test_l1<dtype, 3088, num_iters>(f); \
    test_l1<dtype, 3096, num_iters>(f); \
    test_l1<dtype, 3104, num_iters>(f); \
    test_l1<dtype, 3112, num_iters>(f); \
    test_l1<dtype, 3120, num_iters>(f); \
    test_l1<dtype, 3128, num_iters>(f); \
    test_l1<dtype, 3136, num_iters>(f); \
    test_l1<dtype, 3144, num_iters>(f); \
    test_l1<dtype, 3152, num_iters>(f); \
    test_l1<dtype, 3160, num_iters>(f); \
    test_l1<dtype, 3168, num_iters>(f); \
    test_l1<dtype, 3176, num_iters>(f); \
    test_l1<dtype, 3184, num_iters>(f); \
    test_l1<dtype, 3192, num_iters>(f); \
    test_l1<dtype, 3200, num_iters>(f); \
    test_l1<dtype, 3208, num_iters>(f); \
    test_l1<dtype, 3216, num_iters>(f); \
    test_l1<dtype, 3224, num_iters>(f); \
    test_l1<dtype, 3232, num_iters>(f); \
    test_l1<dtype, 3240, num_iters>(f); \
    test_l1<dtype, 3248, num_iters>(f); \
    test_l1<dtype, 3256, num_iters>(f); \
    test_l1<dtype, 3264, num_iters>(f); \
    test_l1<dtype, 3272, num_iters>(f); \
    test_l1<dtype, 3280, num_iters>(f); \
    test_l1<dtype, 3288, num_iters>(f); \
    test_l1<dtype, 3296, num_iters>(f); \
    test_l1<dtype, 3304, num_iters>(f); \
    test_l1<dtype, 3312, num_iters>(f); \
    test_l1<dtype, 3320, num_iters>(f); \
    test_l1<dtype, 3328, num_iters>(f); \
    test_l1<dtype, 3336, num_iters>(f); \
    test_l1<dtype, 3344, num_iters>(f); \
    test_l1<dtype, 3352, num_iters>(f); \
    test_l1<dtype, 3360, num_iters>(f); \
    test_l1<dtype, 3368, num_iters>(f); \
    test_l1<dtype, 3376, num_iters>(f); \
    test_l1<dtype, 3384, num_iters>(f); \
    test_l1<dtype, 3392, num_iters>(f); \
    test_l1<dtype, 3400, num_iters>(f); \
    test_l1<dtype, 3408, num_iters>(f); \
    test_l1<dtype, 3416, num_iters>(f); \
    test_l1<dtype, 3424, num_iters>(f); \
    test_l1<dtype, 3432, num_iters>(f); \
    test_l1<dtype, 3440, num_iters>(f); \
    test_l1<dtype, 3448, num_iters>(f); \
    test_l1<dtype, 3456, num_iters>(f); \
    test_l1<dtype, 3464, num_iters>(f); \
    test_l1<dtype, 3472, num_iters>(f); \
    test_l1<dtype, 3480, num_iters>(f); \
    test_l1<dtype, 3488, num_iters>(f); \
    test_l1<dtype, 3496, num_iters>(f); \
    test_l1<dtype, 3504, num_iters>(f); \
    test_l1<dtype, 3512, num_iters>(f); \
    test_l1<dtype, 3520, num_iters>(f); \
    test_l1<dtype, 3528, num_iters>(f); \
    test_l1<dtype, 3536, num_iters>(f); \
    test_l1<dtype, 3544, num_iters>(f); \
    test_l1<dtype, 3552, num_iters>(f); \
    test_l1<dtype, 3560, num_iters>(f); \
    test_l1<dtype, 3568, num_iters>(f); \
    test_l1<dtype, 3576, num_iters>(f); \
    test_l1<dtype, 3584, num_iters>(f); \
    test_l1<dtype, 3592, num_iters>(f); \
    test_l1<dtype, 3600, num_iters>(f); \
    test_l1<dtype, 3608, num_iters>(f); \
    test_l1<dtype, 3616, num_iters>(f); \
    test_l1<dtype, 3624, num_iters>(f); \
    test_l1<dtype, 3632, num_iters>(f); \
    test_l1<dtype, 3640, num_iters>(f); \
    test_l1<dtype, 3648, num_iters>(f); \
    test_l1<dtype, 3656, num_iters>(f); \
    test_l1<dtype, 3664, num_iters>(f); \
    test_l1<dtype, 3672, num_iters>(f); \
    test_l1<dtype, 3680, num_iters>(f); \
    test_l1<dtype, 3688, num_iters>(f); \
    test_l1<dtype, 3696, num_iters>(f); \
    test_l1<dtype, 3704, num_iters>(f); \
    test_l1<dtype, 3712, num_iters>(f); \
    test_l1<dtype, 3720, num_iters>(f); \
    test_l1<dtype, 3728, num_iters>(f); \
    test_l1<dtype, 3736, num_iters>(f); \
    test_l1<dtype, 3744, num_iters>(f); \
    test_l1<dtype, 3752, num_iters>(f); \
    test_l1<dtype, 3760, num_iters>(f); \
    test_l1<dtype, 3768, num_iters>(f); \
    test_l1<dtype, 3776, num_iters>(f); \
    test_l1<dtype, 3784, num_iters>(f); \
    test_l1<dtype, 3792, num_iters>(f); \
    test_l1<dtype, 3800, num_iters>(f); \
    test_l1<dtype, 3808, num_iters>(f); \
    test_l1<dtype, 3816, num_iters>(f); \
    test_l1<dtype, 3824, num_iters>(f); \
    test_l1<dtype, 3832, num_iters>(f); \
    test_l1<dtype, 3840, num_iters>(f); \
    test_l1<dtype, 3848, num_iters>(f); \
    test_l1<dtype, 3856, num_iters>(f); \
    test_l1<dtype, 3864, num_iters>(f); \
    test_l1<dtype, 3872, num_iters>(f); \
    test_l1<dtype, 3880, num_iters>(f); \
    test_l1<dtype, 3888, num_iters>(f); \
    test_l1<dtype, 3896, num_iters>(f); \
    test_l1<dtype, 3904, num_iters>(f); \
    test_l1<dtype, 3912, num_iters>(f); \
    test_l1<dtype, 3920, num_iters>(f); \
    test_l1<dtype, 3928, num_iters>(f); \
    test_l1<dtype, 3936, num_iters>(f); \
    test_l1<dtype, 3944, num_iters>(f); \
    test_l1<dtype, 3952, num_iters>(f); \
    test_l1<dtype, 3960, num_iters>(f); \
    test_l1<dtype, 3968, num_iters>(f); \
    test_l1<dtype, 3976, num_iters>(f); \
    test_l1<dtype, 3984, num_iters>(f); \
    test_l1<dtype, 3992, num_iters>(f); \
    test_l1<dtype, 4000, num_iters>(f); \
    test_l1<dtype, 4008, num_iters>(f); \
    test_l1<dtype, 4016, num_iters>(f); \
    test_l1<dtype, 4024, num_iters>(f); \
    test_l1<dtype, 4032, num_iters>(f); \
    test_l1<dtype, 4040, num_iters>(f); \
    test_l1<dtype, 4048, num_iters>(f); \
    test_l1<dtype, 4056, num_iters>(f); \
    test_l1<dtype, 4064, num_iters>(f); \
    test_l1<dtype, 4072, num_iters>(f); \
    test_l1<dtype, 4080, num_iters>(f); \
    test_l1<dtype, 4088, num_iters>(f); \
    test_l1<dtype, 4096, num_iters>(f); \
    test_l1<dtype, 4104, num_iters>(f); \
    test_l1<dtype, 4112, num_iters>(f); \
    test_l1<dtype, 4120, num_iters>(f); \
    test_l1<dtype, 4128, num_iters>(f); \
    test_l1<dtype, 4136, num_iters>(f); \
    test_l1<dtype, 4144, num_iters>(f); \
    test_l1<dtype, 4152, num_iters>(f); \
    test_l1<dtype, 4160, num_iters>(f); \
    test_l1<dtype, 4168, num_iters>(f); \
    test_l1<dtype, 4176, num_iters>(f); \
    test_l1<dtype, 4184, num_iters>(f); \
    test_l1<dtype, 4192, num_iters>(f); \
    test_l1<dtype, 4200, num_iters>(f); \
    test_l1<dtype, 4208, num_iters>(f); \
    test_l1<dtype, 4216, num_iters>(f); \
    test_l1<dtype, 4224, num_iters>(f); \
    test_l1<dtype, 4232, num_iters>(f); \
    test_l1<dtype, 4240, num_iters>(f); \
    test_l1<dtype, 4248, num_iters>(f); \
    test_l1<dtype, 4256, num_iters>(f); \
    test_l1<dtype, 4264, num_iters>(f); \
    test_l1<dtype, 4272, num_iters>(f); \
    test_l1<dtype, 4280, num_iters>(f); \
    test_l1<dtype, 4288, num_iters>(f); \
    test_l1<dtype, 4296, num_iters>(f); \
    test_l1<dtype, 4304, num_iters>(f); \
    test_l1<dtype, 4312, num_iters>(f); \
    test_l1<dtype, 4320, num_iters>(f); \
    test_l1<dtype, 4328, num_iters>(f); \
    test_l1<dtype, 4336, num_iters>(f); \
    test_l1<dtype, 4344, num_iters>(f); \
    test_l1<dtype, 4352, num_iters>(f); \
    test_l1<dtype, 4360, num_iters>(f); \
    test_l1<dtype, 4368, num_iters>(f); \
    test_l1<dtype, 4376, num_iters>(f); \
    test_l1<dtype, 4384, num_iters>(f); \
    test_l1<dtype, 4392, num_iters>(f); \
    test_l1<dtype, 4400, num_iters>(f); \
    test_l1<dtype, 4408, num_iters>(f); \
    test_l1<dtype, 4416, num_iters>(f); \
    test_l1<dtype, 4424, num_iters>(f); \
    test_l1<dtype, 4432, num_iters>(f); \
    test_l1<dtype, 4440, num_iters>(f); \
    test_l1<dtype, 4448, num_iters>(f); \
    test_l1<dtype, 4456, num_iters>(f); \
    test_l1<dtype, 4464, num_iters>(f); \
    test_l1<dtype, 4472, num_iters>(f); \
    test_l1<dtype, 4480, num_iters>(f); \
    test_l1<dtype, 4488, num_iters>(f); \
    test_l1<dtype, 4496, num_iters>(f); \
    test_l1<dtype, 4504, num_iters>(f); \
    test_l1<dtype, 4512, num_iters>(f); \
    test_l1<dtype, 4520, num_iters>(f); \
    test_l1<dtype, 4528, num_iters>(f); \
    test_l1<dtype, 4536, num_iters>(f); \
    test_l1<dtype, 4544, num_iters>(f); \
    test_l1<dtype, 4552, num_iters>(f); \
    test_l1<dtype, 4560, num_iters>(f); \
    test_l1<dtype, 4568, num_iters>(f); \
    test_l1<dtype, 4576, num_iters>(f); \
    test_l1<dtype, 4584, num_iters>(f); \
    test_l1<dtype, 4592, num_iters>(f); \
    test_l1<dtype, 4600, num_iters>(f); \
    test_l1<dtype, 4608, num_iters>(f); \
    test_l1<dtype, 4616, num_iters>(f); \
    test_l1<dtype, 4624, num_iters>(f); \
    test_l1<dtype, 4632, num_iters>(f); \
    test_l1<dtype, 4640, num_iters>(f); \
    test_l1<dtype, 4648, num_iters>(f); \
    test_l1<dtype, 4656, num_iters>(f); \
    test_l1<dtype, 4664, num_iters>(f); \
    test_l1<dtype, 4672, num_iters>(f); \
    test_l1<dtype, 4680, num_iters>(f); \
    test_l1<dtype, 4688, num_iters>(f); \
    test_l1<dtype, 4696, num_iters>(f); \
    test_l1<dtype, 4704, num_iters>(f); \
    test_l1<dtype, 4712, num_iters>(f); \
    test_l1<dtype, 4720, num_iters>(f); \
    test_l1<dtype, 4728, num_iters>(f); \
    test_l1<dtype, 4736, num_iters>(f); \
    test_l1<dtype, 4744, num_iters>(f); \
    test_l1<dtype, 4752, num_iters>(f); \
    test_l1<dtype, 4760, num_iters>(f); \
    test_l1<dtype, 4768, num_iters>(f); \
    test_l1<dtype, 4776, num_iters>(f); \
    test_l1<dtype, 4784, num_iters>(f); \
    test_l1<dtype, 4792, num_iters>(f); \
    test_l1<dtype, 4800, num_iters>(f); \
    test_l1<dtype, 4808, num_iters>(f); \
    test_l1<dtype, 4816, num_iters>(f); \
    test_l1<dtype, 4824, num_iters>(f); \
    test_l1<dtype, 4832, num_iters>(f); \
    test_l1<dtype, 4840, num_iters>(f); \
    test_l1<dtype, 4848, num_iters>(f); \
    test_l1<dtype, 4856, num_iters>(f); \
    test_l1<dtype, 4864, num_iters>(f); \
    test_l1<dtype, 4872, num_iters>(f); \
    test_l1<dtype, 4880, num_iters>(f); \
    test_l1<dtype, 4888, num_iters>(f); \
    test_l1<dtype, 4896, num_iters>(f); \
    test_l1<dtype, 4904, num_iters>(f); \
    test_l1<dtype, 4912, num_iters>(f); \
    test_l1<dtype, 4920, num_iters>(f); \
    test_l1<dtype, 4928, num_iters>(f); \
    test_l1<dtype, 4936, num_iters>(f); \
    test_l1<dtype, 4944, num_iters>(f); \
    test_l1<dtype, 4952, num_iters>(f); \
    test_l1<dtype, 4960, num_iters>(f); \
    test_l1<dtype, 4968, num_iters>(f); \
    test_l1<dtype, 4976, num_iters>(f); \
    test_l1<dtype, 4984, num_iters>(f); \
    test_l1<dtype, 4992, num_iters>(f); \
    test_l1<dtype, 5000, num_iters>(f); \
    test_l1<dtype, 5008, num_iters>(f); \
    test_l1<dtype, 5016, num_iters>(f); \
    test_l1<dtype, 5024, num_iters>(f); \
    test_l1<dtype, 5032, num_iters>(f); \
    test_l1<dtype, 5040, num_iters>(f); \
    test_l1<dtype, 5048, num_iters>(f); \
    test_l1<dtype, 5056, num_iters>(f); \
    test_l1<dtype, 5064, num_iters>(f); \
    test_l1<dtype, 5072, num_iters>(f); \
    test_l1<dtype, 5080, num_iters>(f); \
    test_l1<dtype, 5088, num_iters>(f); \
    test_l1<dtype, 5096, num_iters>(f); \
    test_l1<dtype, 5104, num_iters>(f); \
    test_l1<dtype, 5112, num_iters>(f); \
    test_l1<dtype, 5120, num_iters>(f); \
    test_l1<dtype, 5128, num_iters>(f); \
    test_l1<dtype, 5136, num_iters>(f); \
    test_l1<dtype, 5144, num_iters>(f); \
    test_l1<dtype, 5152, num_iters>(f); \
    test_l1<dtype, 5160, num_iters>(f); \
    test_l1<dtype, 5168, num_iters>(f); \
    test_l1<dtype, 5176, num_iters>(f); \
    test_l1<dtype, 5184, num_iters>(f); \
    test_l1<dtype, 5192, num_iters>(f); \
    test_l1<dtype, 5200, num_iters>(f); \
    test_l1<dtype, 5208, num_iters>(f); \
    test_l1<dtype, 5216, num_iters>(f); \
    test_l1<dtype, 5224, num_iters>(f); \
    test_l1<dtype, 5232, num_iters>(f); \
    test_l1<dtype, 5240, num_iters>(f); \
    test_l1<dtype, 5248, num_iters>(f); \
    test_l1<dtype, 5256, num_iters>(f); \
    test_l1<dtype, 5264, num_iters>(f); \
    test_l1<dtype, 5272, num_iters>(f); \
    test_l1<dtype, 5280, num_iters>(f); \
    test_l1<dtype, 5288, num_iters>(f); \
    test_l1<dtype, 5296, num_iters>(f); \
    test_l1<dtype, 5304, num_iters>(f); \
    test_l1<dtype, 5312, num_iters>(f); \
    test_l1<dtype, 5320, num_iters>(f); \
    test_l1<dtype, 5328, num_iters>(f); \
    test_l1<dtype, 5336, num_iters>(f); \
    test_l1<dtype, 5344, num_iters>(f); \
    test_l1<dtype, 5352, num_iters>(f); \
    test_l1<dtype, 5360, num_iters>(f); \
    test_l1<dtype, 5368, num_iters>(f); \
    test_l1<dtype, 5376, num_iters>(f); \
    test_l1<dtype, 5384, num_iters>(f); \
    test_l1<dtype, 5392, num_iters>(f); \
    test_l1<dtype, 5400, num_iters>(f); \
    test_l1<dtype, 5408, num_iters>(f); \
    test_l1<dtype, 5416, num_iters>(f); \
    test_l1<dtype, 5424, num_iters>(f); \
    test_l1<dtype, 5432, num_iters>(f); \
    test_l1<dtype, 5440, num_iters>(f); \
    test_l1<dtype, 5448, num_iters>(f); \
    test_l1<dtype, 5456, num_iters>(f); \
    test_l1<dtype, 5464, num_iters>(f); \
    test_l1<dtype, 5472, num_iters>(f); \
    test_l1<dtype, 5480, num_iters>(f); \
    test_l1<dtype, 5488, num_iters>(f); \
    test_l1<dtype, 5496, num_iters>(f); \
    test_l1<dtype, 5504, num_iters>(f); \
    test_l1<dtype, 5512, num_iters>(f); \
    test_l1<dtype, 5520, num_iters>(f); \
    test_l1<dtype, 5528, num_iters>(f); \
    test_l1<dtype, 5536, num_iters>(f); \
    test_l1<dtype, 5544, num_iters>(f); \
    test_l1<dtype, 5552, num_iters>(f); \
    test_l1<dtype, 5560, num_iters>(f); \
    test_l1<dtype, 5568, num_iters>(f); \
    test_l1<dtype, 5576, num_iters>(f); \
    test_l1<dtype, 5584, num_iters>(f); \
    test_l1<dtype, 5592, num_iters>(f); \
    test_l1<dtype, 5600, num_iters>(f); \
    test_l1<dtype, 5608, num_iters>(f); \
    test_l1<dtype, 5616, num_iters>(f); \
    test_l1<dtype, 5624, num_iters>(f); \
    test_l1<dtype, 5632, num_iters>(f); \
    test_l1<dtype, 5640, num_iters>(f); \
    test_l1<dtype, 5648, num_iters>(f); \
    test_l1<dtype, 5656, num_iters>(f); \
    test_l1<dtype, 5664, num_iters>(f); \
    test_l1<dtype, 5672, num_iters>(f); \
    test_l1<dtype, 5680, num_iters>(f); \
    test_l1<dtype, 5688, num_iters>(f); \
    test_l1<dtype, 5696, num_iters>(f); \
    test_l1<dtype, 5704, num_iters>(f); \
    test_l1<dtype, 5712, num_iters>(f); \
    test_l1<dtype, 5720, num_iters>(f); \
    test_l1<dtype, 5728, num_iters>(f); \
    test_l1<dtype, 5736, num_iters>(f); \
    test_l1<dtype, 5744, num_iters>(f); \
    test_l1<dtype, 5752, num_iters>(f); \
    test_l1<dtype, 5760, num_iters>(f); \
    test_l1<dtype, 5768, num_iters>(f); \
    test_l1<dtype, 5776, num_iters>(f); \
    test_l1<dtype, 5784, num_iters>(f); \
    test_l1<dtype, 5792, num_iters>(f); \
    test_l1<dtype, 5800, num_iters>(f); \
    test_l1<dtype, 5808, num_iters>(f); \
    test_l1<dtype, 5816, num_iters>(f); \
    test_l1<dtype, 5824, num_iters>(f); \
    test_l1<dtype, 5832, num_iters>(f); \
    test_l1<dtype, 5840, num_iters>(f); \
    test_l1<dtype, 5848, num_iters>(f); \
    test_l1<dtype, 5856, num_iters>(f); \
    test_l1<dtype, 5864, num_iters>(f); \
    test_l1<dtype, 5872, num_iters>(f); \
    test_l1<dtype, 5880, num_iters>(f); \
    test_l1<dtype, 5888, num_iters>(f); \
    test_l1<dtype, 5896, num_iters>(f); \
    test_l1<dtype, 5904, num_iters>(f); \
    test_l1<dtype, 5912, num_iters>(f); \
    test_l1<dtype, 5920, num_iters>(f); \
    test_l1<dtype, 5928, num_iters>(f); \
    test_l1<dtype, 5936, num_iters>(f); \
    test_l1<dtype, 5944, num_iters>(f); \
    test_l1<dtype, 5952, num_iters>(f); \
    test_l1<dtype, 5960, num_iters>(f); \
    test_l1<dtype, 5968, num_iters>(f); \
    test_l1<dtype, 5976, num_iters>(f); \
    test_l1<dtype, 5984, num_iters>(f); \
    test_l1<dtype, 5992, num_iters>(f); \
    test_l1<dtype, 6000, num_iters>(f); \
    test_l1<dtype, 6008, num_iters>(f); \
    test_l1<dtype, 6016, num_iters>(f); \
    test_l1<dtype, 6024, num_iters>(f); \
    test_l1<dtype, 6032, num_iters>(f); \
    test_l1<dtype, 6040, num_iters>(f); \
    test_l1<dtype, 6048, num_iters>(f); \
    test_l1<dtype, 6056, num_iters>(f); \
    test_l1<dtype, 6064, num_iters>(f); \
    test_l1<dtype, 6072, num_iters>(f); \
    test_l1<dtype, 6080, num_iters>(f); \
    test_l1<dtype, 6088, num_iters>(f); \
    test_l1<dtype, 6096, num_iters>(f); \
    test_l1<dtype, 6104, num_iters>(f); \
    test_l1<dtype, 6112, num_iters>(f); \
    test_l1<dtype, 6120, num_iters>(f); \
    test_l1<dtype, 6128, num_iters>(f); \
    test_l1<dtype, 6136, num_iters>(f); \
    test_l1<dtype, 6144, num_iters>(f); \
    test_l1<dtype, 6152, num_iters>(f); \
    test_l1<dtype, 6160, num_iters>(f); \
    test_l1<dtype, 6168, num_iters>(f); \
    test_l1<dtype, 6176, num_iters>(f); \
    test_l1<dtype, 6184, num_iters>(f); \
    test_l1<dtype, 6192, num_iters>(f); \
    test_l1<dtype, 6200, num_iters>(f); \
    test_l1<dtype, 6208, num_iters>(f); \
    test_l1<dtype, 6216, num_iters>(f); \
    test_l1<dtype, 6224, num_iters>(f); \
    test_l1<dtype, 6232, num_iters>(f); \
    test_l1<dtype, 6240, num_iters>(f); \
    test_l1<dtype, 6248, num_iters>(f); \
    test_l1<dtype, 6256, num_iters>(f); \
    test_l1<dtype, 6264, num_iters>(f); \
    test_l1<dtype, 6272, num_iters>(f); \
    test_l1<dtype, 6280, num_iters>(f); \
    test_l1<dtype, 6288, num_iters>(f); \
    test_l1<dtype, 6296, num_iters>(f); \
    test_l1<dtype, 6304, num_iters>(f); \
    test_l1<dtype, 6312, num_iters>(f); \
    test_l1<dtype, 6320, num_iters>(f); \
    test_l1<dtype, 6328, num_iters>(f); \
    test_l1<dtype, 6336, num_iters>(f); \
    test_l1<dtype, 6344, num_iters>(f); \
    test_l1<dtype, 6352, num_iters>(f); \
    test_l1<dtype, 6360, num_iters>(f); \
    test_l1<dtype, 6368, num_iters>(f); \
    test_l1<dtype, 6376, num_iters>(f); \
    test_l1<dtype, 6384, num_iters>(f); \
    test_l1<dtype, 6392, num_iters>(f); \
    test_l1<dtype, 6400, num_iters>(f); \
    test_l1<dtype, 6408, num_iters>(f); \
    test_l1<dtype, 6416, num_iters>(f); \
    test_l1<dtype, 6424, num_iters>(f); \
    test_l1<dtype, 6432, num_iters>(f); \
    test_l1<dtype, 6440, num_iters>(f); \
    test_l1<dtype, 6448, num_iters>(f); \
    test_l1<dtype, 6456, num_iters>(f); \
    test_l1<dtype, 6464, num_iters>(f); \
    test_l1<dtype, 6472, num_iters>(f); \
    test_l1<dtype, 6480, num_iters>(f); \
    test_l1<dtype, 6488, num_iters>(f); \
    test_l1<dtype, 6496, num_iters>(f); \
    test_l1<dtype, 6504, num_iters>(f); \
    test_l1<dtype, 6512, num_iters>(f); \
    test_l1<dtype, 6520, num_iters>(f); \
    test_l1<dtype, 6528, num_iters>(f); \
    test_l1<dtype, 6536, num_iters>(f); \
    test_l1<dtype, 6544, num_iters>(f); \
    test_l1<dtype, 6552, num_iters>(f); \
    test_l1<dtype, 6560, num_iters>(f); \
    test_l1<dtype, 6568, num_iters>(f); \
    test_l1<dtype, 6576, num_iters>(f); \
    test_l1<dtype, 6584, num_iters>(f); \
    test_l1<dtype, 6592, num_iters>(f); \
    test_l1<dtype, 6600, num_iters>(f); \
    test_l1<dtype, 6608, num_iters>(f); \
    test_l1<dtype, 6616, num_iters>(f); \
    test_l1<dtype, 6624, num_iters>(f); \
    test_l1<dtype, 6632, num_iters>(f); \
    test_l1<dtype, 6640, num_iters>(f); \
    test_l1<dtype, 6648, num_iters>(f); \
    test_l1<dtype, 6656, num_iters>(f); \
    test_l1<dtype, 6664, num_iters>(f); \
    test_l1<dtype, 6672, num_iters>(f); \
    test_l1<dtype, 6680, num_iters>(f); \
    test_l1<dtype, 6688, num_iters>(f); \
    test_l1<dtype, 6696, num_iters>(f); \
    test_l1<dtype, 6704, num_iters>(f); \
    test_l1<dtype, 6712, num_iters>(f); \
    test_l1<dtype, 6720, num_iters>(f); \
    test_l1<dtype, 6728, num_iters>(f); \
    test_l1<dtype, 6736, num_iters>(f); \
    test_l1<dtype, 6744, num_iters>(f); \
    test_l1<dtype, 6752, num_iters>(f); \
    test_l1<dtype, 6760, num_iters>(f); \
    test_l1<dtype, 6768, num_iters>(f); \
    test_l1<dtype, 6776, num_iters>(f); \
    test_l1<dtype, 6784, num_iters>(f); \
    test_l1<dtype, 6792, num_iters>(f); \
    test_l1<dtype, 6800, num_iters>(f); \
    test_l1<dtype, 6808, num_iters>(f); \
    test_l1<dtype, 6816, num_iters>(f); \
    test_l1<dtype, 6824, num_iters>(f); \
    test_l1<dtype, 6832, num_iters>(f); \
    test_l1<dtype, 6840, num_iters>(f); \
    test_l1<dtype, 6848, num_iters>(f); \
    test_l1<dtype, 6856, num_iters>(f); \
    test_l1<dtype, 6864, num_iters>(f); \
    test_l1<dtype, 6872, num_iters>(f); \
    test_l1<dtype, 6880, num_iters>(f); \
    test_l1<dtype, 6888, num_iters>(f); \
    test_l1<dtype, 6896, num_iters>(f); \
    test_l1<dtype, 6904, num_iters>(f); \
    test_l1<dtype, 6912, num_iters>(f); \
    test_l1<dtype, 6920, num_iters>(f); \
    test_l1<dtype, 6928, num_iters>(f); \
    test_l1<dtype, 6936, num_iters>(f); \
    test_l1<dtype, 6944, num_iters>(f); \
    test_l1<dtype, 6952, num_iters>(f); \
    test_l1<dtype, 6960, num_iters>(f); \
    test_l1<dtype, 6968, num_iters>(f); \
    test_l1<dtype, 6976, num_iters>(f); \
    test_l1<dtype, 6984, num_iters>(f); \
    test_l1<dtype, 6992, num_iters>(f); \
    test_l1<dtype, 7000, num_iters>(f); \
    test_l1<dtype, 7008, num_iters>(f); \
    test_l1<dtype, 7016, num_iters>(f); \
    test_l1<dtype, 7024, num_iters>(f); \
    test_l1<dtype, 7032, num_iters>(f); \
    test_l1<dtype, 7040, num_iters>(f); \
    test_l1<dtype, 7048, num_iters>(f); \
    test_l1<dtype, 7056, num_iters>(f); \
    test_l1<dtype, 7064, num_iters>(f); \
    test_l1<dtype, 7072, num_iters>(f); \
    test_l1<dtype, 7080, num_iters>(f); \
    test_l1<dtype, 7088, num_iters>(f); \
    test_l1<dtype, 7096, num_iters>(f); \
    test_l1<dtype, 7104, num_iters>(f); \
    test_l1<dtype, 7112, num_iters>(f); \
    test_l1<dtype, 7120, num_iters>(f); \
    test_l1<dtype, 7128, num_iters>(f); \
    test_l1<dtype, 7136, num_iters>(f); \
    test_l1<dtype, 7144, num_iters>(f); \
    test_l1<dtype, 7152, num_iters>(f); \
    test_l1<dtype, 7160, num_iters>(f); \
    test_l1<dtype, 7168, num_iters>(f); \
    test_l1<dtype, 7176, num_iters>(f); \
    test_l1<dtype, 7184, num_iters>(f); \
    test_l1<dtype, 7192, num_iters>(f); \
    test_l1<dtype, 7200, num_iters>(f); \
    test_l1<dtype, 7208, num_iters>(f); \
    test_l1<dtype, 7216, num_iters>(f); \
    test_l1<dtype, 7224, num_iters>(f); \
    test_l1<dtype, 7232, num_iters>(f); \
    test_l1<dtype, 7240, num_iters>(f); \
    test_l1<dtype, 7248, num_iters>(f); \
    test_l1<dtype, 7256, num_iters>(f); \
    test_l1<dtype, 7264, num_iters>(f); \
    test_l1<dtype, 7272, num_iters>(f); \
    test_l1<dtype, 7280, num_iters>(f); \
    test_l1<dtype, 7288, num_iters>(f); \
    test_l1<dtype, 7296, num_iters>(f); \
    test_l1<dtype, 7304, num_iters>(f); \
    test_l1<dtype, 7312, num_iters>(f); \
    test_l1<dtype, 7320, num_iters>(f); \
    test_l1<dtype, 7328, num_iters>(f); \
    test_l1<dtype, 7336, num_iters>(f); \
    test_l1<dtype, 7344, num_iters>(f); \
    test_l1<dtype, 7352, num_iters>(f); \
    test_l1<dtype, 7360, num_iters>(f); \
    test_l1<dtype, 7368, num_iters>(f); \
    test_l1<dtype, 7376, num_iters>(f); \
    test_l1<dtype, 7384, num_iters>(f); \
    test_l1<dtype, 7392, num_iters>(f); \
    test_l1<dtype, 7400, num_iters>(f); \
    test_l1<dtype, 7408, num_iters>(f); \
    test_l1<dtype, 7416, num_iters>(f); \
    test_l1<dtype, 7424, num_iters>(f); \
    test_l1<dtype, 7432, num_iters>(f); \
    test_l1<dtype, 7440, num_iters>(f); \
    test_l1<dtype, 7448, num_iters>(f); \
    test_l1<dtype, 7456, num_iters>(f); \
    test_l1<dtype, 7464, num_iters>(f); \
    test_l1<dtype, 7472, num_iters>(f); \
    test_l1<dtype, 7480, num_iters>(f); \
    test_l1<dtype, 7488, num_iters>(f); \
    test_l1<dtype, 7496, num_iters>(f); \
    test_l1<dtype, 7504, num_iters>(f); \
    test_l1<dtype, 7512, num_iters>(f); \
    test_l1<dtype, 7520, num_iters>(f); \
    test_l1<dtype, 7528, num_iters>(f); \
    test_l1<dtype, 7536, num_iters>(f); \
    test_l1<dtype, 7544, num_iters>(f); \
    test_l1<dtype, 7552, num_iters>(f); \
    test_l1<dtype, 7560, num_iters>(f); \
    test_l1<dtype, 7568, num_iters>(f); \
    test_l1<dtype, 7576, num_iters>(f); \
    test_l1<dtype, 7584, num_iters>(f); \
    test_l1<dtype, 7592, num_iters>(f); \
    test_l1<dtype, 7600, num_iters>(f); \
    test_l1<dtype, 7608, num_iters>(f); \
    test_l1<dtype, 7616, num_iters>(f); \
    test_l1<dtype, 7624, num_iters>(f); \
    test_l1<dtype, 7632, num_iters>(f); \
    test_l1<dtype, 7640, num_iters>(f); \
    test_l1<dtype, 7648, num_iters>(f); \
    test_l1<dtype, 7656, num_iters>(f); \
    test_l1<dtype, 7664, num_iters>(f); \
    test_l1<dtype, 7672, num_iters>(f); \
    test_l1<dtype, 7680, num_iters>(f); \
    test_l1<dtype, 7688, num_iters>(f); \
    test_l1<dtype, 7696, num_iters>(f); \
    test_l1<dtype, 7704, num_iters>(f); \
    test_l1<dtype, 7712, num_iters>(f); \
    test_l1<dtype, 7720, num_iters>(f); \
    test_l1<dtype, 7728, num_iters>(f); \
    test_l1<dtype, 7736, num_iters>(f); \
    test_l1<dtype, 7744, num_iters>(f); \
    test_l1<dtype, 7752, num_iters>(f); \
    test_l1<dtype, 7760, num_iters>(f); \
    test_l1<dtype, 7768, num_iters>(f); \
    test_l1<dtype, 7776, num_iters>(f); \
    test_l1<dtype, 7784, num_iters>(f); \
    test_l1<dtype, 7792, num_iters>(f); \
    test_l1<dtype, 7800, num_iters>(f); \
    test_l1<dtype, 7808, num_iters>(f); \
    test_l1<dtype, 7816, num_iters>(f); \
    test_l1<dtype, 7824, num_iters>(f); \
    test_l1<dtype, 7832, num_iters>(f); \
    test_l1<dtype, 7840, num_iters>(f); \
    test_l1<dtype, 7848, num_iters>(f); \
    test_l1<dtype, 7856, num_iters>(f); \
    test_l1<dtype, 7864, num_iters>(f); \
    test_l1<dtype, 7872, num_iters>(f); \
    test_l1<dtype, 7880, num_iters>(f); \
    test_l1<dtype, 7888, num_iters>(f); \
    test_l1<dtype, 7896, num_iters>(f); \
    test_l1<dtype, 7904, num_iters>(f); \
    test_l1<dtype, 7912, num_iters>(f); \
    test_l1<dtype, 7920, num_iters>(f); \
    test_l1<dtype, 7928, num_iters>(f); \
    test_l1<dtype, 7936, num_iters>(f); \
    test_l1<dtype, 7944, num_iters>(f); \
    test_l1<dtype, 7952, num_iters>(f); \
    test_l1<dtype, 7960, num_iters>(f); \
    test_l1<dtype, 7968, num_iters>(f); \
    test_l1<dtype, 7976, num_iters>(f); \
    test_l1<dtype, 7984, num_iters>(f); \
    test_l1<dtype, 7992, num_iters>(f); \
    test_l1<dtype, 8000, num_iters>(f); \
    test_l1<dtype, 8008, num_iters>(f); \
    test_l1<dtype, 8016, num_iters>(f); \
    test_l1<dtype, 8024, num_iters>(f); \
    test_l1<dtype, 8032, num_iters>(f); \
    test_l1<dtype, 8040, num_iters>(f); \
    test_l1<dtype, 8048, num_iters>(f); \
    test_l1<dtype, 8056, num_iters>(f); \
    test_l1<dtype, 8064, num_iters>(f); \
    test_l1<dtype, 8072, num_iters>(f); \
    test_l1<dtype, 8080, num_iters>(f); \
    test_l1<dtype, 8088, num_iters>(f); \
    test_l1<dtype, 8096, num_iters>(f); \
    test_l1<dtype, 8104, num_iters>(f); \
    test_l1<dtype, 8112, num_iters>(f); \
    test_l1<dtype, 8120, num_iters>(f); \
    test_l1<dtype, 8128, num_iters>(f); \
    test_l1<dtype, 8136, num_iters>(f); \
    test_l1<dtype, 8144, num_iters>(f); \
    test_l1<dtype, 8152, num_iters>(f); \
    test_l1<dtype, 8160, num_iters>(f); \
    test_l1<dtype, 8168, num_iters>(f); \
    test_l1<dtype, 8176, num_iters>(f); \
    test_l1<dtype, 8184, num_iters>(f); \
    test_l1<dtype, 8192, num_iters>(f); \
    test_l1<dtype, 8200, num_iters>(f); \
    test_l1<dtype, 8208, num_iters>(f); \
    test_l1<dtype, 8216, num_iters>(f); \
    test_l1<dtype, 8224, num_iters>(f); \
    test_l1<dtype, 8232, num_iters>(f); \
    test_l1<dtype, 8240, num_iters>(f); \
    test_l1<dtype, 8248, num_iters>(f); \
    test_l1<dtype, 8256, num_iters>(f); \
    test_l1<dtype, 8264, num_iters>(f); \
    test_l1<dtype, 8272, num_iters>(f); \
    test_l1<dtype, 8280, num_iters>(f); \
    test_l1<dtype, 8288, num_iters>(f); \
    test_l1<dtype, 8296, num_iters>(f); \
    test_l1<dtype, 8304, num_iters>(f); \
    test_l1<dtype, 8312, num_iters>(f); \
    test_l1<dtype, 8320, num_iters>(f); \
    test_l1<dtype, 8328, num_iters>(f); \
    test_l1<dtype, 8336, num_iters>(f); \
    test_l1<dtype, 8344, num_iters>(f); \
    test_l1<dtype, 8352, num_iters>(f); \
    test_l1<dtype, 8360, num_iters>(f); \
    test_l1<dtype, 8368, num_iters>(f); \
    test_l1<dtype, 8376, num_iters>(f); \
    test_l1<dtype, 8384, num_iters>(f); \
    test_l1<dtype, 8392, num_iters>(f); \
    test_l1<dtype, 8400, num_iters>(f); \
    test_l1<dtype, 8408, num_iters>(f); \
    test_l1<dtype, 8416, num_iters>(f); \
    test_l1<dtype, 8424, num_iters>(f); \
    test_l1<dtype, 8432, num_iters>(f); \
    test_l1<dtype, 8440, num_iters>(f); \
    test_l1<dtype, 8448, num_iters>(f); \
    test_l1<dtype, 8456, num_iters>(f); \
    test_l1<dtype, 8464, num_iters>(f); \
    test_l1<dtype, 8472, num_iters>(f); \
    test_l1<dtype, 8480, num_iters>(f); \
    test_l1<dtype, 8488, num_iters>(f); \
    test_l1<dtype, 8496, num_iters>(f); \
    test_l1<dtype, 8504, num_iters>(f); \
    test_l1<dtype, 8512, num_iters>(f); \
    test_l1<dtype, 8520, num_iters>(f); \
    test_l1<dtype, 8528, num_iters>(f); \
    test_l1<dtype, 8536, num_iters>(f); \
    test_l1<dtype, 8544, num_iters>(f); \
    test_l1<dtype, 8552, num_iters>(f); \
    test_l1<dtype, 8560, num_iters>(f); \
    test_l1<dtype, 8568, num_iters>(f); \
    test_l1<dtype, 8576, num_iters>(f); \
    test_l1<dtype, 8584, num_iters>(f); \
    test_l1<dtype, 8592, num_iters>(f); \
    test_l1<dtype, 8600, num_iters>(f); \
    test_l1<dtype, 8608, num_iters>(f); \
    test_l1<dtype, 8616, num_iters>(f); \
    test_l1<dtype, 8624, num_iters>(f); \
    test_l1<dtype, 8632, num_iters>(f); \
    test_l1<dtype, 8640, num_iters>(f); \
    test_l1<dtype, 8648, num_iters>(f); \
    test_l1<dtype, 8656, num_iters>(f); \
    test_l1<dtype, 8664, num_iters>(f); \
    test_l1<dtype, 8672, num_iters>(f); \
    test_l1<dtype, 8680, num_iters>(f); \
    test_l1<dtype, 8688, num_iters>(f); \
    test_l1<dtype, 8696, num_iters>(f); \
    test_l1<dtype, 8704, num_iters>(f); \
    test_l1<dtype, 8712, num_iters>(f); \
    test_l1<dtype, 8720, num_iters>(f); \
    test_l1<dtype, 8728, num_iters>(f); \
    test_l1<dtype, 8736, num_iters>(f); \
    test_l1<dtype, 8744, num_iters>(f); \
    test_l1<dtype, 8752, num_iters>(f); \
    test_l1<dtype, 8760, num_iters>(f); \
    test_l1<dtype, 8768, num_iters>(f); \
    test_l1<dtype, 8776, num_iters>(f); \
    test_l1<dtype, 8784, num_iters>(f); \
    test_l1<dtype, 8792, num_iters>(f); \
    test_l1<dtype, 8800, num_iters>(f); \
    test_l1<dtype, 8808, num_iters>(f); \
    test_l1<dtype, 8816, num_iters>(f); \
    test_l1<dtype, 8824, num_iters>(f); \
    test_l1<dtype, 8832, num_iters>(f); \
    test_l1<dtype, 8840, num_iters>(f); \
    test_l1<dtype, 8848, num_iters>(f); \
    test_l1<dtype, 8856, num_iters>(f); \
    test_l1<dtype, 8864, num_iters>(f); \
    test_l1<dtype, 8872, num_iters>(f); \
    test_l1<dtype, 8880, num_iters>(f); \
    test_l1<dtype, 8888, num_iters>(f); \
    test_l1<dtype, 8896, num_iters>(f); \
    test_l1<dtype, 8904, num_iters>(f); \
    test_l1<dtype, 8912, num_iters>(f); \
    test_l1<dtype, 8920, num_iters>(f); \
    test_l1<dtype, 8928, num_iters>(f); \
    test_l1<dtype, 8936, num_iters>(f); \
    test_l1<dtype, 8944, num_iters>(f); \
    test_l1<dtype, 8952, num_iters>(f); \
    test_l1<dtype, 8960, num_iters>(f); \
    test_l1<dtype, 8968, num_iters>(f); \
    test_l1<dtype, 8976, num_iters>(f); \
    test_l1<dtype, 8984, num_iters>(f); \
    test_l1<dtype, 8992, num_iters>(f); \
    test_l1<dtype, 9000, num_iters>(f); \
    test_l1<dtype, 9008, num_iters>(f); \
    test_l1<dtype, 9016, num_iters>(f); \
    test_l1<dtype, 9024, num_iters>(f); \
    test_l1<dtype, 9032, num_iters>(f); \
    test_l1<dtype, 9040, num_iters>(f); \
    test_l1<dtype, 9048, num_iters>(f); \
    test_l1<dtype, 9056, num_iters>(f); \
    test_l1<dtype, 9064, num_iters>(f); \
    test_l1<dtype, 9072, num_iters>(f); \
    test_l1<dtype, 9080, num_iters>(f); \
    test_l1<dtype, 9088, num_iters>(f); \
    test_l1<dtype, 9096, num_iters>(f); \
    test_l1<dtype, 9104, num_iters>(f); \
    test_l1<dtype, 9112, num_iters>(f); \
    test_l1<dtype, 9120, num_iters>(f); \
    test_l1<dtype, 9128, num_iters>(f); \
    test_l1<dtype, 9136, num_iters>(f); \
    test_l1<dtype, 9144, num_iters>(f); \
    test_l1<dtype, 9152, num_iters>(f); \
    test_l1<dtype, 9160, num_iters>(f); \
    test_l1<dtype, 9168, num_iters>(f); \
    test_l1<dtype, 9176, num_iters>(f); \
    test_l1<dtype, 9184, num_iters>(f); \
    test_l1<dtype, 9192, num_iters>(f); \
    test_l1<dtype, 9200, num_iters>(f); \
    test_l1<dtype, 9208, num_iters>(f); \
    test_l1<dtype, 9216, num_iters>(f); \
    test_l1<dtype, 9224, num_iters>(f); \
    test_l1<dtype, 9232, num_iters>(f); \
    test_l1<dtype, 9240, num_iters>(f); \
    test_l1<dtype, 9248, num_iters>(f); \
    test_l1<dtype, 9256, num_iters>(f); \
    test_l1<dtype, 9264, num_iters>(f); \
    test_l1<dtype, 9272, num_iters>(f); \
    test_l1<dtype, 9280, num_iters>(f); \
    test_l1<dtype, 9288, num_iters>(f); \
    test_l1<dtype, 9296, num_iters>(f); \
    test_l1<dtype, 9304, num_iters>(f); \
    test_l1<dtype, 9312, num_iters>(f); \
    test_l1<dtype, 9320, num_iters>(f); \
    test_l1<dtype, 9328, num_iters>(f); \
    test_l1<dtype, 9336, num_iters>(f); \
    test_l1<dtype, 9344, num_iters>(f); \
    test_l1<dtype, 9352, num_iters>(f); \
    test_l1<dtype, 9360, num_iters>(f); \
    test_l1<dtype, 9368, num_iters>(f); \
    test_l1<dtype, 9376, num_iters>(f); \
    test_l1<dtype, 9384, num_iters>(f); \
    test_l1<dtype, 9392, num_iters>(f); \
    test_l1<dtype, 9400, num_iters>(f); \
    test_l1<dtype, 9408, num_iters>(f); \
    test_l1<dtype, 9416, num_iters>(f); \
    test_l1<dtype, 9424, num_iters>(f); \
    test_l1<dtype, 9432, num_iters>(f); \
    test_l1<dtype, 9440, num_iters>(f); \
    test_l1<dtype, 9448, num_iters>(f); \
    test_l1<dtype, 9456, num_iters>(f); \
    test_l1<dtype, 9464, num_iters>(f); \
    test_l1<dtype, 9472, num_iters>(f); \
    test_l1<dtype, 9480, num_iters>(f); \
    test_l1<dtype, 9488, num_iters>(f); \
    test_l1<dtype, 9496, num_iters>(f); \
    test_l1<dtype, 9504, num_iters>(f); \
    test_l1<dtype, 9512, num_iters>(f); \
    test_l1<dtype, 9520, num_iters>(f); \
    test_l1<dtype, 9528, num_iters>(f); \
    test_l1<dtype, 9536, num_iters>(f); \
    test_l1<dtype, 9544, num_iters>(f); \
    test_l1<dtype, 9552, num_iters>(f); \
    test_l1<dtype, 9560, num_iters>(f); \
    test_l1<dtype, 9568, num_iters>(f); \
    test_l1<dtype, 9576, num_iters>(f); \
    test_l1<dtype, 9584, num_iters>(f); \
    test_l1<dtype, 9592, num_iters>(f); \
    test_l1<dtype, 9600, num_iters>(f); \
    test_l1<dtype, 9608, num_iters>(f); \
    test_l1<dtype, 9616, num_iters>(f); \
    test_l1<dtype, 9624, num_iters>(f); \
    test_l1<dtype, 9632, num_iters>(f); \
    test_l1<dtype, 9640, num_iters>(f); \
    test_l1<dtype, 9648, num_iters>(f); \
    test_l1<dtype, 9656, num_iters>(f); \
    test_l1<dtype, 9664, num_iters>(f); \
    test_l1<dtype, 9672, num_iters>(f); \
    test_l1<dtype, 9680, num_iters>(f); \
    test_l1<dtype, 9688, num_iters>(f); \
    test_l1<dtype, 9696, num_iters>(f); \
    test_l1<dtype, 9704, num_iters>(f); \
    test_l1<dtype, 9712, num_iters>(f); \
    test_l1<dtype, 9720, num_iters>(f); \
    test_l1<dtype, 9728, num_iters>(f); \
    test_l1<dtype, 9736, num_iters>(f); \
    test_l1<dtype, 9744, num_iters>(f); \
    test_l1<dtype, 9752, num_iters>(f); \
    test_l1<dtype, 9760, num_iters>(f); \
    test_l1<dtype, 9768, num_iters>(f); \
    test_l1<dtype, 9776, num_iters>(f); \
    test_l1<dtype, 9784, num_iters>(f); \
    test_l1<dtype, 9792, num_iters>(f); \
    test_l1<dtype, 9800, num_iters>(f); \
    test_l1<dtype, 9808, num_iters>(f); \
    test_l1<dtype, 9816, num_iters>(f); \
    test_l1<dtype, 9824, num_iters>(f); \
    test_l1<dtype, 9832, num_iters>(f); \
    test_l1<dtype, 9840, num_iters>(f); \
    test_l1<dtype, 9848, num_iters>(f); \
    test_l1<dtype, 9856, num_iters>(f); \
    test_l1<dtype, 9864, num_iters>(f); \
    test_l1<dtype, 9872, num_iters>(f); \
    test_l1<dtype, 9880, num_iters>(f); \
    test_l1<dtype, 9888, num_iters>(f); \
    test_l1<dtype, 9896, num_iters>(f); \
    test_l1<dtype, 9904, num_iters>(f); \
    test_l1<dtype, 9912, num_iters>(f); \
    test_l1<dtype, 9920, num_iters>(f); \
    test_l1<dtype, 9928, num_iters>(f); \
    test_l1<dtype, 9936, num_iters>(f); \
    test_l1<dtype, 9944, num_iters>(f); \
    test_l1<dtype, 9952, num_iters>(f); \
    test_l1<dtype, 9960, num_iters>(f); \
    test_l1<dtype, 9968, num_iters>(f); \
    test_l1<dtype, 9976, num_iters>(f); \
    test_l1<dtype, 9984, num_iters>(f); \
    test_l1<dtype, 9992, num_iters>(f); \
    test_l1<dtype, 10000, num_iters>(f); \
    test_l1<dtype, 10008, num_iters>(f); \
    test_l1<dtype, 10016, num_iters>(f); \
    test_l1<dtype, 10024, num_iters>(f); \
    test_l1<dtype, 10032, num_iters>(f); \
    test_l1<dtype, 10040, num_iters>(f); \
    test_l1<dtype, 10048, num_iters>(f); \
    test_l1<dtype, 10056, num_iters>(f); \
    test_l1<dtype, 10064, num_iters>(f); \
    test_l1<dtype, 10072, num_iters>(f); \
    test_l1<dtype, 10080, num_iters>(f); \
    test_l1<dtype, 10088, num_iters>(f); \
    test_l1<dtype, 10096, num_iters>(f); \
    test_l1<dtype, 10104, num_iters>(f); \
    test_l1<dtype, 10112, num_iters>(f); \
    test_l1<dtype, 10120, num_iters>(f); \
    test_l1<dtype, 10128, num_iters>(f); \
    test_l1<dtype, 10136, num_iters>(f); \
    test_l1<dtype, 10144, num_iters>(f); \
    test_l1<dtype, 10152, num_iters>(f); \
    test_l1<dtype, 10160, num_iters>(f); \
    test_l1<dtype, 10168, num_iters>(f); \
    test_l1<dtype, 10176, num_iters>(f); \
    test_l1<dtype, 10184, num_iters>(f); \
    test_l1<dtype, 10192, num_iters>(f); \
    test_l1<dtype, 10200, num_iters>(f); \
    test_l1<dtype, 10208, num_iters>(f); \
    test_l1<dtype, 10216, num_iters>(f); \
    test_l1<dtype, 10224, num_iters>(f); \
    test_l1<dtype, 10232, num_iters>(f); \
    test_l1<dtype, 10240, num_iters>(f); \
    test_l1<dtype, 10248, num_iters>(f); \
    test_l1<dtype, 10256, num_iters>(f); \
    test_l1<dtype, 10264, num_iters>(f); \
    test_l1<dtype, 10272, num_iters>(f); \
    test_l1<dtype, 10280, num_iters>(f); \
    test_l1<dtype, 10288, num_iters>(f); \
    test_l1<dtype, 10296, num_iters>(f); \
    test_l1<dtype, 10304, num_iters>(f); \
    test_l1<dtype, 10312, num_iters>(f); \
    test_l1<dtype, 10320, num_iters>(f); \
    test_l1<dtype, 10328, num_iters>(f); \
    test_l1<dtype, 10336, num_iters>(f); \
    test_l1<dtype, 10344, num_iters>(f); \
    test_l1<dtype, 10352, num_iters>(f); \
    test_l1<dtype, 10360, num_iters>(f); \
    test_l1<dtype, 10368, num_iters>(f); \
    test_l1<dtype, 10376, num_iters>(f); \
    test_l1<dtype, 10384, num_iters>(f); \
    test_l1<dtype, 10392, num_iters>(f); \
    test_l1<dtype, 10400, num_iters>(f); \
    test_l1<dtype, 10408, num_iters>(f); \
    test_l1<dtype, 10416, num_iters>(f); \
    test_l1<dtype, 10424, num_iters>(f); \
    test_l1<dtype, 10432, num_iters>(f); \
    test_l1<dtype, 10440, num_iters>(f); \
    test_l1<dtype, 10448, num_iters>(f); \
    test_l1<dtype, 10456, num_iters>(f); \
    test_l1<dtype, 10464, num_iters>(f); \
    test_l1<dtype, 10472, num_iters>(f); \
    test_l1<dtype, 10480, num_iters>(f); \
    test_l1<dtype, 10488, num_iters>(f); \
    test_l1<dtype, 10496, num_iters>(f); \
    test_l1<dtype, 10504, num_iters>(f); \
    test_l1<dtype, 10512, num_iters>(f); \
    test_l1<dtype, 10520, num_iters>(f); \
    test_l1<dtype, 10528, num_iters>(f); \
    test_l1<dtype, 10536, num_iters>(f); \
    test_l1<dtype, 10544, num_iters>(f); \
    test_l1<dtype, 10552, num_iters>(f); \
    test_l1<dtype, 10560, num_iters>(f); \
    test_l1<dtype, 10568, num_iters>(f); \
    test_l1<dtype, 10576, num_iters>(f); \
    test_l1<dtype, 10584, num_iters>(f); \
    test_l1<dtype, 10592, num_iters>(f); \
    test_l1<dtype, 10600, num_iters>(f); \
    test_l1<dtype, 10608, num_iters>(f); \
    test_l1<dtype, 10616, num_iters>(f); \
    test_l1<dtype, 10624, num_iters>(f); \
    test_l1<dtype, 10632, num_iters>(f); \
    test_l1<dtype, 10640, num_iters>(f); \
    test_l1<dtype, 10648, num_iters>(f); \
    test_l1<dtype, 10656, num_iters>(f); \
    test_l1<dtype, 10664, num_iters>(f); \
    test_l1<dtype, 10672, num_iters>(f); \
    test_l1<dtype, 10680, num_iters>(f); \
    test_l1<dtype, 10688, num_iters>(f); \
    test_l1<dtype, 10696, num_iters>(f); \
    test_l1<dtype, 10704, num_iters>(f); \
    test_l1<dtype, 10712, num_iters>(f); \
    test_l1<dtype, 10720, num_iters>(f); \
    test_l1<dtype, 10728, num_iters>(f); \
    test_l1<dtype, 10736, num_iters>(f); \
    test_l1<dtype, 10744, num_iters>(f); \
    test_l1<dtype, 10752, num_iters>(f); \
    test_l1<dtype, 10760, num_iters>(f); \
    test_l1<dtype, 10768, num_iters>(f); \
    test_l1<dtype, 10776, num_iters>(f); \
    test_l1<dtype, 10784, num_iters>(f); \
    test_l1<dtype, 10792, num_iters>(f); \
    test_l1<dtype, 10800, num_iters>(f); \
    test_l1<dtype, 10808, num_iters>(f); \
    test_l1<dtype, 10816, num_iters>(f); \
    test_l1<dtype, 10824, num_iters>(f); \
    test_l1<dtype, 10832, num_iters>(f); \
    test_l1<dtype, 10840, num_iters>(f); \
    test_l1<dtype, 10848, num_iters>(f); \
    test_l1<dtype, 10856, num_iters>(f); \
    test_l1<dtype, 10864, num_iters>(f); \
    test_l1<dtype, 10872, num_iters>(f); \
    test_l1<dtype, 10880, num_iters>(f); \
    test_l1<dtype, 10888, num_iters>(f); \
    test_l1<dtype, 10896, num_iters>(f); \
    test_l1<dtype, 10904, num_iters>(f); \
    test_l1<dtype, 10912, num_iters>(f); \
    test_l1<dtype, 10920, num_iters>(f); \
    test_l1<dtype, 10928, num_iters>(f); \
    test_l1<dtype, 10936, num_iters>(f); \
    test_l1<dtype, 10944, num_iters>(f); \
    test_l1<dtype, 10952, num_iters>(f); \
    test_l1<dtype, 10960, num_iters>(f); \
    test_l1<dtype, 10968, num_iters>(f); \
    test_l1<dtype, 10976, num_iters>(f); \
    test_l1<dtype, 10984, num_iters>(f); \
    test_l1<dtype, 10992, num_iters>(f); \
    test_l1<dtype, 11000, num_iters>(f); \
    test_l1<dtype, 11008, num_iters>(f); \
    test_l1<dtype, 11016, num_iters>(f); \
    test_l1<dtype, 11024, num_iters>(f); \
    test_l1<dtype, 11032, num_iters>(f); \
    test_l1<dtype, 11040, num_iters>(f); \
    test_l1<dtype, 11048, num_iters>(f); \
    test_l1<dtype, 11056, num_iters>(f); \
    test_l1<dtype, 11064, num_iters>(f); \
    test_l1<dtype, 11072, num_iters>(f); \
    test_l1<dtype, 11080, num_iters>(f); \
    test_l1<dtype, 11088, num_iters>(f); \
    test_l1<dtype, 11096, num_iters>(f); \
    test_l1<dtype, 11104, num_iters>(f); \
    test_l1<dtype, 11112, num_iters>(f); \
    test_l1<dtype, 11120, num_iters>(f); \
    test_l1<dtype, 11128, num_iters>(f); \
    test_l1<dtype, 11136, num_iters>(f); \
    test_l1<dtype, 11144, num_iters>(f); \
    test_l1<dtype, 11152, num_iters>(f); \
    test_l1<dtype, 11160, num_iters>(f); \
    test_l1<dtype, 11168, num_iters>(f); \
    test_l1<dtype, 11176, num_iters>(f); \
    test_l1<dtype, 11184, num_iters>(f); \
    test_l1<dtype, 11192, num_iters>(f); \
    test_l1<dtype, 11200, num_iters>(f); \
    test_l1<dtype, 11208, num_iters>(f); \
    test_l1<dtype, 11216, num_iters>(f); \
    test_l1<dtype, 11224, num_iters>(f); \
    test_l1<dtype, 11232, num_iters>(f); \
    test_l1<dtype, 11240, num_iters>(f); \
    test_l1<dtype, 11248, num_iters>(f); \
    test_l1<dtype, 11256, num_iters>(f); \
    test_l1<dtype, 11264, num_iters>(f); \
    test_l1<dtype, 11272, num_iters>(f); \
    test_l1<dtype, 11280, num_iters>(f); \
    test_l1<dtype, 11288, num_iters>(f); \
    test_l1<dtype, 11296, num_iters>(f); \
    test_l1<dtype, 11304, num_iters>(f); \
    test_l1<dtype, 11312, num_iters>(f); \
    test_l1<dtype, 11320, num_iters>(f); \
    test_l1<dtype, 11328, num_iters>(f); \
    test_l1<dtype, 11336, num_iters>(f); \
    test_l1<dtype, 11344, num_iters>(f); \
    test_l1<dtype, 11352, num_iters>(f); \
    test_l1<dtype, 11360, num_iters>(f); \
    test_l1<dtype, 11368, num_iters>(f); \
    test_l1<dtype, 11376, num_iters>(f); \
    test_l1<dtype, 11384, num_iters>(f); \
    test_l1<dtype, 11392, num_iters>(f); \
    test_l1<dtype, 11400, num_iters>(f); \
    test_l1<dtype, 11408, num_iters>(f); \
    test_l1<dtype, 11416, num_iters>(f); \
    test_l1<dtype, 11424, num_iters>(f); \
    test_l1<dtype, 11432, num_iters>(f); \
    test_l1<dtype, 11440, num_iters>(f); \
    test_l1<dtype, 11448, num_iters>(f); \
    test_l1<dtype, 11456, num_iters>(f); \
    test_l1<dtype, 11464, num_iters>(f); \
    test_l1<dtype, 11472, num_iters>(f); \
    test_l1<dtype, 11480, num_iters>(f); \
    test_l1<dtype, 11488, num_iters>(f); \
    test_l1<dtype, 11496, num_iters>(f); \
    test_l1<dtype, 11504, num_iters>(f); \
    test_l1<dtype, 11512, num_iters>(f); \
    test_l1<dtype, 11520, num_iters>(f); \
    test_l1<dtype, 11528, num_iters>(f); \
    test_l1<dtype, 11536, num_iters>(f); \
    test_l1<dtype, 11544, num_iters>(f); \
    test_l1<dtype, 11552, num_iters>(f); \
    test_l1<dtype, 11560, num_iters>(f); \
    test_l1<dtype, 11568, num_iters>(f); \
    test_l1<dtype, 11576, num_iters>(f); \
    test_l1<dtype, 11584, num_iters>(f); \
    test_l1<dtype, 11592, num_iters>(f); \
    test_l1<dtype, 11600, num_iters>(f); \
    test_l1<dtype, 11608, num_iters>(f); \
    test_l1<dtype, 11616, num_iters>(f); \
    test_l1<dtype, 11624, num_iters>(f); \
    test_l1<dtype, 11632, num_iters>(f); \
    test_l1<dtype, 11640, num_iters>(f); \
    test_l1<dtype, 11648, num_iters>(f); \
    test_l1<dtype, 11656, num_iters>(f); \
    test_l1<dtype, 11664, num_iters>(f); \
    test_l1<dtype, 11672, num_iters>(f); \
    test_l1<dtype, 11680, num_iters>(f); \
    test_l1<dtype, 11688, num_iters>(f); \
    test_l1<dtype, 11696, num_iters>(f); \
    test_l1<dtype, 11704, num_iters>(f); \
    test_l1<dtype, 11712, num_iters>(f); \
    test_l1<dtype, 11720, num_iters>(f); \
    test_l1<dtype, 11728, num_iters>(f); \
    test_l1<dtype, 11736, num_iters>(f); \
    test_l1<dtype, 11744, num_iters>(f); \
    test_l1<dtype, 11752, num_iters>(f); \
    test_l1<dtype, 11760, num_iters>(f); \
    test_l1<dtype, 11768, num_iters>(f); \
    test_l1<dtype, 11776, num_iters>(f); \
    test_l1<dtype, 11784, num_iters>(f); \
    test_l1<dtype, 11792, num_iters>(f); \
    test_l1<dtype, 11800, num_iters>(f); \
    test_l1<dtype, 11808, num_iters>(f); \
    test_l1<dtype, 11816, num_iters>(f); \
    test_l1<dtype, 11824, num_iters>(f); \
    test_l1<dtype, 11832, num_iters>(f); \
    test_l1<dtype, 11840, num_iters>(f); \
    test_l1<dtype, 11848, num_iters>(f); \
    test_l1<dtype, 11856, num_iters>(f); \
    test_l1<dtype, 11864, num_iters>(f); \
    test_l1<dtype, 11872, num_iters>(f); \
    test_l1<dtype, 11880, num_iters>(f); \
    test_l1<dtype, 11888, num_iters>(f); \
    test_l1<dtype, 11896, num_iters>(f); \
    test_l1<dtype, 11904, num_iters>(f); \
    test_l1<dtype, 11912, num_iters>(f); \
    test_l1<dtype, 11920, num_iters>(f); \
    test_l1<dtype, 11928, num_iters>(f); \
    test_l1<dtype, 11936, num_iters>(f); \
    test_l1<dtype, 11944, num_iters>(f); \
    test_l1<dtype, 11952, num_iters>(f); \
    test_l1<dtype, 11960, num_iters>(f); \
    test_l1<dtype, 11968, num_iters>(f); \
    test_l1<dtype, 11976, num_iters>(f); \
    test_l1<dtype, 11984, num_iters>(f); \
    test_l1<dtype, 11992, num_iters>(f); \
    test_l1<dtype, 12000, num_iters>(f); \
    test_l1<dtype, 12008, num_iters>(f); \
    test_l1<dtype, 12016, num_iters>(f); \
    test_l1<dtype, 12024, num_iters>(f); \
    test_l1<dtype, 12032, num_iters>(f); \
    test_l1<dtype, 12040, num_iters>(f); \
    test_l1<dtype, 12048, num_iters>(f); \
    test_l1<dtype, 12056, num_iters>(f); \
    test_l1<dtype, 12064, num_iters>(f); \
    test_l1<dtype, 12072, num_iters>(f); \
    test_l1<dtype, 12080, num_iters>(f); \
    test_l1<dtype, 12088, num_iters>(f); \
    test_l1<dtype, 12096, num_iters>(f); \
    test_l1<dtype, 12104, num_iters>(f); \
    test_l1<dtype, 12112, num_iters>(f); \
    test_l1<dtype, 12120, num_iters>(f); \
    test_l1<dtype, 12128, num_iters>(f); \
    test_l1<dtype, 12136, num_iters>(f); \
    test_l1<dtype, 12144, num_iters>(f); \
    test_l1<dtype, 12152, num_iters>(f); \
    test_l1<dtype, 12160, num_iters>(f); \
    test_l1<dtype, 12168, num_iters>(f); \
    test_l1<dtype, 12176, num_iters>(f); \
    test_l1<dtype, 12184, num_iters>(f); \
    test_l1<dtype, 12192, num_iters>(f); \
    test_l1<dtype, 12200, num_iters>(f); \
    test_l1<dtype, 12208, num_iters>(f); \
    test_l1<dtype, 12216, num_iters>(f); \
    test_l1<dtype, 12224, num_iters>(f); \
    test_l1<dtype, 12232, num_iters>(f); \
    test_l1<dtype, 12240, num_iters>(f); \
    test_l1<dtype, 12248, num_iters>(f); \
    test_l1<dtype, 12256, num_iters>(f); \
    test_l1<dtype, 12264, num_iters>(f); \
    test_l1<dtype, 12272, num_iters>(f); \
    test_l1<dtype, 12280, num_iters>(f); \
    test_l1<dtype, 12288, num_iters>(f); \
    test_l1<dtype, 12296, num_iters>(f); \
    test_l1<dtype, 12304, num_iters>(f); \
    test_l1<dtype, 12312, num_iters>(f); \
    test_l1<dtype, 12320, num_iters>(f); \
    test_l1<dtype, 12328, num_iters>(f); \
    test_l1<dtype, 12336, num_iters>(f); \
    test_l1<dtype, 12344, num_iters>(f); \
    test_l1<dtype, 12352, num_iters>(f); \
    test_l1<dtype, 12360, num_iters>(f); \
    test_l1<dtype, 12368, num_iters>(f); \
    test_l1<dtype, 12376, num_iters>(f); \
    test_l1<dtype, 12384, num_iters>(f); \
    test_l1<dtype, 12392, num_iters>(f); \
    test_l1<dtype, 12400, num_iters>(f); \
    test_l1<dtype, 12408, num_iters>(f); \
    test_l1<dtype, 12416, num_iters>(f); \
    test_l1<dtype, 12424, num_iters>(f); \
    test_l1<dtype, 12432, num_iters>(f); \
    test_l1<dtype, 12440, num_iters>(f); \
    test_l1<dtype, 12448, num_iters>(f); \
    test_l1<dtype, 12456, num_iters>(f); \
    test_l1<dtype, 12464, num_iters>(f); \
    test_l1<dtype, 12472, num_iters>(f); \
    test_l1<dtype, 12480, num_iters>(f); \
    test_l1<dtype, 12488, num_iters>(f); \
    test_l1<dtype, 12496, num_iters>(f); \
    test_l1<dtype, 12504, num_iters>(f); \
    test_l1<dtype, 12512, num_iters>(f); \
    test_l1<dtype, 12520, num_iters>(f); \
    test_l1<dtype, 12528, num_iters>(f); \
    test_l1<dtype, 12536, num_iters>(f); \
    test_l1<dtype, 12544, num_iters>(f); \
    test_l1<dtype, 12552, num_iters>(f); \
    test_l1<dtype, 12560, num_iters>(f); \
    test_l1<dtype, 12568, num_iters>(f); \
    test_l1<dtype, 12576, num_iters>(f); \
    test_l1<dtype, 12584, num_iters>(f); \
    test_l1<dtype, 12592, num_iters>(f); \
    test_l1<dtype, 12600, num_iters>(f); \
    test_l1<dtype, 12608, num_iters>(f); \
    test_l1<dtype, 12616, num_iters>(f); \
    test_l1<dtype, 12624, num_iters>(f); \
    test_l1<dtype, 12632, num_iters>(f); \
    test_l1<dtype, 12640, num_iters>(f); \
    test_l1<dtype, 12648, num_iters>(f); \
    test_l1<dtype, 12656, num_iters>(f); \
    test_l1<dtype, 12664, num_iters>(f); \
    test_l1<dtype, 12672, num_iters>(f); \
    test_l1<dtype, 12680, num_iters>(f); \
    test_l1<dtype, 12688, num_iters>(f); \
    test_l1<dtype, 12696, num_iters>(f); \
    test_l1<dtype, 12704, num_iters>(f); \
    test_l1<dtype, 12712, num_iters>(f); \
    test_l1<dtype, 12720, num_iters>(f); \
    test_l1<dtype, 12728, num_iters>(f); \
    test_l1<dtype, 12736, num_iters>(f); \
    test_l1<dtype, 12744, num_iters>(f); \
    test_l1<dtype, 12752, num_iters>(f); \
    test_l1<dtype, 12760, num_iters>(f); \
    test_l1<dtype, 12768, num_iters>(f); \
    test_l1<dtype, 12776, num_iters>(f); \
    test_l1<dtype, 12784, num_iters>(f); \
    test_l1<dtype, 12792, num_iters>(f); \
    test_l1<dtype, 12800, num_iters>(f); \
    test_l1<dtype, 12808, num_iters>(f); \
    test_l1<dtype, 12816, num_iters>(f); \
    test_l1<dtype, 12824, num_iters>(f); \
    test_l1<dtype, 12832, num_iters>(f); \
    test_l1<dtype, 12840, num_iters>(f); \
    test_l1<dtype, 12848, num_iters>(f); \
    test_l1<dtype, 12856, num_iters>(f); \
    test_l1<dtype, 12864, num_iters>(f); \
    test_l1<dtype, 12872, num_iters>(f); \
    test_l1<dtype, 12880, num_iters>(f); \
    test_l1<dtype, 12888, num_iters>(f); \
    test_l1<dtype, 12896, num_iters>(f); \
    test_l1<dtype, 12904, num_iters>(f); \
    test_l1<dtype, 12912, num_iters>(f); \
    test_l1<dtype, 12920, num_iters>(f); \
    test_l1<dtype, 12928, num_iters>(f); \
    test_l1<dtype, 12936, num_iters>(f); \
    test_l1<dtype, 12944, num_iters>(f); \
    test_l1<dtype, 12952, num_iters>(f); \
    test_l1<dtype, 12960, num_iters>(f); \
    test_l1<dtype, 12968, num_iters>(f); \
    test_l1<dtype, 12976, num_iters>(f); \
    test_l1<dtype, 12984, num_iters>(f); \
    test_l1<dtype, 12992, num_iters>(f); \
    test_l1<dtype, 13000, num_iters>(f); \
    test_l1<dtype, 13008, num_iters>(f); \
    test_l1<dtype, 13016, num_iters>(f); \
    test_l1<dtype, 13024, num_iters>(f); \
    test_l1<dtype, 13032, num_iters>(f); \
    test_l1<dtype, 13040, num_iters>(f); \
    test_l1<dtype, 13048, num_iters>(f); \
    test_l1<dtype, 13056, num_iters>(f); \
    test_l1<dtype, 13064, num_iters>(f); \
    test_l1<dtype, 13072, num_iters>(f); \
    test_l1<dtype, 13080, num_iters>(f); \
    test_l1<dtype, 13088, num_iters>(f); \
    test_l1<dtype, 13096, num_iters>(f); \
    test_l1<dtype, 13104, num_iters>(f); \
    test_l1<dtype, 13112, num_iters>(f); \
    test_l1<dtype, 13120, num_iters>(f); \
    test_l1<dtype, 13128, num_iters>(f); \
    test_l1<dtype, 13136, num_iters>(f); \
    test_l1<dtype, 13144, num_iters>(f); \
    test_l1<dtype, 13152, num_iters>(f); \
    test_l1<dtype, 13160, num_iters>(f); \
    test_l1<dtype, 13168, num_iters>(f); \
    test_l1<dtype, 13176, num_iters>(f); \
    test_l1<dtype, 13184, num_iters>(f); \
    test_l1<dtype, 13192, num_iters>(f); \
    test_l1<dtype, 13200, num_iters>(f); \
    test_l1<dtype, 13208, num_iters>(f); \
    test_l1<dtype, 13216, num_iters>(f); \
    test_l1<dtype, 13224, num_iters>(f); \
    test_l1<dtype, 13232, num_iters>(f); \
    test_l1<dtype, 13240, num_iters>(f); \
    test_l1<dtype, 13248, num_iters>(f); \
    test_l1<dtype, 13256, num_iters>(f); \
    test_l1<dtype, 13264, num_iters>(f); \
    test_l1<dtype, 13272, num_iters>(f); \
    test_l1<dtype, 13280, num_iters>(f); \
    test_l1<dtype, 13288, num_iters>(f); \
    test_l1<dtype, 13296, num_iters>(f); \
    test_l1<dtype, 13304, num_iters>(f); \
    test_l1<dtype, 13312, num_iters>(f); \
    test_l1<dtype, 13320, num_iters>(f); \
    test_l1<dtype, 13328, num_iters>(f); \
    test_l1<dtype, 13336, num_iters>(f); \
    test_l1<dtype, 13344, num_iters>(f); \
    test_l1<dtype, 13352, num_iters>(f); \
    test_l1<dtype, 13360, num_iters>(f); \
    test_l1<dtype, 13368, num_iters>(f); \
    test_l1<dtype, 13376, num_iters>(f); \
    test_l1<dtype, 13384, num_iters>(f); \
    test_l1<dtype, 13392, num_iters>(f); \
    test_l1<dtype, 13400, num_iters>(f); \
    test_l1<dtype, 13408, num_iters>(f); \
    test_l1<dtype, 13416, num_iters>(f); \
    test_l1<dtype, 13424, num_iters>(f); \
    test_l1<dtype, 13432, num_iters>(f); \
    test_l1<dtype, 13440, num_iters>(f); \
    test_l1<dtype, 13448, num_iters>(f); \
    test_l1<dtype, 13456, num_iters>(f); \
    test_l1<dtype, 13464, num_iters>(f); \
    test_l1<dtype, 13472, num_iters>(f); \
    test_l1<dtype, 13480, num_iters>(f); \
    test_l1<dtype, 13488, num_iters>(f); \
    test_l1<dtype, 13496, num_iters>(f); \
    test_l1<dtype, 13504, num_iters>(f); \
    test_l1<dtype, 13512, num_iters>(f); \
    test_l1<dtype, 13520, num_iters>(f); \
    test_l1<dtype, 13528, num_iters>(f); \
    test_l1<dtype, 13536, num_iters>(f); \
    test_l1<dtype, 13544, num_iters>(f); \
    test_l1<dtype, 13552, num_iters>(f); \
    test_l1<dtype, 13560, num_iters>(f); \
    test_l1<dtype, 13568, num_iters>(f); \
    test_l1<dtype, 13576, num_iters>(f); \
    test_l1<dtype, 13584, num_iters>(f); \
    test_l1<dtype, 13592, num_iters>(f); \
    test_l1<dtype, 13600, num_iters>(f); \
    test_l1<dtype, 13608, num_iters>(f); \
    test_l1<dtype, 13616, num_iters>(f); \
    test_l1<dtype, 13624, num_iters>(f); \
    test_l1<dtype, 13632, num_iters>(f); \
    test_l1<dtype, 13640, num_iters>(f); \
    test_l1<dtype, 13648, num_iters>(f); \
    test_l1<dtype, 13656, num_iters>(f); \
    test_l1<dtype, 13664, num_iters>(f); \
    test_l1<dtype, 13672, num_iters>(f); \
    test_l1<dtype, 13680, num_iters>(f); \
    test_l1<dtype, 13688, num_iters>(f); \
    test_l1<dtype, 13696, num_iters>(f); \
    test_l1<dtype, 13704, num_iters>(f); \
    test_l1<dtype, 13712, num_iters>(f); \
    test_l1<dtype, 13720, num_iters>(f); \
    test_l1<dtype, 13728, num_iters>(f); \
    test_l1<dtype, 13736, num_iters>(f); \
    test_l1<dtype, 13744, num_iters>(f); \
    test_l1<dtype, 13752, num_iters>(f); \
    test_l1<dtype, 13760, num_iters>(f); \
    test_l1<dtype, 13768, num_iters>(f); \
    test_l1<dtype, 13776, num_iters>(f); \
    test_l1<dtype, 13784, num_iters>(f); \
    test_l1<dtype, 13792, num_iters>(f); \
    test_l1<dtype, 13800, num_iters>(f); \
    test_l1<dtype, 13808, num_iters>(f); \
    test_l1<dtype, 13816, num_iters>(f); \
    test_l1<dtype, 13824, num_iters>(f); \
    test_l1<dtype, 13832, num_iters>(f); \
    test_l1<dtype, 13840, num_iters>(f); \
    test_l1<dtype, 13848, num_iters>(f); \
    test_l1<dtype, 13856, num_iters>(f); \
    test_l1<dtype, 13864, num_iters>(f); \
    test_l1<dtype, 13872, num_iters>(f); \
    test_l1<dtype, 13880, num_iters>(f); \
    test_l1<dtype, 13888, num_iters>(f); \
    test_l1<dtype, 13896, num_iters>(f); \
    test_l1<dtype, 13904, num_iters>(f); \
    test_l1<dtype, 13912, num_iters>(f); \
    test_l1<dtype, 13920, num_iters>(f); \
    test_l1<dtype, 13928, num_iters>(f); \
    test_l1<dtype, 13936, num_iters>(f); \
    test_l1<dtype, 13944, num_iters>(f); \
    test_l1<dtype, 13952, num_iters>(f); \
    test_l1<dtype, 13960, num_iters>(f); \
    test_l1<dtype, 13968, num_iters>(f); \
    test_l1<dtype, 13976, num_iters>(f); \
    test_l1<dtype, 13984, num_iters>(f); \
    test_l1<dtype, 13992, num_iters>(f); \
    test_l1<dtype, 14000, num_iters>(f); \
    test_l1<dtype, 14008, num_iters>(f); \
    test_l1<dtype, 14016, num_iters>(f); \
    test_l1<dtype, 14024, num_iters>(f); \
    test_l1<dtype, 14032, num_iters>(f); \
    test_l1<dtype, 14040, num_iters>(f); \
    test_l1<dtype, 14048, num_iters>(f); \
    test_l1<dtype, 14056, num_iters>(f); \
    test_l1<dtype, 14064, num_iters>(f); \
    test_l1<dtype, 14072, num_iters>(f); \
    test_l1<dtype, 14080, num_iters>(f); \
    test_l1<dtype, 14088, num_iters>(f); \
    test_l1<dtype, 14096, num_iters>(f); \
    test_l1<dtype, 14104, num_iters>(f); \
    test_l1<dtype, 14112, num_iters>(f); \
    test_l1<dtype, 14120, num_iters>(f); \
    test_l1<dtype, 14128, num_iters>(f); \
    test_l1<dtype, 14136, num_iters>(f); \
    test_l1<dtype, 14144, num_iters>(f); \
    test_l1<dtype, 14152, num_iters>(f); \
    test_l1<dtype, 14160, num_iters>(f); \
    test_l1<dtype, 14168, num_iters>(f); \
    test_l1<dtype, 14176, num_iters>(f); \
    test_l1<dtype, 14184, num_iters>(f); \
    test_l1<dtype, 14192, num_iters>(f); \
    test_l1<dtype, 14200, num_iters>(f); \
    test_l1<dtype, 14208, num_iters>(f); \
    test_l1<dtype, 14216, num_iters>(f); \
    test_l1<dtype, 14224, num_iters>(f); \
    test_l1<dtype, 14232, num_iters>(f); \
    test_l1<dtype, 14240, num_iters>(f); \
    test_l1<dtype, 14248, num_iters>(f); \
    test_l1<dtype, 14256, num_iters>(f); \
    test_l1<dtype, 14264, num_iters>(f); \
    test_l1<dtype, 14272, num_iters>(f); \
    test_l1<dtype, 14280, num_iters>(f); \
    test_l1<dtype, 14288, num_iters>(f); \
    test_l1<dtype, 14296, num_iters>(f); \
    test_l1<dtype, 14304, num_iters>(f); \
    test_l1<dtype, 14312, num_iters>(f); \
    test_l1<dtype, 14320, num_iters>(f); \
    test_l1<dtype, 14328, num_iters>(f); \
    test_l1<dtype, 14336, num_iters>(f); \
    test_l1<dtype, 14344, num_iters>(f); \
    test_l1<dtype, 14352, num_iters>(f); \
    test_l1<dtype, 14360, num_iters>(f); \
    test_l1<dtype, 14368, num_iters>(f); \
    test_l1<dtype, 14376, num_iters>(f); \
    test_l1<dtype, 14384, num_iters>(f); \
    test_l1<dtype, 14392, num_iters>(f); \
    test_l1<dtype, 14400, num_iters>(f); \
    test_l1<dtype, 14408, num_iters>(f); \
    test_l1<dtype, 14416, num_iters>(f); \
    test_l1<dtype, 14424, num_iters>(f); \
    test_l1<dtype, 14432, num_iters>(f); \
    test_l1<dtype, 14440, num_iters>(f); \
    test_l1<dtype, 14448, num_iters>(f); \
    test_l1<dtype, 14456, num_iters>(f); \
    test_l1<dtype, 14464, num_iters>(f); \
    test_l1<dtype, 14472, num_iters>(f); \
    test_l1<dtype, 14480, num_iters>(f); \
    test_l1<dtype, 14488, num_iters>(f); \
    test_l1<dtype, 14496, num_iters>(f); \
    test_l1<dtype, 14504, num_iters>(f); \
    test_l1<dtype, 14512, num_iters>(f); \
    test_l1<dtype, 14520, num_iters>(f); \
    test_l1<dtype, 14528, num_iters>(f); \
    test_l1<dtype, 14536, num_iters>(f); \
    test_l1<dtype, 14544, num_iters>(f); \
    test_l1<dtype, 14552, num_iters>(f); \
    test_l1<dtype, 14560, num_iters>(f); \
    test_l1<dtype, 14568, num_iters>(f); \
    test_l1<dtype, 14576, num_iters>(f); \
    test_l1<dtype, 14584, num_iters>(f); \
    test_l1<dtype, 14592, num_iters>(f); \
    test_l1<dtype, 14600, num_iters>(f); \
    test_l1<dtype, 14608, num_iters>(f); \
    test_l1<dtype, 14616, num_iters>(f); \
    test_l1<dtype, 14624, num_iters>(f); \
    test_l1<dtype, 14632, num_iters>(f); \
    test_l1<dtype, 14640, num_iters>(f); \
    test_l1<dtype, 14648, num_iters>(f); \
    test_l1<dtype, 14656, num_iters>(f); \
    test_l1<dtype, 14664, num_iters>(f); \
    test_l1<dtype, 14672, num_iters>(f); \
    test_l1<dtype, 14680, num_iters>(f); \
    test_l1<dtype, 14688, num_iters>(f); \
    test_l1<dtype, 14696, num_iters>(f); \
    test_l1<dtype, 14704, num_iters>(f); \
    test_l1<dtype, 14712, num_iters>(f); \
    test_l1<dtype, 14720, num_iters>(f); \
    test_l1<dtype, 14728, num_iters>(f); \
    test_l1<dtype, 14736, num_iters>(f); \
    test_l1<dtype, 14744, num_iters>(f); \
    test_l1<dtype, 14752, num_iters>(f); \
    test_l1<dtype, 14760, num_iters>(f); \
    test_l1<dtype, 14768, num_iters>(f); \
    test_l1<dtype, 14776, num_iters>(f); \
    test_l1<dtype, 14784, num_iters>(f); \
    test_l1<dtype, 14792, num_iters>(f); \
    test_l1<dtype, 14800, num_iters>(f); \
    test_l1<dtype, 14808, num_iters>(f); \
    test_l1<dtype, 14816, num_iters>(f); \
    test_l1<dtype, 14824, num_iters>(f); \
    test_l1<dtype, 14832, num_iters>(f); \
    test_l1<dtype, 14840, num_iters>(f); \
    test_l1<dtype, 14848, num_iters>(f); \
    test_l1<dtype, 14856, num_iters>(f); \
    test_l1<dtype, 14864, num_iters>(f); \
    test_l1<dtype, 14872, num_iters>(f); \
    test_l1<dtype, 14880, num_iters>(f); \
    test_l1<dtype, 14888, num_iters>(f); \
    test_l1<dtype, 14896, num_iters>(f); \
    test_l1<dtype, 14904, num_iters>(f); \
    test_l1<dtype, 14912, num_iters>(f); \
    test_l1<dtype, 14920, num_iters>(f); \
    test_l1<dtype, 14928, num_iters>(f); \
    test_l1<dtype, 14936, num_iters>(f); \
    test_l1<dtype, 14944, num_iters>(f); \
    test_l1<dtype, 14952, num_iters>(f); \
    test_l1<dtype, 14960, num_iters>(f); \
    test_l1<dtype, 14968, num_iters>(f); \
    test_l1<dtype, 14976, num_iters>(f); \
    test_l1<dtype, 14984, num_iters>(f); \
    test_l1<dtype, 14992, num_iters>(f); \
    test_l1<dtype, 15000, num_iters>(f); \
    test_l1<dtype, 15008, num_iters>(f); \
    test_l1<dtype, 15016, num_iters>(f); \
    test_l1<dtype, 15024, num_iters>(f); \
    test_l1<dtype, 15032, num_iters>(f); \
    test_l1<dtype, 15040, num_iters>(f); \
    test_l1<dtype, 15048, num_iters>(f); \
    test_l1<dtype, 15056, num_iters>(f); \
    test_l1<dtype, 15064, num_iters>(f); \
    test_l1<dtype, 15072, num_iters>(f); \
    test_l1<dtype, 15080, num_iters>(f); \
    test_l1<dtype, 15088, num_iters>(f); \
    test_l1<dtype, 15096, num_iters>(f); \
    test_l1<dtype, 15104, num_iters>(f); \
    test_l1<dtype, 15112, num_iters>(f); \
    test_l1<dtype, 15120, num_iters>(f); \
    test_l1<dtype, 15128, num_iters>(f); \
    test_l1<dtype, 15136, num_iters>(f); \
    test_l1<dtype, 15144, num_iters>(f); \
    test_l1<dtype, 15152, num_iters>(f); \
    test_l1<dtype, 15160, num_iters>(f); \
    test_l1<dtype, 15168, num_iters>(f); \
    test_l1<dtype, 15176, num_iters>(f); \
    test_l1<dtype, 15184, num_iters>(f); \
    test_l1<dtype, 15192, num_iters>(f); \
    test_l1<dtype, 15200, num_iters>(f); \
    test_l1<dtype, 15208, num_iters>(f); \
    test_l1<dtype, 15216, num_iters>(f); \
    test_l1<dtype, 15224, num_iters>(f); \
    test_l1<dtype, 15232, num_iters>(f); \
    test_l1<dtype, 15240, num_iters>(f); \
    test_l1<dtype, 15248, num_iters>(f); \
    test_l1<dtype, 15256, num_iters>(f); \
    test_l1<dtype, 15264, num_iters>(f); \
    test_l1<dtype, 15272, num_iters>(f); \
    test_l1<dtype, 15280, num_iters>(f); \
    test_l1<dtype, 15288, num_iters>(f); \
    test_l1<dtype, 15296, num_iters>(f); \
    test_l1<dtype, 15304, num_iters>(f); \
    test_l1<dtype, 15312, num_iters>(f); \
    test_l1<dtype, 15320, num_iters>(f); \
    test_l1<dtype, 15328, num_iters>(f); \
    test_l1<dtype, 15336, num_iters>(f); \
    test_l1<dtype, 15344, num_iters>(f); \
    test_l1<dtype, 15352, num_iters>(f); \
    test_l1<dtype, 15360, num_iters>(f); \
    test_l1<dtype, 15368, num_iters>(f); \
    test_l1<dtype, 15376, num_iters>(f); \
    test_l1<dtype, 15384, num_iters>(f); \
    test_l1<dtype, 15392, num_iters>(f); \
    test_l1<dtype, 15400, num_iters>(f); \
    test_l1<dtype, 15408, num_iters>(f); \
    test_l1<dtype, 15416, num_iters>(f); \
    test_l1<dtype, 15424, num_iters>(f); \
    test_l1<dtype, 15432, num_iters>(f); \
    test_l1<dtype, 15440, num_iters>(f); \
    test_l1<dtype, 15448, num_iters>(f); \
    test_l1<dtype, 15456, num_iters>(f); \
    test_l1<dtype, 15464, num_iters>(f); \
    test_l1<dtype, 15472, num_iters>(f); \
    test_l1<dtype, 15480, num_iters>(f); \
    test_l1<dtype, 15488, num_iters>(f); \
    test_l1<dtype, 15496, num_iters>(f); \
    test_l1<dtype, 15504, num_iters>(f); \
    test_l1<dtype, 15512, num_iters>(f); \
    test_l1<dtype, 15520, num_iters>(f); \
    test_l1<dtype, 15528, num_iters>(f); \
    test_l1<dtype, 15536, num_iters>(f); \
    test_l1<dtype, 15544, num_iters>(f); \
    test_l1<dtype, 15552, num_iters>(f); \
    test_l1<dtype, 15560, num_iters>(f); \
    test_l1<dtype, 15568, num_iters>(f); \
    test_l1<dtype, 15576, num_iters>(f); \
    test_l1<dtype, 15584, num_iters>(f); \
    test_l1<dtype, 15592, num_iters>(f); \
    test_l1<dtype, 15600, num_iters>(f); \
    test_l1<dtype, 15608, num_iters>(f); \
    test_l1<dtype, 15616, num_iters>(f); \
    test_l1<dtype, 15624, num_iters>(f); \
    test_l1<dtype, 15632, num_iters>(f); \
    test_l1<dtype, 15640, num_iters>(f); \
    test_l1<dtype, 15648, num_iters>(f); \
    test_l1<dtype, 15656, num_iters>(f); \
    test_l1<dtype, 15664, num_iters>(f); \
    test_l1<dtype, 15672, num_iters>(f); \
    test_l1<dtype, 15680, num_iters>(f); \
    test_l1<dtype, 15688, num_iters>(f); \
    test_l1<dtype, 15696, num_iters>(f); \
    test_l1<dtype, 15704, num_iters>(f); \
    test_l1<dtype, 15712, num_iters>(f); \
    test_l1<dtype, 15720, num_iters>(f); \
    test_l1<dtype, 15728, num_iters>(f); \
    test_l1<dtype, 15736, num_iters>(f); \
    test_l1<dtype, 15744, num_iters>(f); \
    test_l1<dtype, 15752, num_iters>(f); \
    test_l1<dtype, 15760, num_iters>(f); \
    test_l1<dtype, 15768, num_iters>(f); \
    test_l1<dtype, 15776, num_iters>(f); \
    test_l1<dtype, 15784, num_iters>(f); \
    test_l1<dtype, 15792, num_iters>(f); \
    test_l1<dtype, 15800, num_iters>(f); \
    test_l1<dtype, 15808, num_iters>(f); \
    test_l1<dtype, 15816, num_iters>(f); \
    test_l1<dtype, 15824, num_iters>(f); \
    test_l1<dtype, 15832, num_iters>(f); \
    test_l1<dtype, 15840, num_iters>(f); \
    test_l1<dtype, 15848, num_iters>(f); \
    test_l1<dtype, 15856, num_iters>(f); \
    test_l1<dtype, 15864, num_iters>(f); \
    test_l1<dtype, 15872, num_iters>(f); \
    test_l1<dtype, 15880, num_iters>(f); \
    test_l1<dtype, 15888, num_iters>(f); \
    test_l1<dtype, 15896, num_iters>(f); \
    test_l1<dtype, 15904, num_iters>(f); \
    test_l1<dtype, 15912, num_iters>(f); \
    test_l1<dtype, 15920, num_iters>(f); \
    test_l1<dtype, 15928, num_iters>(f); \
    test_l1<dtype, 15936, num_iters>(f); \
    test_l1<dtype, 15944, num_iters>(f); \
    test_l1<dtype, 15952, num_iters>(f); \
    test_l1<dtype, 15960, num_iters>(f); \
    test_l1<dtype, 15968, num_iters>(f); \
    test_l1<dtype, 15976, num_iters>(f); \
    test_l1<dtype, 15984, num_iters>(f); \
    test_l1<dtype, 15992, num_iters>(f); \
    test_l1<dtype, 16000, num_iters>(f); \
    test_l1<dtype, 16008, num_iters>(f); \
    test_l1<dtype, 16016, num_iters>(f); \
    test_l1<dtype, 16024, num_iters>(f); \
    test_l1<dtype, 16032, num_iters>(f); \
    test_l1<dtype, 16040, num_iters>(f); \
    test_l1<dtype, 16048, num_iters>(f); \
    test_l1<dtype, 16056, num_iters>(f); \
    test_l1<dtype, 16064, num_iters>(f); \
    test_l1<dtype, 16072, num_iters>(f); \
    test_l1<dtype, 16080, num_iters>(f); \
    test_l1<dtype, 16088, num_iters>(f); \
    test_l1<dtype, 16096, num_iters>(f); \
    test_l1<dtype, 16104, num_iters>(f); \
    test_l1<dtype, 16112, num_iters>(f); \
    test_l1<dtype, 16120, num_iters>(f); \
    test_l1<dtype, 16128, num_iters>(f); \
    test_l1<dtype, 16136, num_iters>(f); \
    test_l1<dtype, 16144, num_iters>(f); \
    test_l1<dtype, 16152, num_iters>(f); \
    test_l1<dtype, 16160, num_iters>(f); \
    test_l1<dtype, 16168, num_iters>(f); \
    test_l1<dtype, 16176, num_iters>(f); \
    test_l1<dtype, 16184, num_iters>(f); \
    test_l1<dtype, 16192, num_iters>(f); \
    test_l1<dtype, 16200, num_iters>(f); \
    test_l1<dtype, 16208, num_iters>(f); \
    test_l1<dtype, 16216, num_iters>(f); \
    test_l1<dtype, 16224, num_iters>(f); \
    test_l1<dtype, 16232, num_iters>(f); \
    test_l1<dtype, 16240, num_iters>(f); \
    test_l1<dtype, 16248, num_iters>(f); \
    test_l1<dtype, 16256, num_iters>(f); \
    test_l1<dtype, 16264, num_iters>(f); \
    test_l1<dtype, 16272, num_iters>(f); \
    test_l1<dtype, 16280, num_iters>(f); \
    test_l1<dtype, 16288, num_iters>(f); \
    test_l1<dtype, 16296, num_iters>(f); \
    test_l1<dtype, 16304, num_iters>(f); \
    test_l1<dtype, 16312, num_iters>(f); \
    test_l1<dtype, 16320, num_iters>(f); \
    test_l1<dtype, 16328, num_iters>(f); \
    test_l1<dtype, 16336, num_iters>(f); \
    test_l1<dtype, 16344, num_iters>(f); \
    test_l1<dtype, 16352, num_iters>(f); \
    test_l1<dtype, 16360, num_iters>(f); \
    test_l1<dtype, 16368, num_iters>(f); \
    test_l1<dtype, 16376, num_iters>(f); \
    test_l1<dtype, 16384, num_iters>(f); \
    test_l1<dtype, 16392, num_iters>(f); \
    test_l1<dtype, 16400, num_iters>(f); \
    test_l1<dtype, 16408, num_iters>(f); \
    test_l1<dtype, 16416, num_iters>(f); \
    test_l1<dtype, 16424, num_iters>(f); \
    test_l1<dtype, 16432, num_iters>(f); \
    test_l1<dtype, 16440, num_iters>(f); \
    test_l1<dtype, 16448, num_iters>(f); \
    test_l1<dtype, 16456, num_iters>(f); \
    test_l1<dtype, 16464, num_iters>(f); \
    test_l1<dtype, 16472, num_iters>(f); \
    test_l1<dtype, 16480, num_iters>(f); \
    test_l1<dtype, 16488, num_iters>(f); \
    test_l1<dtype, 16496, num_iters>(f); \
    test_l1<dtype, 16504, num_iters>(f); \
    test_l1<dtype, 16512, num_iters>(f); \
    test_l1<dtype, 16520, num_iters>(f); \
    test_l1<dtype, 16528, num_iters>(f); \
    test_l1<dtype, 16536, num_iters>(f); \
    test_l1<dtype, 16544, num_iters>(f); \
    test_l1<dtype, 16552, num_iters>(f); \
    test_l1<dtype, 16560, num_iters>(f); \
    test_l1<dtype, 16568, num_iters>(f); \
    test_l1<dtype, 16576, num_iters>(f); \
    test_l1<dtype, 16584, num_iters>(f); \
    test_l1<dtype, 16592, num_iters>(f); \
    test_l1<dtype, 16600, num_iters>(f); \
    test_l1<dtype, 16608, num_iters>(f); \
    test_l1<dtype, 16616, num_iters>(f); \
    test_l1<dtype, 16624, num_iters>(f); \
    test_l1<dtype, 16632, num_iters>(f); \
    test_l1<dtype, 16640, num_iters>(f); \
    test_l1<dtype, 16648, num_iters>(f); \
    test_l1<dtype, 16656, num_iters>(f); \
    test_l1<dtype, 16664, num_iters>(f); \
    test_l1<dtype, 16672, num_iters>(f); \
    test_l1<dtype, 16680, num_iters>(f); \
    test_l1<dtype, 16688, num_iters>(f); \
    test_l1<dtype, 16696, num_iters>(f); \
    test_l1<dtype, 16704, num_iters>(f); \
    test_l1<dtype, 16712, num_iters>(f); \
    test_l1<dtype, 16720, num_iters>(f); \
    test_l1<dtype, 16728, num_iters>(f); \
    test_l1<dtype, 16736, num_iters>(f); \
    test_l1<dtype, 16744, num_iters>(f); \
    test_l1<dtype, 16752, num_iters>(f); \
    test_l1<dtype, 16760, num_iters>(f); \
    test_l1<dtype, 16768, num_iters>(f); \
    test_l1<dtype, 16776, num_iters>(f); \
    test_l1<dtype, 16784, num_iters>(f); \
    test_l1<dtype, 16792, num_iters>(f); \
    test_l1<dtype, 16800, num_iters>(f); \
    test_l1<dtype, 16808, num_iters>(f); \
    test_l1<dtype, 16816, num_iters>(f); \
    test_l1<dtype, 16824, num_iters>(f); \
    test_l1<dtype, 16832, num_iters>(f); \
    test_l1<dtype, 16840, num_iters>(f); \
    test_l1<dtype, 16848, num_iters>(f); \
    test_l1<dtype, 16856, num_iters>(f); \
    test_l1<dtype, 16864, num_iters>(f); \
    test_l1<dtype, 16872, num_iters>(f); \
    test_l1<dtype, 16880, num_iters>(f); \
    test_l1<dtype, 16888, num_iters>(f); \
    test_l1<dtype, 16896, num_iters>(f); \
    test_l1<dtype, 16904, num_iters>(f); \
    test_l1<dtype, 16912, num_iters>(f); \
    test_l1<dtype, 16920, num_iters>(f); \
    test_l1<dtype, 16928, num_iters>(f); \
    test_l1<dtype, 16936, num_iters>(f); \
    test_l1<dtype, 16944, num_iters>(f); \
    test_l1<dtype, 16952, num_iters>(f); \
    test_l1<dtype, 16960, num_iters>(f); \
    test_l1<dtype, 16968, num_iters>(f); \
    test_l1<dtype, 16976, num_iters>(f); \
    test_l1<dtype, 16984, num_iters>(f); \
    test_l1<dtype, 16992, num_iters>(f); \
    test_l1<dtype, 17000, num_iters>(f); \
    test_l1<dtype, 17008, num_iters>(f); \
    test_l1<dtype, 17016, num_iters>(f); \
    test_l1<dtype, 17024, num_iters>(f); \
    test_l1<dtype, 17032, num_iters>(f); \
    test_l1<dtype, 17040, num_iters>(f); \
    test_l1<dtype, 17048, num_iters>(f); \
    test_l1<dtype, 17056, num_iters>(f); \
    test_l1<dtype, 17064, num_iters>(f); \
    test_l1<dtype, 17072, num_iters>(f); \
    test_l1<dtype, 17080, num_iters>(f); \
    test_l1<dtype, 17088, num_iters>(f); \
    test_l1<dtype, 17096, num_iters>(f); \
    test_l1<dtype, 17104, num_iters>(f); \
    test_l1<dtype, 17112, num_iters>(f); \
    test_l1<dtype, 17120, num_iters>(f); \
    test_l1<dtype, 17128, num_iters>(f); \
    test_l1<dtype, 17136, num_iters>(f); \
    test_l1<dtype, 17144, num_iters>(f); \
    test_l1<dtype, 17152, num_iters>(f); \
    test_l1<dtype, 17160, num_iters>(f); \
    test_l1<dtype, 17168, num_iters>(f); \
    test_l1<dtype, 17176, num_iters>(f); \
    test_l1<dtype, 17184, num_iters>(f); \
    test_l1<dtype, 17192, num_iters>(f); \
    test_l1<dtype, 17200, num_iters>(f); \
    test_l1<dtype, 17208, num_iters>(f); \
    test_l1<dtype, 17216, num_iters>(f); \
    test_l1<dtype, 17224, num_iters>(f); \
    test_l1<dtype, 17232, num_iters>(f); \
    test_l1<dtype, 17240, num_iters>(f); \
    test_l1<dtype, 17248, num_iters>(f); \
    test_l1<dtype, 17256, num_iters>(f); \
    test_l1<dtype, 17264, num_iters>(f); \
    test_l1<dtype, 17272, num_iters>(f); \
    test_l1<dtype, 17280, num_iters>(f); \
    test_l1<dtype, 17288, num_iters>(f); \
    test_l1<dtype, 17296, num_iters>(f); \
    test_l1<dtype, 17304, num_iters>(f); \
    test_l1<dtype, 17312, num_iters>(f); \
    test_l1<dtype, 17320, num_iters>(f); \
    test_l1<dtype, 17328, num_iters>(f); \
    test_l1<dtype, 17336, num_iters>(f); \
    test_l1<dtype, 17344, num_iters>(f); \
    test_l1<dtype, 17352, num_iters>(f); \
    test_l1<dtype, 17360, num_iters>(f); \
    test_l1<dtype, 17368, num_iters>(f); \
    test_l1<dtype, 17376, num_iters>(f); \
    test_l1<dtype, 17384, num_iters>(f); \
    test_l1<dtype, 17392, num_iters>(f); \
    test_l1<dtype, 17400, num_iters>(f); \
    test_l1<dtype, 17408, num_iters>(f); \
    test_l1<dtype, 17416, num_iters>(f); \
    test_l1<dtype, 17424, num_iters>(f); \
    test_l1<dtype, 17432, num_iters>(f); \
    test_l1<dtype, 17440, num_iters>(f); \
    test_l1<dtype, 17448, num_iters>(f); \
    test_l1<dtype, 17456, num_iters>(f); \
    test_l1<dtype, 17464, num_iters>(f); \
    test_l1<dtype, 17472, num_iters>(f); \
    test_l1<dtype, 17480, num_iters>(f); \
    test_l1<dtype, 17488, num_iters>(f); \
    test_l1<dtype, 17496, num_iters>(f); \
    test_l1<dtype, 17504, num_iters>(f); \
    test_l1<dtype, 17512, num_iters>(f); \
    test_l1<dtype, 17520, num_iters>(f); \
    test_l1<dtype, 17528, num_iters>(f); \
    test_l1<dtype, 17536, num_iters>(f); \
    test_l1<dtype, 17544, num_iters>(f); \
    test_l1<dtype, 17552, num_iters>(f); \
    test_l1<dtype, 17560, num_iters>(f); \
    test_l1<dtype, 17568, num_iters>(f); \
    test_l1<dtype, 17576, num_iters>(f); \
    test_l1<dtype, 17584, num_iters>(f); \
    test_l1<dtype, 17592, num_iters>(f); \
    test_l1<dtype, 17600, num_iters>(f); \
    test_l1<dtype, 17608, num_iters>(f); \
    test_l1<dtype, 17616, num_iters>(f); \
    test_l1<dtype, 17624, num_iters>(f); \
    test_l1<dtype, 17632, num_iters>(f); \
    test_l1<dtype, 17640, num_iters>(f); \
    test_l1<dtype, 17648, num_iters>(f); \
    test_l1<dtype, 17656, num_iters>(f); \
    test_l1<dtype, 17664, num_iters>(f); \
    test_l1<dtype, 17672, num_iters>(f); \
    test_l1<dtype, 17680, num_iters>(f); \
    test_l1<dtype, 17688, num_iters>(f); \
    test_l1<dtype, 17696, num_iters>(f); \
    test_l1<dtype, 17704, num_iters>(f); \
    test_l1<dtype, 17712, num_iters>(f); \
    test_l1<dtype, 17720, num_iters>(f); \
    test_l1<dtype, 17728, num_iters>(f); \
    test_l1<dtype, 17736, num_iters>(f); \
    test_l1<dtype, 17744, num_iters>(f); \
    test_l1<dtype, 17752, num_iters>(f); \
    test_l1<dtype, 17760, num_iters>(f); \
    test_l1<dtype, 17768, num_iters>(f); \
    test_l1<dtype, 17776, num_iters>(f); \
    test_l1<dtype, 17784, num_iters>(f); \
    test_l1<dtype, 17792, num_iters>(f); \
    test_l1<dtype, 17800, num_iters>(f); \
    test_l1<dtype, 17808, num_iters>(f); \
    test_l1<dtype, 17816, num_iters>(f); \
    test_l1<dtype, 17824, num_iters>(f); \
    test_l1<dtype, 17832, num_iters>(f); \
    test_l1<dtype, 17840, num_iters>(f); \
    test_l1<dtype, 17848, num_iters>(f); \
    test_l1<dtype, 17856, num_iters>(f); \
    test_l1<dtype, 17864, num_iters>(f); \
    test_l1<dtype, 17872, num_iters>(f); \
    test_l1<dtype, 17880, num_iters>(f); \
    test_l1<dtype, 17888, num_iters>(f); \
    test_l1<dtype, 17896, num_iters>(f); \
    test_l1<dtype, 17904, num_iters>(f); \
    test_l1<dtype, 17912, num_iters>(f); \
    test_l1<dtype, 17920, num_iters>(f); \
    test_l1<dtype, 17928, num_iters>(f); \
    test_l1<dtype, 17936, num_iters>(f); \
    test_l1<dtype, 17944, num_iters>(f); \
    test_l1<dtype, 17952, num_iters>(f); \
    test_l1<dtype, 17960, num_iters>(f); \
    test_l1<dtype, 17968, num_iters>(f); \
    test_l1<dtype, 17976, num_iters>(f); \
    test_l1<dtype, 17984, num_iters>(f); \
    test_l1<dtype, 17992, num_iters>(f); \
    test_l1<dtype, 18000, num_iters>(f); \
    test_l1<dtype, 18008, num_iters>(f); \
    test_l1<dtype, 18016, num_iters>(f); \
    test_l1<dtype, 18024, num_iters>(f); \
    test_l1<dtype, 18032, num_iters>(f); \
    test_l1<dtype, 18040, num_iters>(f); \
    test_l1<dtype, 18048, num_iters>(f); \
    test_l1<dtype, 18056, num_iters>(f); \
    test_l1<dtype, 18064, num_iters>(f); \
    test_l1<dtype, 18072, num_iters>(f); \
    test_l1<dtype, 18080, num_iters>(f); \
    test_l1<dtype, 18088, num_iters>(f); \
    test_l1<dtype, 18096, num_iters>(f); \
    test_l1<dtype, 18104, num_iters>(f); \
    test_l1<dtype, 18112, num_iters>(f); \
    test_l1<dtype, 18120, num_iters>(f); \
    test_l1<dtype, 18128, num_iters>(f); \
    test_l1<dtype, 18136, num_iters>(f); \
    test_l1<dtype, 18144, num_iters>(f); \
    test_l1<dtype, 18152, num_iters>(f); \
    test_l1<dtype, 18160, num_iters>(f); \
    test_l1<dtype, 18168, num_iters>(f); \
    test_l1<dtype, 18176, num_iters>(f); \
    test_l1<dtype, 18184, num_iters>(f); \
    test_l1<dtype, 18192, num_iters>(f); \
    test_l1<dtype, 18200, num_iters>(f); \
    test_l1<dtype, 18208, num_iters>(f); \
    test_l1<dtype, 18216, num_iters>(f); \
    test_l1<dtype, 18224, num_iters>(f); \
    test_l1<dtype, 18232, num_iters>(f); \
    test_l1<dtype, 18240, num_iters>(f); \
    test_l1<dtype, 18248, num_iters>(f); \
    test_l1<dtype, 18256, num_iters>(f); \
    test_l1<dtype, 18264, num_iters>(f); \
    test_l1<dtype, 18272, num_iters>(f); \
    test_l1<dtype, 18280, num_iters>(f); \
    test_l1<dtype, 18288, num_iters>(f); \
    test_l1<dtype, 18296, num_iters>(f); \
    test_l1<dtype, 18304, num_iters>(f); \
    test_l1<dtype, 18312, num_iters>(f); \
    test_l1<dtype, 18320, num_iters>(f); \
    test_l1<dtype, 18328, num_iters>(f); \
    test_l1<dtype, 18336, num_iters>(f); \
    test_l1<dtype, 18344, num_iters>(f); \
    test_l1<dtype, 18352, num_iters>(f); \
    test_l1<dtype, 18360, num_iters>(f); \
    test_l1<dtype, 18368, num_iters>(f); \
    test_l1<dtype, 18376, num_iters>(f); \
    test_l1<dtype, 18384, num_iters>(f); \
    test_l1<dtype, 18392, num_iters>(f); \
    test_l1<dtype, 18400, num_iters>(f); \
    test_l1<dtype, 18408, num_iters>(f); \
    test_l1<dtype, 18416, num_iters>(f); \
    test_l1<dtype, 18424, num_iters>(f); \
    test_l1<dtype, 18432, num_iters>(f); \
    test_l1<dtype, 18440, num_iters>(f); \
    test_l1<dtype, 18448, num_iters>(f); \
    test_l1<dtype, 18456, num_iters>(f); \
    test_l1<dtype, 18464, num_iters>(f); \
    test_l1<dtype, 18472, num_iters>(f); \
    test_l1<dtype, 18480, num_iters>(f); \
    test_l1<dtype, 18488, num_iters>(f); \
    test_l1<dtype, 18496, num_iters>(f); \
    test_l1<dtype, 18504, num_iters>(f); \
    test_l1<dtype, 18512, num_iters>(f); \
    test_l1<dtype, 18520, num_iters>(f); \
    test_l1<dtype, 18528, num_iters>(f); \
    test_l1<dtype, 18536, num_iters>(f); \
    test_l1<dtype, 18544, num_iters>(f); \
    test_l1<dtype, 18552, num_iters>(f); \
    test_l1<dtype, 18560, num_iters>(f); \
    test_l1<dtype, 18568, num_iters>(f); \
    test_l1<dtype, 18576, num_iters>(f); \
    test_l1<dtype, 18584, num_iters>(f); \
    test_l1<dtype, 18592, num_iters>(f); \
    test_l1<dtype, 18600, num_iters>(f); \
    test_l1<dtype, 18608, num_iters>(f); \
    test_l1<dtype, 18616, num_iters>(f); \
    test_l1<dtype, 18624, num_iters>(f); \
    test_l1<dtype, 18632, num_iters>(f); \
    test_l1<dtype, 18640, num_iters>(f); \
    test_l1<dtype, 18648, num_iters>(f); \
    test_l1<dtype, 18656, num_iters>(f); \
    test_l1<dtype, 18664, num_iters>(f); \
    test_l1<dtype, 18672, num_iters>(f); \
    test_l1<dtype, 18680, num_iters>(f); \
    test_l1<dtype, 18688, num_iters>(f); \
    test_l1<dtype, 18696, num_iters>(f); \
    test_l1<dtype, 18704, num_iters>(f); \
    test_l1<dtype, 18712, num_iters>(f); \
    test_l1<dtype, 18720, num_iters>(f); \
    test_l1<dtype, 18728, num_iters>(f); \
    test_l1<dtype, 18736, num_iters>(f); \
    test_l1<dtype, 18744, num_iters>(f); \
    test_l1<dtype, 18752, num_iters>(f); \
    test_l1<dtype, 18760, num_iters>(f); \
    test_l1<dtype, 18768, num_iters>(f); \
    test_l1<dtype, 18776, num_iters>(f); \
    test_l1<dtype, 18784, num_iters>(f); \
    test_l1<dtype, 18792, num_iters>(f); \
    test_l1<dtype, 18800, num_iters>(f); \
    test_l1<dtype, 18808, num_iters>(f); \
    test_l1<dtype, 18816, num_iters>(f); \
    test_l1<dtype, 18824, num_iters>(f); \
    test_l1<dtype, 18832, num_iters>(f); \
    test_l1<dtype, 18840, num_iters>(f); \
    test_l1<dtype, 18848, num_iters>(f); \
    test_l1<dtype, 18856, num_iters>(f); \
    test_l1<dtype, 18864, num_iters>(f); \
    test_l1<dtype, 18872, num_iters>(f); \
    test_l1<dtype, 18880, num_iters>(f); \
    test_l1<dtype, 18888, num_iters>(f); \
    test_l1<dtype, 18896, num_iters>(f); \
    test_l1<dtype, 18904, num_iters>(f); \
    test_l1<dtype, 18912, num_iters>(f); \
    test_l1<dtype, 18920, num_iters>(f); \
    test_l1<dtype, 18928, num_iters>(f); \
    test_l1<dtype, 18936, num_iters>(f); \
    test_l1<dtype, 18944, num_iters>(f); \
    test_l1<dtype, 18952, num_iters>(f); \
    test_l1<dtype, 18960, num_iters>(f); \
    test_l1<dtype, 18968, num_iters>(f); \
    test_l1<dtype, 18976, num_iters>(f); \
    test_l1<dtype, 18984, num_iters>(f); \
    test_l1<dtype, 18992, num_iters>(f); \
    test_l1<dtype, 19000, num_iters>(f); \
    test_l1<dtype, 19008, num_iters>(f); \
    test_l1<dtype, 19016, num_iters>(f); \
    test_l1<dtype, 19024, num_iters>(f); \
    test_l1<dtype, 19032, num_iters>(f); \
    test_l1<dtype, 19040, num_iters>(f); \
    test_l1<dtype, 19048, num_iters>(f); \
    test_l1<dtype, 19056, num_iters>(f); \
    test_l1<dtype, 19064, num_iters>(f); \
    test_l1<dtype, 19072, num_iters>(f); \
    test_l1<dtype, 19080, num_iters>(f); \
    test_l1<dtype, 19088, num_iters>(f); \
    test_l1<dtype, 19096, num_iters>(f); \
    test_l1<dtype, 19104, num_iters>(f); \
    test_l1<dtype, 19112, num_iters>(f); \
    test_l1<dtype, 19120, num_iters>(f); \
    test_l1<dtype, 19128, num_iters>(f); \
    test_l1<dtype, 19136, num_iters>(f); \
    test_l1<dtype, 19144, num_iters>(f); \
    test_l1<dtype, 19152, num_iters>(f); \
    test_l1<dtype, 19160, num_iters>(f); \
    test_l1<dtype, 19168, num_iters>(f); \
    test_l1<dtype, 19176, num_iters>(f); \
    test_l1<dtype, 19184, num_iters>(f); \
    test_l1<dtype, 19192, num_iters>(f); \
    test_l1<dtype, 19200, num_iters>(f); \
    test_l1<dtype, 19208, num_iters>(f); \
    test_l1<dtype, 19216, num_iters>(f); \
    test_l1<dtype, 19224, num_iters>(f); \
    test_l1<dtype, 19232, num_iters>(f); \
    test_l1<dtype, 19240, num_iters>(f); \
    test_l1<dtype, 19248, num_iters>(f); \
    test_l1<dtype, 19256, num_iters>(f); \
    test_l1<dtype, 19264, num_iters>(f); \
    test_l1<dtype, 19272, num_iters>(f); \
    test_l1<dtype, 19280, num_iters>(f); \
    test_l1<dtype, 19288, num_iters>(f); \
    test_l1<dtype, 19296, num_iters>(f); \
    test_l1<dtype, 19304, num_iters>(f); \
    test_l1<dtype, 19312, num_iters>(f); \
    test_l1<dtype, 19320, num_iters>(f); \
    test_l1<dtype, 19328, num_iters>(f); \
    test_l1<dtype, 19336, num_iters>(f); \
    test_l1<dtype, 19344, num_iters>(f); \
    test_l1<dtype, 19352, num_iters>(f); \
    test_l1<dtype, 19360, num_iters>(f); \
    test_l1<dtype, 19368, num_iters>(f); \
    test_l1<dtype, 19376, num_iters>(f); \
    test_l1<dtype, 19384, num_iters>(f); \
    test_l1<dtype, 19392, num_iters>(f); \
    test_l1<dtype, 19400, num_iters>(f); \
    test_l1<dtype, 19408, num_iters>(f); \
    test_l1<dtype, 19416, num_iters>(f); \
    test_l1<dtype, 19424, num_iters>(f); \
    test_l1<dtype, 19432, num_iters>(f); \
    test_l1<dtype, 19440, num_iters>(f); \
    test_l1<dtype, 19448, num_iters>(f); \
    test_l1<dtype, 19456, num_iters>(f); \
    test_l1<dtype, 19464, num_iters>(f); \
    test_l1<dtype, 19472, num_iters>(f); \
    test_l1<dtype, 19480, num_iters>(f); \
    test_l1<dtype, 19488, num_iters>(f); \
    test_l1<dtype, 19496, num_iters>(f); \
    test_l1<dtype, 19504, num_iters>(f); \
    test_l1<dtype, 19512, num_iters>(f); \
    test_l1<dtype, 19520, num_iters>(f); \
    test_l1<dtype, 19528, num_iters>(f); \
    test_l1<dtype, 19536, num_iters>(f); \
    test_l1<dtype, 19544, num_iters>(f); \
    test_l1<dtype, 19552, num_iters>(f); \
    test_l1<dtype, 19560, num_iters>(f); \
    test_l1<dtype, 19568, num_iters>(f); \
    test_l1<dtype, 19576, num_iters>(f); \
    test_l1<dtype, 19584, num_iters>(f); \
    test_l1<dtype, 19592, num_iters>(f); \
    test_l1<dtype, 19600, num_iters>(f); \
    test_l1<dtype, 19608, num_iters>(f); \
    test_l1<dtype, 19616, num_iters>(f); \
    test_l1<dtype, 19624, num_iters>(f); \
    test_l1<dtype, 19632, num_iters>(f); \
    test_l1<dtype, 19640, num_iters>(f); \
    test_l1<dtype, 19648, num_iters>(f); \
    test_l1<dtype, 19656, num_iters>(f); \
    test_l1<dtype, 19664, num_iters>(f); \
    test_l1<dtype, 19672, num_iters>(f); \
    test_l1<dtype, 19680, num_iters>(f); \
    test_l1<dtype, 19688, num_iters>(f); \
    test_l1<dtype, 19696, num_iters>(f); \
    test_l1<dtype, 19704, num_iters>(f); \
    test_l1<dtype, 19712, num_iters>(f); \
    test_l1<dtype, 19720, num_iters>(f); \
    test_l1<dtype, 19728, num_iters>(f); \
    test_l1<dtype, 19736, num_iters>(f); \
    test_l1<dtype, 19744, num_iters>(f); \
    test_l1<dtype, 19752, num_iters>(f); \
    test_l1<dtype, 19760, num_iters>(f); \
    test_l1<dtype, 19768, num_iters>(f); \
    test_l1<dtype, 19776, num_iters>(f); \
    test_l1<dtype, 19784, num_iters>(f); \
    test_l1<dtype, 19792, num_iters>(f); \
    test_l1<dtype, 19800, num_iters>(f); \
    test_l1<dtype, 19808, num_iters>(f); \
    test_l1<dtype, 19816, num_iters>(f); \
    test_l1<dtype, 19824, num_iters>(f); \
    test_l1<dtype, 19832, num_iters>(f); \
    test_l1<dtype, 19840, num_iters>(f); \
    test_l1<dtype, 19848, num_iters>(f); \
    test_l1<dtype, 19856, num_iters>(f); \
    test_l1<dtype, 19864, num_iters>(f); \
    test_l1<dtype, 19872, num_iters>(f); \
    test_l1<dtype, 19880, num_iters>(f); \
    test_l1<dtype, 19888, num_iters>(f); \
    test_l1<dtype, 19896, num_iters>(f); \
    test_l1<dtype, 19904, num_iters>(f); \
    test_l1<dtype, 19912, num_iters>(f); \
    test_l1<dtype, 19920, num_iters>(f); \
    test_l1<dtype, 19928, num_iters>(f); \
    test_l1<dtype, 19936, num_iters>(f); \
    test_l1<dtype, 19944, num_iters>(f); \
    test_l1<dtype, 19952, num_iters>(f); \
    test_l1<dtype, 19960, num_iters>(f); \
    test_l1<dtype, 19968, num_iters>(f); \
    test_l1<dtype, 19976, num_iters>(f); \
    test_l1<dtype, 19984, num_iters>(f); \
    test_l1<dtype, 19992, num_iters>(f); \
    test_l1<dtype, 20000, num_iters>(f); \
    test_l1<dtype, 20008, num_iters>(f); \
    test_l1<dtype, 20016, num_iters>(f); \
    test_l1<dtype, 20024, num_iters>(f); \
    test_l1<dtype, 20032, num_iters>(f); \
    test_l1<dtype, 20040, num_iters>(f); \
    test_l1<dtype, 20048, num_iters>(f); \
    test_l1<dtype, 20056, num_iters>(f); \
    test_l1<dtype, 20064, num_iters>(f); \
    test_l1<dtype, 20072, num_iters>(f); \
    test_l1<dtype, 20080, num_iters>(f); \
    test_l1<dtype, 20088, num_iters>(f); \
    test_l1<dtype, 20096, num_iters>(f); \
    test_l1<dtype, 20104, num_iters>(f); \
    test_l1<dtype, 20112, num_iters>(f); \
    test_l1<dtype, 20120, num_iters>(f); \
    test_l1<dtype, 20128, num_iters>(f); \
    test_l1<dtype, 20136, num_iters>(f); \
    test_l1<dtype, 20144, num_iters>(f); \
    test_l1<dtype, 20152, num_iters>(f); \
    test_l1<dtype, 20160, num_iters>(f); \
    test_l1<dtype, 20168, num_iters>(f); \
    test_l1<dtype, 20176, num_iters>(f); \
    test_l1<dtype, 20184, num_iters>(f); \
    test_l1<dtype, 20192, num_iters>(f); \
    test_l1<dtype, 20200, num_iters>(f); \
    test_l1<dtype, 20208, num_iters>(f); \
    test_l1<dtype, 20216, num_iters>(f); \
    test_l1<dtype, 20224, num_iters>(f); \
    test_l1<dtype, 20232, num_iters>(f); \
    test_l1<dtype, 20240, num_iters>(f); \
    test_l1<dtype, 20248, num_iters>(f); \
    test_l1<dtype, 20256, num_iters>(f); \
    test_l1<dtype, 20264, num_iters>(f); \
    test_l1<dtype, 20272, num_iters>(f); \
    test_l1<dtype, 20280, num_iters>(f); \
    test_l1<dtype, 20288, num_iters>(f); \
    test_l1<dtype, 20296, num_iters>(f); \
    test_l1<dtype, 20304, num_iters>(f); \
    test_l1<dtype, 20312, num_iters>(f); \
    test_l1<dtype, 20320, num_iters>(f); \
    test_l1<dtype, 20328, num_iters>(f); \
    test_l1<dtype, 20336, num_iters>(f); \
    test_l1<dtype, 20344, num_iters>(f); \
    test_l1<dtype, 20352, num_iters>(f); \
    test_l1<dtype, 20360, num_iters>(f); \
    test_l1<dtype, 20368, num_iters>(f); \
    test_l1<dtype, 20376, num_iters>(f); \
    test_l1<dtype, 20384, num_iters>(f); \
    test_l1<dtype, 20392, num_iters>(f); \
    test_l1<dtype, 20400, num_iters>(f); \
    test_l1<dtype, 20408, num_iters>(f); \
    test_l1<dtype, 20416, num_iters>(f); \
    test_l1<dtype, 20424, num_iters>(f); \
    test_l1<dtype, 20432, num_iters>(f); \
    test_l1<dtype, 20440, num_iters>(f); \
    test_l1<dtype, 20448, num_iters>(f); \
    test_l1<dtype, 20456, num_iters>(f); \
    test_l1<dtype, 20464, num_iters>(f); \
    test_l1<dtype, 20472, num_iters>(f); \
    test_l1<dtype, 20480, num_iters>(f); \
    test_l1<dtype, 20488, num_iters>(f); \
    test_l1<dtype, 20496, num_iters>(f); \
    test_l1<dtype, 20504, num_iters>(f); \
    test_l1<dtype, 20512, num_iters>(f); \
    test_l1<dtype, 20520, num_iters>(f); \
    test_l1<dtype, 20528, num_iters>(f); \
    test_l1<dtype, 20536, num_iters>(f); \
    test_l1<dtype, 20544, num_iters>(f); \
    test_l1<dtype, 20552, num_iters>(f); \
    test_l1<dtype, 20560, num_iters>(f); \
    test_l1<dtype, 20568, num_iters>(f); \
    test_l1<dtype, 20576, num_iters>(f); \
    test_l1<dtype, 20584, num_iters>(f); \
    test_l1<dtype, 20592, num_iters>(f); \
    test_l1<dtype, 20600, num_iters>(f); \
    test_l1<dtype, 20608, num_iters>(f); \
    test_l1<dtype, 20616, num_iters>(f); \
    test_l1<dtype, 20624, num_iters>(f); \
    test_l1<dtype, 20632, num_iters>(f); \
    test_l1<dtype, 20640, num_iters>(f); \
    test_l1<dtype, 20648, num_iters>(f); \
    test_l1<dtype, 20656, num_iters>(f); \
    test_l1<dtype, 20664, num_iters>(f); \
    test_l1<dtype, 20672, num_iters>(f); \
    test_l1<dtype, 20680, num_iters>(f); \
    test_l1<dtype, 20688, num_iters>(f); \
    test_l1<dtype, 20696, num_iters>(f); \
    test_l1<dtype, 20704, num_iters>(f); \
    test_l1<dtype, 20712, num_iters>(f); \
    test_l1<dtype, 20720, num_iters>(f); \
    test_l1<dtype, 20728, num_iters>(f); \
    test_l1<dtype, 20736, num_iters>(f); \
    test_l1<dtype, 20744, num_iters>(f); \
    test_l1<dtype, 20752, num_iters>(f); \
    test_l1<dtype, 20760, num_iters>(f); \
    test_l1<dtype, 20768, num_iters>(f); \
    test_l1<dtype, 20776, num_iters>(f); \
    test_l1<dtype, 20784, num_iters>(f); \
    test_l1<dtype, 20792, num_iters>(f); \
    test_l1<dtype, 20800, num_iters>(f); \
    test_l1<dtype, 20808, num_iters>(f); \
    test_l1<dtype, 20816, num_iters>(f); \
    test_l1<dtype, 20824, num_iters>(f); \
    test_l1<dtype, 20832, num_iters>(f); \
    test_l1<dtype, 20840, num_iters>(f); \
    test_l1<dtype, 20848, num_iters>(f); \
    test_l1<dtype, 20856, num_iters>(f); \
    test_l1<dtype, 20864, num_iters>(f); \
    test_l1<dtype, 20872, num_iters>(f); \
    test_l1<dtype, 20880, num_iters>(f); \
    test_l1<dtype, 20888, num_iters>(f); \
    test_l1<dtype, 20896, num_iters>(f); \
    test_l1<dtype, 20904, num_iters>(f); \
    test_l1<dtype, 20912, num_iters>(f); \
    test_l1<dtype, 20920, num_iters>(f); \
    test_l1<dtype, 20928, num_iters>(f); \
    test_l1<dtype, 20936, num_iters>(f); \
    test_l1<dtype, 20944, num_iters>(f); \
    test_l1<dtype, 20952, num_iters>(f); \
    test_l1<dtype, 20960, num_iters>(f); \
    test_l1<dtype, 20968, num_iters>(f); \
    test_l1<dtype, 20976, num_iters>(f); \
    test_l1<dtype, 20984, num_iters>(f); \
    test_l1<dtype, 20992, num_iters>(f); \
    test_l1<dtype, 21000, num_iters>(f); \
    test_l1<dtype, 21008, num_iters>(f); \
    test_l1<dtype, 21016, num_iters>(f); \
    test_l1<dtype, 21024, num_iters>(f); \
    test_l1<dtype, 21032, num_iters>(f); \
    test_l1<dtype, 21040, num_iters>(f); \
    test_l1<dtype, 21048, num_iters>(f); \
    test_l1<dtype, 21056, num_iters>(f); \
    test_l1<dtype, 21064, num_iters>(f); \
    test_l1<dtype, 21072, num_iters>(f); \
    test_l1<dtype, 21080, num_iters>(f); \
    test_l1<dtype, 21088, num_iters>(f); \
    test_l1<dtype, 21096, num_iters>(f); \
    test_l1<dtype, 21104, num_iters>(f); \
    test_l1<dtype, 21112, num_iters>(f); \
    test_l1<dtype, 21120, num_iters>(f); \
    test_l1<dtype, 21128, num_iters>(f); \
    test_l1<dtype, 21136, num_iters>(f); \
    test_l1<dtype, 21144, num_iters>(f); \
    test_l1<dtype, 21152, num_iters>(f); \
    test_l1<dtype, 21160, num_iters>(f); \
    test_l1<dtype, 21168, num_iters>(f); \
    test_l1<dtype, 21176, num_iters>(f); \
    test_l1<dtype, 21184, num_iters>(f); \
    test_l1<dtype, 21192, num_iters>(f); \
    test_l1<dtype, 21200, num_iters>(f); \
    test_l1<dtype, 21208, num_iters>(f); \
    test_l1<dtype, 21216, num_iters>(f); \
    test_l1<dtype, 21224, num_iters>(f); \
    test_l1<dtype, 21232, num_iters>(f); \
    test_l1<dtype, 21240, num_iters>(f); \
    test_l1<dtype, 21248, num_iters>(f); \
    test_l1<dtype, 21256, num_iters>(f); \
    test_l1<dtype, 21264, num_iters>(f); \
    test_l1<dtype, 21272, num_iters>(f); \
    test_l1<dtype, 21280, num_iters>(f); \
    test_l1<dtype, 21288, num_iters>(f); \
    test_l1<dtype, 21296, num_iters>(f); \
    test_l1<dtype, 21304, num_iters>(f); \
    test_l1<dtype, 21312, num_iters>(f); \
    test_l1<dtype, 21320, num_iters>(f); \
    test_l1<dtype, 21328, num_iters>(f); \
    test_l1<dtype, 21336, num_iters>(f); \
    test_l1<dtype, 21344, num_iters>(f); \
    test_l1<dtype, 21352, num_iters>(f); \
    test_l1<dtype, 21360, num_iters>(f); \
    test_l1<dtype, 21368, num_iters>(f); \
    test_l1<dtype, 21376, num_iters>(f); \
    test_l1<dtype, 21384, num_iters>(f); \
    test_l1<dtype, 21392, num_iters>(f); \
    test_l1<dtype, 21400, num_iters>(f); \
    test_l1<dtype, 21408, num_iters>(f); \
    test_l1<dtype, 21416, num_iters>(f); \
    test_l1<dtype, 21424, num_iters>(f); \
    test_l1<dtype, 21432, num_iters>(f); \
    test_l1<dtype, 21440, num_iters>(f); \
    test_l1<dtype, 21448, num_iters>(f); \
    test_l1<dtype, 21456, num_iters>(f); \
    test_l1<dtype, 21464, num_iters>(f); \
    test_l1<dtype, 21472, num_iters>(f); \
    test_l1<dtype, 21480, num_iters>(f); \
    test_l1<dtype, 21488, num_iters>(f); \
    test_l1<dtype, 21496, num_iters>(f); \
    test_l1<dtype, 21504, num_iters>(f); \
    test_l1<dtype, 21512, num_iters>(f); \
    test_l1<dtype, 21520, num_iters>(f); \
    test_l1<dtype, 21528, num_iters>(f); \
    test_l1<dtype, 21536, num_iters>(f); \
    test_l1<dtype, 21544, num_iters>(f); \
    test_l1<dtype, 21552, num_iters>(f); \
    test_l1<dtype, 21560, num_iters>(f); \
    test_l1<dtype, 21568, num_iters>(f); \
    test_l1<dtype, 21576, num_iters>(f); \
    test_l1<dtype, 21584, num_iters>(f); \
    test_l1<dtype, 21592, num_iters>(f); \
    test_l1<dtype, 21600, num_iters>(f); \
    test_l1<dtype, 21608, num_iters>(f); \
    test_l1<dtype, 21616, num_iters>(f); \
    test_l1<dtype, 21624, num_iters>(f); \
    test_l1<dtype, 21632, num_iters>(f); \
    test_l1<dtype, 21640, num_iters>(f); \
    test_l1<dtype, 21648, num_iters>(f); \
    test_l1<dtype, 21656, num_iters>(f); \
    test_l1<dtype, 21664, num_iters>(f); \
    test_l1<dtype, 21672, num_iters>(f); \
    test_l1<dtype, 21680, num_iters>(f); \
    test_l1<dtype, 21688, num_iters>(f); \
    test_l1<dtype, 21696, num_iters>(f); \
    test_l1<dtype, 21704, num_iters>(f); \
    test_l1<dtype, 21712, num_iters>(f); \
    test_l1<dtype, 21720, num_iters>(f); \
    test_l1<dtype, 21728, num_iters>(f); \
    test_l1<dtype, 21736, num_iters>(f); \
    test_l1<dtype, 21744, num_iters>(f); \
    test_l1<dtype, 21752, num_iters>(f); \
    test_l1<dtype, 21760, num_iters>(f); \
    test_l1<dtype, 21768, num_iters>(f); \
    test_l1<dtype, 21776, num_iters>(f); \
    test_l1<dtype, 21784, num_iters>(f); \
    test_l1<dtype, 21792, num_iters>(f); \
    test_l1<dtype, 21800, num_iters>(f); \
    test_l1<dtype, 21808, num_iters>(f); \
    test_l1<dtype, 21816, num_iters>(f); \
    test_l1<dtype, 21824, num_iters>(f); \
    test_l1<dtype, 21832, num_iters>(f); \
    test_l1<dtype, 21840, num_iters>(f); \
    test_l1<dtype, 21848, num_iters>(f); \
    test_l1<dtype, 21856, num_iters>(f); \
    test_l1<dtype, 21864, num_iters>(f); \
    test_l1<dtype, 21872, num_iters>(f); \
    test_l1<dtype, 21880, num_iters>(f); \
    test_l1<dtype, 21888, num_iters>(f); \
    test_l1<dtype, 21896, num_iters>(f); \
    test_l1<dtype, 21904, num_iters>(f); \
    test_l1<dtype, 21912, num_iters>(f); \
    test_l1<dtype, 21920, num_iters>(f); \
    test_l1<dtype, 21928, num_iters>(f); \
    test_l1<dtype, 21936, num_iters>(f); \
    test_l1<dtype, 21944, num_iters>(f); \
    test_l1<dtype, 21952, num_iters>(f); \
    test_l1<dtype, 21960, num_iters>(f); \
    test_l1<dtype, 21968, num_iters>(f); \
    test_l1<dtype, 21976, num_iters>(f); \
    test_l1<dtype, 21984, num_iters>(f); \
    test_l1<dtype, 21992, num_iters>(f); \
    test_l1<dtype, 22000, num_iters>(f); \
    test_l1<dtype, 22008, num_iters>(f); \
    test_l1<dtype, 22016, num_iters>(f); \
    test_l1<dtype, 22024, num_iters>(f); \
    test_l1<dtype, 22032, num_iters>(f); \
    test_l1<dtype, 22040, num_iters>(f); \
    test_l1<dtype, 22048, num_iters>(f); \
    test_l1<dtype, 22056, num_iters>(f); \
    test_l1<dtype, 22064, num_iters>(f); \
    test_l1<dtype, 22072, num_iters>(f); \
    test_l1<dtype, 22080, num_iters>(f); \
    test_l1<dtype, 22088, num_iters>(f); \
    test_l1<dtype, 22096, num_iters>(f); \
    test_l1<dtype, 22104, num_iters>(f); \
    test_l1<dtype, 22112, num_iters>(f); \
    test_l1<dtype, 22120, num_iters>(f); \
    test_l1<dtype, 22128, num_iters>(f); \
    test_l1<dtype, 22136, num_iters>(f); \
    test_l1<dtype, 22144, num_iters>(f); \
    test_l1<dtype, 22152, num_iters>(f); \
    test_l1<dtype, 22160, num_iters>(f); \
    test_l1<dtype, 22168, num_iters>(f); \
    test_l1<dtype, 22176, num_iters>(f); \
    test_l1<dtype, 22184, num_iters>(f); \
    test_l1<dtype, 22192, num_iters>(f); \
    test_l1<dtype, 22200, num_iters>(f); \
    test_l1<dtype, 22208, num_iters>(f); \
    test_l1<dtype, 22216, num_iters>(f); \
    test_l1<dtype, 22224, num_iters>(f); \
    test_l1<dtype, 22232, num_iters>(f); \
    test_l1<dtype, 22240, num_iters>(f); \
    test_l1<dtype, 22248, num_iters>(f); \
    test_l1<dtype, 22256, num_iters>(f); \
    test_l1<dtype, 22264, num_iters>(f); \
    test_l1<dtype, 22272, num_iters>(f); \
    test_l1<dtype, 22280, num_iters>(f); \
    test_l1<dtype, 22288, num_iters>(f); \
    test_l1<dtype, 22296, num_iters>(f); \
    test_l1<dtype, 22304, num_iters>(f); \
    test_l1<dtype, 22312, num_iters>(f); \
    test_l1<dtype, 22320, num_iters>(f); \
    test_l1<dtype, 22328, num_iters>(f); \
    test_l1<dtype, 22336, num_iters>(f); \
    test_l1<dtype, 22344, num_iters>(f); \
    test_l1<dtype, 22352, num_iters>(f); \
    test_l1<dtype, 22360, num_iters>(f); \
    test_l1<dtype, 22368, num_iters>(f); \
    test_l1<dtype, 22376, num_iters>(f); \
    test_l1<dtype, 22384, num_iters>(f); \
    test_l1<dtype, 22392, num_iters>(f); \
    test_l1<dtype, 22400, num_iters>(f); \
    test_l1<dtype, 22408, num_iters>(f); \
    test_l1<dtype, 22416, num_iters>(f); \
    test_l1<dtype, 22424, num_iters>(f); \
    test_l1<dtype, 22432, num_iters>(f); \
    test_l1<dtype, 22440, num_iters>(f); \
    test_l1<dtype, 22448, num_iters>(f); \
    test_l1<dtype, 22456, num_iters>(f); \
    test_l1<dtype, 22464, num_iters>(f); \
    test_l1<dtype, 22472, num_iters>(f); \
    test_l1<dtype, 22480, num_iters>(f); \
    test_l1<dtype, 22488, num_iters>(f); \
    test_l1<dtype, 22496, num_iters>(f); \
    test_l1<dtype, 22504, num_iters>(f); \
    test_l1<dtype, 22512, num_iters>(f); \
    test_l1<dtype, 22520, num_iters>(f); \
    test_l1<dtype, 22528, num_iters>(f); \
    test_l1<dtype, 22536, num_iters>(f); \
    test_l1<dtype, 22544, num_iters>(f); \
    test_l1<dtype, 22552, num_iters>(f); \
    test_l1<dtype, 22560, num_iters>(f); \
    test_l1<dtype, 22568, num_iters>(f); \
    test_l1<dtype, 22576, num_iters>(f); \
    test_l1<dtype, 22584, num_iters>(f); \
    test_l1<dtype, 22592, num_iters>(f); \
    test_l1<dtype, 22600, num_iters>(f); \
    test_l1<dtype, 22608, num_iters>(f); \
    test_l1<dtype, 22616, num_iters>(f); \
    test_l1<dtype, 22624, num_iters>(f); \
    test_l1<dtype, 22632, num_iters>(f); \
    test_l1<dtype, 22640, num_iters>(f); \
    test_l1<dtype, 22648, num_iters>(f); \
    test_l1<dtype, 22656, num_iters>(f); \
    test_l1<dtype, 22664, num_iters>(f); \
    test_l1<dtype, 22672, num_iters>(f); \
    test_l1<dtype, 22680, num_iters>(f); \
    test_l1<dtype, 22688, num_iters>(f); \
    test_l1<dtype, 22696, num_iters>(f); \
    test_l1<dtype, 22704, num_iters>(f); \
    test_l1<dtype, 22712, num_iters>(f); \
    test_l1<dtype, 22720, num_iters>(f); \
    test_l1<dtype, 22728, num_iters>(f); \
    test_l1<dtype, 22736, num_iters>(f); \
    test_l1<dtype, 22744, num_iters>(f); \
    test_l1<dtype, 22752, num_iters>(f); \
    test_l1<dtype, 22760, num_iters>(f); \
    test_l1<dtype, 22768, num_iters>(f); \
    test_l1<dtype, 22776, num_iters>(f); \
    test_l1<dtype, 22784, num_iters>(f); \
    test_l1<dtype, 22792, num_iters>(f); \
    test_l1<dtype, 22800, num_iters>(f); \
    test_l1<dtype, 22808, num_iters>(f); \
    test_l1<dtype, 22816, num_iters>(f); \
    test_l1<dtype, 22824, num_iters>(f); \
    test_l1<dtype, 22832, num_iters>(f); \
    test_l1<dtype, 22840, num_iters>(f); \
    test_l1<dtype, 22848, num_iters>(f); \
    test_l1<dtype, 22856, num_iters>(f); \
    test_l1<dtype, 22864, num_iters>(f); \
    test_l1<dtype, 22872, num_iters>(f); \
    test_l1<dtype, 22880, num_iters>(f); \
    test_l1<dtype, 22888, num_iters>(f); \
    test_l1<dtype, 22896, num_iters>(f); \
    test_l1<dtype, 22904, num_iters>(f); \
    test_l1<dtype, 22912, num_iters>(f); \
    test_l1<dtype, 22920, num_iters>(f); \
    test_l1<dtype, 22928, num_iters>(f); \
    test_l1<dtype, 22936, num_iters>(f); \
    test_l1<dtype, 22944, num_iters>(f); \
    test_l1<dtype, 22952, num_iters>(f); \
    test_l1<dtype, 22960, num_iters>(f); \
    test_l1<dtype, 22968, num_iters>(f); \
    test_l1<dtype, 22976, num_iters>(f); \
    test_l1<dtype, 22984, num_iters>(f); \
    test_l1<dtype, 22992, num_iters>(f); \
    test_l1<dtype, 23000, num_iters>(f); \
    test_l1<dtype, 23008, num_iters>(f); \
    test_l1<dtype, 23016, num_iters>(f); \
    test_l1<dtype, 23024, num_iters>(f); \
    test_l1<dtype, 23032, num_iters>(f); \
    test_l1<dtype, 23040, num_iters>(f); \
    test_l1<dtype, 23048, num_iters>(f); \
    test_l1<dtype, 23056, num_iters>(f); \
    test_l1<dtype, 23064, num_iters>(f); \
    test_l1<dtype, 23072, num_iters>(f); \
    test_l1<dtype, 23080, num_iters>(f); \
    test_l1<dtype, 23088, num_iters>(f); \
    test_l1<dtype, 23096, num_iters>(f); \
    test_l1<dtype, 23104, num_iters>(f); \
    test_l1<dtype, 23112, num_iters>(f); \
    test_l1<dtype, 23120, num_iters>(f); \
    test_l1<dtype, 23128, num_iters>(f); \
    test_l1<dtype, 23136, num_iters>(f); \
    test_l1<dtype, 23144, num_iters>(f); \
    test_l1<dtype, 23152, num_iters>(f); \
    test_l1<dtype, 23160, num_iters>(f); \
    test_l1<dtype, 23168, num_iters>(f); \
    test_l1<dtype, 23176, num_iters>(f); \
    test_l1<dtype, 23184, num_iters>(f); \
    test_l1<dtype, 23192, num_iters>(f); \
    test_l1<dtype, 23200, num_iters>(f); \
    test_l1<dtype, 23208, num_iters>(f); \
    test_l1<dtype, 23216, num_iters>(f); \
    test_l1<dtype, 23224, num_iters>(f); \
    test_l1<dtype, 23232, num_iters>(f); \
    test_l1<dtype, 23240, num_iters>(f); \
    test_l1<dtype, 23248, num_iters>(f); \
    test_l1<dtype, 23256, num_iters>(f); \
    test_l1<dtype, 23264, num_iters>(f); \
    test_l1<dtype, 23272, num_iters>(f); \
    test_l1<dtype, 23280, num_iters>(f); \
    test_l1<dtype, 23288, num_iters>(f); \
    test_l1<dtype, 23296, num_iters>(f); \
    test_l1<dtype, 23304, num_iters>(f); \
    test_l1<dtype, 23312, num_iters>(f); \
    test_l1<dtype, 23320, num_iters>(f); \
    test_l1<dtype, 23328, num_iters>(f); \
    test_l1<dtype, 23336, num_iters>(f); \
    test_l1<dtype, 23344, num_iters>(f); \
    test_l1<dtype, 23352, num_iters>(f); \
    test_l1<dtype, 23360, num_iters>(f); \
    test_l1<dtype, 23368, num_iters>(f); \
    test_l1<dtype, 23376, num_iters>(f); \
    test_l1<dtype, 23384, num_iters>(f); \
    test_l1<dtype, 23392, num_iters>(f); \
    test_l1<dtype, 23400, num_iters>(f); \
    test_l1<dtype, 23408, num_iters>(f); \
    test_l1<dtype, 23416, num_iters>(f); \
    test_l1<dtype, 23424, num_iters>(f); \
    test_l1<dtype, 23432, num_iters>(f); \
    test_l1<dtype, 23440, num_iters>(f); \
    test_l1<dtype, 23448, num_iters>(f); \
    test_l1<dtype, 23456, num_iters>(f); \
    test_l1<dtype, 23464, num_iters>(f); \
    test_l1<dtype, 23472, num_iters>(f); \
    test_l1<dtype, 23480, num_iters>(f); \
    test_l1<dtype, 23488, num_iters>(f); \
    test_l1<dtype, 23496, num_iters>(f); \
    test_l1<dtype, 23504, num_iters>(f); \
    test_l1<dtype, 23512, num_iters>(f); \
    test_l1<dtype, 23520, num_iters>(f); \
    test_l1<dtype, 23528, num_iters>(f); \
    test_l1<dtype, 23536, num_iters>(f); \
    test_l1<dtype, 23544, num_iters>(f); \
    test_l1<dtype, 23552, num_iters>(f); \
    test_l1<dtype, 23560, num_iters>(f); \
    test_l1<dtype, 23568, num_iters>(f); \
    test_l1<dtype, 23576, num_iters>(f); \
    test_l1<dtype, 23584, num_iters>(f); \
    test_l1<dtype, 23592, num_iters>(f); \
    test_l1<dtype, 23600, num_iters>(f); \
    test_l1<dtype, 23608, num_iters>(f); \
    test_l1<dtype, 23616, num_iters>(f); \
    test_l1<dtype, 23624, num_iters>(f); \
    test_l1<dtype, 23632, num_iters>(f); \
    test_l1<dtype, 23640, num_iters>(f); \
    test_l1<dtype, 23648, num_iters>(f); \
    test_l1<dtype, 23656, num_iters>(f); \
    test_l1<dtype, 23664, num_iters>(f); \
    test_l1<dtype, 23672, num_iters>(f); \
    test_l1<dtype, 23680, num_iters>(f); \
    test_l1<dtype, 23688, num_iters>(f); \
    test_l1<dtype, 23696, num_iters>(f); \
    test_l1<dtype, 23704, num_iters>(f); \
    test_l1<dtype, 23712, num_iters>(f); \
    test_l1<dtype, 23720, num_iters>(f); \
    test_l1<dtype, 23728, num_iters>(f); \
    test_l1<dtype, 23736, num_iters>(f); \
    test_l1<dtype, 23744, num_iters>(f); \
    test_l1<dtype, 23752, num_iters>(f); \
    test_l1<dtype, 23760, num_iters>(f); \
    test_l1<dtype, 23768, num_iters>(f); \
    test_l1<dtype, 23776, num_iters>(f); \
    test_l1<dtype, 23784, num_iters>(f); \
    test_l1<dtype, 23792, num_iters>(f); \
    test_l1<dtype, 23800, num_iters>(f); \
    test_l1<dtype, 23808, num_iters>(f); \
    test_l1<dtype, 23816, num_iters>(f); \
    test_l1<dtype, 23824, num_iters>(f); \
    test_l1<dtype, 23832, num_iters>(f); \
    test_l1<dtype, 23840, num_iters>(f); \
    test_l1<dtype, 23848, num_iters>(f); \
    test_l1<dtype, 23856, num_iters>(f); \
    test_l1<dtype, 23864, num_iters>(f); \
    test_l1<dtype, 23872, num_iters>(f); \
    test_l1<dtype, 23880, num_iters>(f); \
    test_l1<dtype, 23888, num_iters>(f); \
    test_l1<dtype, 23896, num_iters>(f); \
    test_l1<dtype, 23904, num_iters>(f); \
    test_l1<dtype, 23912, num_iters>(f); \
    test_l1<dtype, 23920, num_iters>(f); \
    test_l1<dtype, 23928, num_iters>(f); \
    test_l1<dtype, 23936, num_iters>(f); \
    test_l1<dtype, 23944, num_iters>(f); \
    test_l1<dtype, 23952, num_iters>(f); \
    test_l1<dtype, 23960, num_iters>(f); \
    test_l1<dtype, 23968, num_iters>(f); \
    test_l1<dtype, 23976, num_iters>(f); \
    test_l1<dtype, 23984, num_iters>(f); \
    test_l1<dtype, 23992, num_iters>(f); \
    test_l1<dtype, 24000, num_iters>(f); \
    test_l1<dtype, 24008, num_iters>(f); \
    test_l1<dtype, 24016, num_iters>(f); \
    test_l1<dtype, 24024, num_iters>(f); \
    test_l1<dtype, 24032, num_iters>(f); \
    test_l1<dtype, 24040, num_iters>(f); \
    test_l1<dtype, 24048, num_iters>(f); \
    test_l1<dtype, 24056, num_iters>(f); \
    test_l1<dtype, 24064, num_iters>(f); \
    test_l1<dtype, 24072, num_iters>(f); \
    test_l1<dtype, 24080, num_iters>(f); \
    test_l1<dtype, 24088, num_iters>(f); \
    test_l1<dtype, 24096, num_iters>(f); \
    test_l1<dtype, 24104, num_iters>(f); \
    test_l1<dtype, 24112, num_iters>(f); \
    test_l1<dtype, 24120, num_iters>(f); \
    test_l1<dtype, 24128, num_iters>(f); \
    test_l1<dtype, 24136, num_iters>(f); \
    test_l1<dtype, 24144, num_iters>(f); \
    test_l1<dtype, 24152, num_iters>(f); \
    test_l1<dtype, 24160, num_iters>(f); \
    test_l1<dtype, 24168, num_iters>(f); \
    test_l1<dtype, 24176, num_iters>(f); \
    test_l1<dtype, 24184, num_iters>(f); \
    test_l1<dtype, 24192, num_iters>(f); \
    test_l1<dtype, 24200, num_iters>(f); \
    test_l1<dtype, 24208, num_iters>(f); \
    test_l1<dtype, 24216, num_iters>(f); \
    test_l1<dtype, 24224, num_iters>(f); \
    test_l1<dtype, 24232, num_iters>(f); \
    test_l1<dtype, 24240, num_iters>(f); \
    test_l1<dtype, 24248, num_iters>(f); \
    test_l1<dtype, 24256, num_iters>(f); \
    test_l1<dtype, 24264, num_iters>(f); \
    test_l1<dtype, 24272, num_iters>(f); \
    test_l1<dtype, 24280, num_iters>(f); \
    test_l1<dtype, 24288, num_iters>(f); \
    test_l1<dtype, 24296, num_iters>(f); \
    test_l1<dtype, 24304, num_iters>(f); \
    test_l1<dtype, 24312, num_iters>(f); \
    test_l1<dtype, 24320, num_iters>(f); \
    test_l1<dtype, 24328, num_iters>(f); \
    test_l1<dtype, 24336, num_iters>(f); \
    test_l1<dtype, 24344, num_iters>(f); \
    test_l1<dtype, 24352, num_iters>(f); \
    test_l1<dtype, 24360, num_iters>(f); \
    test_l1<dtype, 24368, num_iters>(f); \
    test_l1<dtype, 24376, num_iters>(f); \
    test_l1<dtype, 24384, num_iters>(f); \
    test_l1<dtype, 24392, num_iters>(f); \
    test_l1<dtype, 24400, num_iters>(f); \
    test_l1<dtype, 24408, num_iters>(f); \
    test_l1<dtype, 24416, num_iters>(f); \
    test_l1<dtype, 24424, num_iters>(f); \
    test_l1<dtype, 24432, num_iters>(f); \
    test_l1<dtype, 24440, num_iters>(f); \
    test_l1<dtype, 24448, num_iters>(f); \
    test_l1<dtype, 24456, num_iters>(f); \
    test_l1<dtype, 24464, num_iters>(f); \
    test_l1<dtype, 24472, num_iters>(f); \
    test_l1<dtype, 24480, num_iters>(f); \
    test_l1<dtype, 24488, num_iters>(f); \
    test_l1<dtype, 24496, num_iters>(f); \
    test_l1<dtype, 24504, num_iters>(f); \
    test_l1<dtype, 24512, num_iters>(f); \
    test_l1<dtype, 24520, num_iters>(f); \
    test_l1<dtype, 24528, num_iters>(f); \
    test_l1<dtype, 24536, num_iters>(f); \
    test_l1<dtype, 24544, num_iters>(f); \
    test_l1<dtype, 24552, num_iters>(f); \
    test_l1<dtype, 24560, num_iters>(f); \
    test_l1<dtype, 24568, num_iters>(f); \
    test_l1<dtype, 24576, num_iters>(f); \
    test_l1<dtype, 24584, num_iters>(f); \
    test_l1<dtype, 24592, num_iters>(f); \
    test_l1<dtype, 24600, num_iters>(f); \
    test_l1<dtype, 24608, num_iters>(f); \
    test_l1<dtype, 24616, num_iters>(f); \
    test_l1<dtype, 24624, num_iters>(f); \
    test_l1<dtype, 24632, num_iters>(f); \
    test_l1<dtype, 24640, num_iters>(f); \
    test_l1<dtype, 24648, num_iters>(f); \
    test_l1<dtype, 24656, num_iters>(f); \
    test_l1<dtype, 24664, num_iters>(f); \
    test_l1<dtype, 24672, num_iters>(f); \
    test_l1<dtype, 24680, num_iters>(f); \
    test_l1<dtype, 24688, num_iters>(f); \
    test_l1<dtype, 24696, num_iters>(f); \
    test_l1<dtype, 24704, num_iters>(f); \
    test_l1<dtype, 24712, num_iters>(f); \
    test_l1<dtype, 24720, num_iters>(f); \
    test_l1<dtype, 24728, num_iters>(f); \
    test_l1<dtype, 24736, num_iters>(f); \
    test_l1<dtype, 24744, num_iters>(f); \
    test_l1<dtype, 24752, num_iters>(f); \
    test_l1<dtype, 24760, num_iters>(f); \
    test_l1<dtype, 24768, num_iters>(f); \
    test_l1<dtype, 24776, num_iters>(f); \
    test_l1<dtype, 24784, num_iters>(f); \
    test_l1<dtype, 24792, num_iters>(f); \
    test_l1<dtype, 24800, num_iters>(f); \
    test_l1<dtype, 24808, num_iters>(f); \
    test_l1<dtype, 24816, num_iters>(f); \
    test_l1<dtype, 24824, num_iters>(f); \
    test_l1<dtype, 24832, num_iters>(f); \
    test_l1<dtype, 24840, num_iters>(f); \
    test_l1<dtype, 24848, num_iters>(f); \
    test_l1<dtype, 24856, num_iters>(f); \
    test_l1<dtype, 24864, num_iters>(f); \
    test_l1<dtype, 24872, num_iters>(f); \
    test_l1<dtype, 24880, num_iters>(f); \
    test_l1<dtype, 24888, num_iters>(f); \
    test_l1<dtype, 24896, num_iters>(f); \
    test_l1<dtype, 24904, num_iters>(f); \
    test_l1<dtype, 24912, num_iters>(f); \
    test_l1<dtype, 24920, num_iters>(f); \
    test_l1<dtype, 24928, num_iters>(f); \
    test_l1<dtype, 24936, num_iters>(f); \
    test_l1<dtype, 24944, num_iters>(f); \
    test_l1<dtype, 24952, num_iters>(f); \
    test_l1<dtype, 24960, num_iters>(f); \
    test_l1<dtype, 24968, num_iters>(f); \
    test_l1<dtype, 24976, num_iters>(f); \
    test_l1<dtype, 24984, num_iters>(f); \
    test_l1<dtype, 24992, num_iters>(f); \
    test_l1<dtype, 25000, num_iters>(f); \
    test_l1<dtype, 25008, num_iters>(f); \
    test_l1<dtype, 25016, num_iters>(f); \
    test_l1<dtype, 25024, num_iters>(f); \
    test_l1<dtype, 25032, num_iters>(f); \
    test_l1<dtype, 25040, num_iters>(f); \
    test_l1<dtype, 25048, num_iters>(f); \
    test_l1<dtype, 25056, num_iters>(f); \
    test_l1<dtype, 25064, num_iters>(f); \
    test_l1<dtype, 25072, num_iters>(f); \
    test_l1<dtype, 25080, num_iters>(f); \
    test_l1<dtype, 25088, num_iters>(f); \
    test_l1<dtype, 25096, num_iters>(f); \
    test_l1<dtype, 25104, num_iters>(f); \
    test_l1<dtype, 25112, num_iters>(f); \
    test_l1<dtype, 25120, num_iters>(f); \
    test_l1<dtype, 25128, num_iters>(f); \
    test_l1<dtype, 25136, num_iters>(f); \
    test_l1<dtype, 25144, num_iters>(f); \
    test_l1<dtype, 25152, num_iters>(f); \
    test_l1<dtype, 25160, num_iters>(f); \
    test_l1<dtype, 25168, num_iters>(f); \
    test_l1<dtype, 25176, num_iters>(f); \
    test_l1<dtype, 25184, num_iters>(f); \
    test_l1<dtype, 25192, num_iters>(f); \
    test_l1<dtype, 25200, num_iters>(f); \
    test_l1<dtype, 25208, num_iters>(f); \
    test_l1<dtype, 25216, num_iters>(f); \
    test_l1<dtype, 25224, num_iters>(f); \
    test_l1<dtype, 25232, num_iters>(f); \
    test_l1<dtype, 25240, num_iters>(f); \
    test_l1<dtype, 25248, num_iters>(f); \
    test_l1<dtype, 25256, num_iters>(f); \
    test_l1<dtype, 25264, num_iters>(f); \
    test_l1<dtype, 25272, num_iters>(f); \
    test_l1<dtype, 25280, num_iters>(f); \
    test_l1<dtype, 25288, num_iters>(f); \
    test_l1<dtype, 25296, num_iters>(f); \
    test_l1<dtype, 25304, num_iters>(f); \
    test_l1<dtype, 25312, num_iters>(f); \
    test_l1<dtype, 25320, num_iters>(f); \
    test_l1<dtype, 25328, num_iters>(f); \
    test_l1<dtype, 25336, num_iters>(f); \
    test_l1<dtype, 25344, num_iters>(f); \
    test_l1<dtype, 25352, num_iters>(f); \
    test_l1<dtype, 25360, num_iters>(f); \
    test_l1<dtype, 25368, num_iters>(f); \
    test_l1<dtype, 25376, num_iters>(f); \
    test_l1<dtype, 25384, num_iters>(f); \
    test_l1<dtype, 25392, num_iters>(f); \
    test_l1<dtype, 25400, num_iters>(f); \
    test_l1<dtype, 25408, num_iters>(f); \
    test_l1<dtype, 25416, num_iters>(f); \
    test_l1<dtype, 25424, num_iters>(f); \
    test_l1<dtype, 25432, num_iters>(f); \
    test_l1<dtype, 25440, num_iters>(f); \
    test_l1<dtype, 25448, num_iters>(f); \
    test_l1<dtype, 25456, num_iters>(f); \
    test_l1<dtype, 25464, num_iters>(f); \
    test_l1<dtype, 25472, num_iters>(f); \
    test_l1<dtype, 25480, num_iters>(f); \
    test_l1<dtype, 25488, num_iters>(f); \
    test_l1<dtype, 25496, num_iters>(f); \
    test_l1<dtype, 25504, num_iters>(f); \
    test_l1<dtype, 25512, num_iters>(f); \
    test_l1<dtype, 25520, num_iters>(f); \
    test_l1<dtype, 25528, num_iters>(f); \
    test_l1<dtype, 25536, num_iters>(f); \
    test_l1<dtype, 25544, num_iters>(f); \
    test_l1<dtype, 25552, num_iters>(f); \
    test_l1<dtype, 25560, num_iters>(f); \
    test_l1<dtype, 25568, num_iters>(f); \
    test_l1<dtype, 25576, num_iters>(f); \
    test_l1<dtype, 25584, num_iters>(f); \
    test_l1<dtype, 25592, num_iters>(f); \
    test_l1<dtype, 25600, num_iters>(f); \
    test_l1<dtype, 25608, num_iters>(f); \
    test_l1<dtype, 25616, num_iters>(f); \
    test_l1<dtype, 25624, num_iters>(f); \
    test_l1<dtype, 25632, num_iters>(f); \
    test_l1<dtype, 25640, num_iters>(f); \
    test_l1<dtype, 25648, num_iters>(f); \
    test_l1<dtype, 25656, num_iters>(f); \
    test_l1<dtype, 25664, num_iters>(f); \
    test_l1<dtype, 25672, num_iters>(f); \
    test_l1<dtype, 25680, num_iters>(f); \
    test_l1<dtype, 25688, num_iters>(f); \
    test_l1<dtype, 25696, num_iters>(f); \
    test_l1<dtype, 25704, num_iters>(f); \
    test_l1<dtype, 25712, num_iters>(f); \
    test_l1<dtype, 25720, num_iters>(f); \
    test_l1<dtype, 25728, num_iters>(f); \
    test_l1<dtype, 25736, num_iters>(f); \
    test_l1<dtype, 25744, num_iters>(f); \
    test_l1<dtype, 25752, num_iters>(f); \
    test_l1<dtype, 25760, num_iters>(f); \
    test_l1<dtype, 25768, num_iters>(f); \
    test_l1<dtype, 25776, num_iters>(f); \
    test_l1<dtype, 25784, num_iters>(f); \
    test_l1<dtype, 25792, num_iters>(f); \
    test_l1<dtype, 25800, num_iters>(f); \
    test_l1<dtype, 25808, num_iters>(f); \
    test_l1<dtype, 25816, num_iters>(f); \
    test_l1<dtype, 25824, num_iters>(f); \
    test_l1<dtype, 25832, num_iters>(f); \
    test_l1<dtype, 25840, num_iters>(f); \
    test_l1<dtype, 25848, num_iters>(f); \
    test_l1<dtype, 25856, num_iters>(f); \
    test_l1<dtype, 25864, num_iters>(f); \
    test_l1<dtype, 25872, num_iters>(f); \
    test_l1<dtype, 25880, num_iters>(f); \
    test_l1<dtype, 25888, num_iters>(f); \
    test_l1<dtype, 25896, num_iters>(f); \
    test_l1<dtype, 25904, num_iters>(f); \
    test_l1<dtype, 25912, num_iters>(f); \
    test_l1<dtype, 25920, num_iters>(f); \
    test_l1<dtype, 25928, num_iters>(f); \
    test_l1<dtype, 25936, num_iters>(f); \
    test_l1<dtype, 25944, num_iters>(f); \
    test_l1<dtype, 25952, num_iters>(f); \
    test_l1<dtype, 25960, num_iters>(f); \
    test_l1<dtype, 25968, num_iters>(f); \
    test_l1<dtype, 25976, num_iters>(f); \
    test_l1<dtype, 25984, num_iters>(f); \
    test_l1<dtype, 25992, num_iters>(f); \
    test_l1<dtype, 26000, num_iters>(f); \
    test_l1<dtype, 26008, num_iters>(f); \
    test_l1<dtype, 26016, num_iters>(f); \
    test_l1<dtype, 26024, num_iters>(f); \
    test_l1<dtype, 26032, num_iters>(f); \
    test_l1<dtype, 26040, num_iters>(f); \
    test_l1<dtype, 26048, num_iters>(f); \
    test_l1<dtype, 26056, num_iters>(f); \
    test_l1<dtype, 26064, num_iters>(f); \
    test_l1<dtype, 26072, num_iters>(f); \
    test_l1<dtype, 26080, num_iters>(f); \
    test_l1<dtype, 26088, num_iters>(f); \
    test_l1<dtype, 26096, num_iters>(f); \
    test_l1<dtype, 26104, num_iters>(f); \
    test_l1<dtype, 26112, num_iters>(f); \
    test_l1<dtype, 26120, num_iters>(f); \
    test_l1<dtype, 26128, num_iters>(f); \
    test_l1<dtype, 26136, num_iters>(f); \
    test_l1<dtype, 26144, num_iters>(f); \
    test_l1<dtype, 26152, num_iters>(f); \
    test_l1<dtype, 26160, num_iters>(f); \
    test_l1<dtype, 26168, num_iters>(f); \
    test_l1<dtype, 26176, num_iters>(f); \
    test_l1<dtype, 26184, num_iters>(f); \
    test_l1<dtype, 26192, num_iters>(f); \
    test_l1<dtype, 26200, num_iters>(f); \
    test_l1<dtype, 26208, num_iters>(f); \
    test_l1<dtype, 26216, num_iters>(f); \
    test_l1<dtype, 26224, num_iters>(f); \
    test_l1<dtype, 26232, num_iters>(f); \
    test_l1<dtype, 26240, num_iters>(f); \
    test_l1<dtype, 26248, num_iters>(f); \
    test_l1<dtype, 26256, num_iters>(f); \
    test_l1<dtype, 26264, num_iters>(f); \
    test_l1<dtype, 26272, num_iters>(f); \
    test_l1<dtype, 26280, num_iters>(f); \
    test_l1<dtype, 26288, num_iters>(f); \
    test_l1<dtype, 26296, num_iters>(f); \
    test_l1<dtype, 26304, num_iters>(f); \
    test_l1<dtype, 26312, num_iters>(f); \
    test_l1<dtype, 26320, num_iters>(f); \
    test_l1<dtype, 26328, num_iters>(f); \
    test_l1<dtype, 26336, num_iters>(f); \
    test_l1<dtype, 26344, num_iters>(f); \
    test_l1<dtype, 26352, num_iters>(f); \
    test_l1<dtype, 26360, num_iters>(f); \
    test_l1<dtype, 26368, num_iters>(f); \
    test_l1<dtype, 26376, num_iters>(f); \
    test_l1<dtype, 26384, num_iters>(f); \
    test_l1<dtype, 26392, num_iters>(f); \
    test_l1<dtype, 26400, num_iters>(f); \
    test_l1<dtype, 26408, num_iters>(f); \
    test_l1<dtype, 26416, num_iters>(f); \
    test_l1<dtype, 26424, num_iters>(f); \
    test_l1<dtype, 26432, num_iters>(f); \
    test_l1<dtype, 26440, num_iters>(f); \
    test_l1<dtype, 26448, num_iters>(f); \
    test_l1<dtype, 26456, num_iters>(f); \
    test_l1<dtype, 26464, num_iters>(f); \
    test_l1<dtype, 26472, num_iters>(f); \
    test_l1<dtype, 26480, num_iters>(f); \
    test_l1<dtype, 26488, num_iters>(f); \
    test_l1<dtype, 26496, num_iters>(f); \
    test_l1<dtype, 26504, num_iters>(f); \
    test_l1<dtype, 26512, num_iters>(f); \
    test_l1<dtype, 26520, num_iters>(f); \
    test_l1<dtype, 26528, num_iters>(f); \
    test_l1<dtype, 26536, num_iters>(f); \
    test_l1<dtype, 26544, num_iters>(f); \
    test_l1<dtype, 26552, num_iters>(f); \
    test_l1<dtype, 26560, num_iters>(f); \
    test_l1<dtype, 26568, num_iters>(f); \
    test_l1<dtype, 26576, num_iters>(f); \
    test_l1<dtype, 26584, num_iters>(f); \
    test_l1<dtype, 26592, num_iters>(f); \
    test_l1<dtype, 26600, num_iters>(f); \
    test_l1<dtype, 26608, num_iters>(f); \
    test_l1<dtype, 26616, num_iters>(f); \
    test_l1<dtype, 26624, num_iters>(f); \
    test_l1<dtype, 26632, num_iters>(f); \
    test_l1<dtype, 26640, num_iters>(f); \
    test_l1<dtype, 26648, num_iters>(f); \
    test_l1<dtype, 26656, num_iters>(f); \
    test_l1<dtype, 26664, num_iters>(f); \
    test_l1<dtype, 26672, num_iters>(f); \
    test_l1<dtype, 26680, num_iters>(f); \
    test_l1<dtype, 26688, num_iters>(f); \
    test_l1<dtype, 26696, num_iters>(f); \
    test_l1<dtype, 26704, num_iters>(f); \
    test_l1<dtype, 26712, num_iters>(f); \
    test_l1<dtype, 26720, num_iters>(f); \
    test_l1<dtype, 26728, num_iters>(f); \
    test_l1<dtype, 26736, num_iters>(f); \
    test_l1<dtype, 26744, num_iters>(f); \
    test_l1<dtype, 26752, num_iters>(f); \
    test_l1<dtype, 26760, num_iters>(f); \
    test_l1<dtype, 26768, num_iters>(f); \
    test_l1<dtype, 26776, num_iters>(f); \
    test_l1<dtype, 26784, num_iters>(f); \
    test_l1<dtype, 26792, num_iters>(f); \
    test_l1<dtype, 26800, num_iters>(f); \
    test_l1<dtype, 26808, num_iters>(f); \
    test_l1<dtype, 26816, num_iters>(f); \
    test_l1<dtype, 26824, num_iters>(f); \
    test_l1<dtype, 26832, num_iters>(f); \
    test_l1<dtype, 26840, num_iters>(f); \
    test_l1<dtype, 26848, num_iters>(f); \
    test_l1<dtype, 26856, num_iters>(f); \
    test_l1<dtype, 26864, num_iters>(f); \
    test_l1<dtype, 26872, num_iters>(f); \
    test_l1<dtype, 26880, num_iters>(f); \
    test_l1<dtype, 26888, num_iters>(f); \
    test_l1<dtype, 26896, num_iters>(f); \
    test_l1<dtype, 26904, num_iters>(f); \
    test_l1<dtype, 26912, num_iters>(f); \
    test_l1<dtype, 26920, num_iters>(f); \
    test_l1<dtype, 26928, num_iters>(f); \
    test_l1<dtype, 26936, num_iters>(f); \
    test_l1<dtype, 26944, num_iters>(f); \
    test_l1<dtype, 26952, num_iters>(f); \
    test_l1<dtype, 26960, num_iters>(f); \
    test_l1<dtype, 26968, num_iters>(f); \
    test_l1<dtype, 26976, num_iters>(f); \
    test_l1<dtype, 26984, num_iters>(f); \
    test_l1<dtype, 26992, num_iters>(f); \
    test_l1<dtype, 27000, num_iters>(f); \
    test_l1<dtype, 27008, num_iters>(f); \
    test_l1<dtype, 27016, num_iters>(f); \
    test_l1<dtype, 27024, num_iters>(f); \
    test_l1<dtype, 27032, num_iters>(f); \
    test_l1<dtype, 27040, num_iters>(f); \
    test_l1<dtype, 27048, num_iters>(f); \
    test_l1<dtype, 27056, num_iters>(f); \
    test_l1<dtype, 27064, num_iters>(f); \
    test_l1<dtype, 27072, num_iters>(f); \
    test_l1<dtype, 27080, num_iters>(f); \
    test_l1<dtype, 27088, num_iters>(f); \
    test_l1<dtype, 27096, num_iters>(f); \
    test_l1<dtype, 27104, num_iters>(f); \
    test_l1<dtype, 27112, num_iters>(f); \
    test_l1<dtype, 27120, num_iters>(f); \
    test_l1<dtype, 27128, num_iters>(f); \
    test_l1<dtype, 27136, num_iters>(f); \
    test_l1<dtype, 27144, num_iters>(f); \
    test_l1<dtype, 27152, num_iters>(f); \
    test_l1<dtype, 27160, num_iters>(f); \
    test_l1<dtype, 27168, num_iters>(f); \
    test_l1<dtype, 27176, num_iters>(f); \
    test_l1<dtype, 27184, num_iters>(f); \
    test_l1<dtype, 27192, num_iters>(f); \
    test_l1<dtype, 27200, num_iters>(f); \
    test_l1<dtype, 27208, num_iters>(f); \
    test_l1<dtype, 27216, num_iters>(f); \
    test_l1<dtype, 27224, num_iters>(f); \
    test_l1<dtype, 27232, num_iters>(f); \
    test_l1<dtype, 27240, num_iters>(f); \
    test_l1<dtype, 27248, num_iters>(f); \
    test_l1<dtype, 27256, num_iters>(f); \
    test_l1<dtype, 27264, num_iters>(f); \
    test_l1<dtype, 27272, num_iters>(f); \
    test_l1<dtype, 27280, num_iters>(f); \
    test_l1<dtype, 27288, num_iters>(f); \
    test_l1<dtype, 27296, num_iters>(f); \
    test_l1<dtype, 27304, num_iters>(f); \
    test_l1<dtype, 27312, num_iters>(f); \
    test_l1<dtype, 27320, num_iters>(f); \
    test_l1<dtype, 27328, num_iters>(f); \
    test_l1<dtype, 27336, num_iters>(f); \
    test_l1<dtype, 27344, num_iters>(f); \
    test_l1<dtype, 27352, num_iters>(f); \
    test_l1<dtype, 27360, num_iters>(f); \
    test_l1<dtype, 27368, num_iters>(f); \
    test_l1<dtype, 27376, num_iters>(f); \
    test_l1<dtype, 27384, num_iters>(f); \
    test_l1<dtype, 27392, num_iters>(f); \
    test_l1<dtype, 27400, num_iters>(f); \
    test_l1<dtype, 27408, num_iters>(f); \
    test_l1<dtype, 27416, num_iters>(f); \
    test_l1<dtype, 27424, num_iters>(f); \
    test_l1<dtype, 27432, num_iters>(f); \
    test_l1<dtype, 27440, num_iters>(f); \
    test_l1<dtype, 27448, num_iters>(f); \
    test_l1<dtype, 27456, num_iters>(f); \
    test_l1<dtype, 27464, num_iters>(f); \
    test_l1<dtype, 27472, num_iters>(f); \
    test_l1<dtype, 27480, num_iters>(f); \
    test_l1<dtype, 27488, num_iters>(f); \
    test_l1<dtype, 27496, num_iters>(f); \
    test_l1<dtype, 27504, num_iters>(f); \
    test_l1<dtype, 27512, num_iters>(f); \
    test_l1<dtype, 27520, num_iters>(f); \
    test_l1<dtype, 27528, num_iters>(f); \
    test_l1<dtype, 27536, num_iters>(f); \
    test_l1<dtype, 27544, num_iters>(f); \
    test_l1<dtype, 27552, num_iters>(f); \
    test_l1<dtype, 27560, num_iters>(f); \
    test_l1<dtype, 27568, num_iters>(f); \
    test_l1<dtype, 27576, num_iters>(f); \
    test_l1<dtype, 27584, num_iters>(f); \
    test_l1<dtype, 27592, num_iters>(f); \
    test_l1<dtype, 27600, num_iters>(f); \
    test_l1<dtype, 27608, num_iters>(f); \
    test_l1<dtype, 27616, num_iters>(f); \
    test_l1<dtype, 27624, num_iters>(f); \
    test_l1<dtype, 27632, num_iters>(f); \
    test_l1<dtype, 27640, num_iters>(f); \
    test_l1<dtype, 27648, num_iters>(f); \
    test_l1<dtype, 27656, num_iters>(f); \
    test_l1<dtype, 27664, num_iters>(f); \
    test_l1<dtype, 27672, num_iters>(f); \
    test_l1<dtype, 27680, num_iters>(f); \
    test_l1<dtype, 27688, num_iters>(f); \
    test_l1<dtype, 27696, num_iters>(f); \
    test_l1<dtype, 27704, num_iters>(f); \
    test_l1<dtype, 27712, num_iters>(f); \
    test_l1<dtype, 27720, num_iters>(f); \
    test_l1<dtype, 27728, num_iters>(f); \
    test_l1<dtype, 27736, num_iters>(f); \
    test_l1<dtype, 27744, num_iters>(f); \
    test_l1<dtype, 27752, num_iters>(f); \
    test_l1<dtype, 27760, num_iters>(f); \
    test_l1<dtype, 27768, num_iters>(f); \
    test_l1<dtype, 27776, num_iters>(f); \
    test_l1<dtype, 27784, num_iters>(f); \
    test_l1<dtype, 27792, num_iters>(f); \
    test_l1<dtype, 27800, num_iters>(f); \
    test_l1<dtype, 27808, num_iters>(f); \
    test_l1<dtype, 27816, num_iters>(f); \
    test_l1<dtype, 27824, num_iters>(f); \
    test_l1<dtype, 27832, num_iters>(f); \
    test_l1<dtype, 27840, num_iters>(f); \
    test_l1<dtype, 27848, num_iters>(f); \
    test_l1<dtype, 27856, num_iters>(f); \
    test_l1<dtype, 27864, num_iters>(f); \
    test_l1<dtype, 27872, num_iters>(f); \
    test_l1<dtype, 27880, num_iters>(f); \
    test_l1<dtype, 27888, num_iters>(f); \
    test_l1<dtype, 27896, num_iters>(f); \
    test_l1<dtype, 27904, num_iters>(f); \
    test_l1<dtype, 27912, num_iters>(f); \
    test_l1<dtype, 27920, num_iters>(f); \
    test_l1<dtype, 27928, num_iters>(f); \
    test_l1<dtype, 27936, num_iters>(f); \
    test_l1<dtype, 27944, num_iters>(f); \
    test_l1<dtype, 27952, num_iters>(f); \
    test_l1<dtype, 27960, num_iters>(f); \
    test_l1<dtype, 27968, num_iters>(f); \
    test_l1<dtype, 27976, num_iters>(f); \
    test_l1<dtype, 27984, num_iters>(f); \
    test_l1<dtype, 27992, num_iters>(f); \
    test_l1<dtype, 28000, num_iters>(f); \
    test_l1<dtype, 28008, num_iters>(f); \
    test_l1<dtype, 28016, num_iters>(f); \
    test_l1<dtype, 28024, num_iters>(f); \
    test_l1<dtype, 28032, num_iters>(f); \
    test_l1<dtype, 28040, num_iters>(f); \
    test_l1<dtype, 28048, num_iters>(f); \
    test_l1<dtype, 28056, num_iters>(f); \
    test_l1<dtype, 28064, num_iters>(f); \
    test_l1<dtype, 28072, num_iters>(f); \
    test_l1<dtype, 28080, num_iters>(f); \
    test_l1<dtype, 28088, num_iters>(f); \
    test_l1<dtype, 28096, num_iters>(f); \
    test_l1<dtype, 28104, num_iters>(f); \
    test_l1<dtype, 28112, num_iters>(f); \
    test_l1<dtype, 28120, num_iters>(f); \
    test_l1<dtype, 28128, num_iters>(f); \
    test_l1<dtype, 28136, num_iters>(f); \
    test_l1<dtype, 28144, num_iters>(f); \
    test_l1<dtype, 28152, num_iters>(f); \
    test_l1<dtype, 28160, num_iters>(f); \
    test_l1<dtype, 28168, num_iters>(f); \
    test_l1<dtype, 28176, num_iters>(f); \
    test_l1<dtype, 28184, num_iters>(f); \
    test_l1<dtype, 28192, num_iters>(f); \
    test_l1<dtype, 28200, num_iters>(f); \
    test_l1<dtype, 28208, num_iters>(f); \
    test_l1<dtype, 28216, num_iters>(f); \
    test_l1<dtype, 28224, num_iters>(f); \
    test_l1<dtype, 28232, num_iters>(f); \
    test_l1<dtype, 28240, num_iters>(f); \
    test_l1<dtype, 28248, num_iters>(f); \
    test_l1<dtype, 28256, num_iters>(f); \
    test_l1<dtype, 28264, num_iters>(f); \
    test_l1<dtype, 28272, num_iters>(f); \
    test_l1<dtype, 28280, num_iters>(f); \
    test_l1<dtype, 28288, num_iters>(f); \
    test_l1<dtype, 28296, num_iters>(f); \
    test_l1<dtype, 28304, num_iters>(f); \
    test_l1<dtype, 28312, num_iters>(f); \
    test_l1<dtype, 28320, num_iters>(f); \
    test_l1<dtype, 28328, num_iters>(f); \
    test_l1<dtype, 28336, num_iters>(f); \
    test_l1<dtype, 28344, num_iters>(f); \
    test_l1<dtype, 28352, num_iters>(f); \
    test_l1<dtype, 28360, num_iters>(f); \
    test_l1<dtype, 28368, num_iters>(f); \
    test_l1<dtype, 28376, num_iters>(f); \
    test_l1<dtype, 28384, num_iters>(f); \
    test_l1<dtype, 28392, num_iters>(f); \
    test_l1<dtype, 28400, num_iters>(f); \
    test_l1<dtype, 28408, num_iters>(f); \
    test_l1<dtype, 28416, num_iters>(f); \
    test_l1<dtype, 28424, num_iters>(f); \
    test_l1<dtype, 28432, num_iters>(f); \
    test_l1<dtype, 28440, num_iters>(f); \
    test_l1<dtype, 28448, num_iters>(f); \
    test_l1<dtype, 28456, num_iters>(f); \
    test_l1<dtype, 28464, num_iters>(f); \
    test_l1<dtype, 28472, num_iters>(f); \
    test_l1<dtype, 28480, num_iters>(f); \
    test_l1<dtype, 28488, num_iters>(f); \
    test_l1<dtype, 28496, num_iters>(f); \
    test_l1<dtype, 28504, num_iters>(f); \
    test_l1<dtype, 28512, num_iters>(f); \
    test_l1<dtype, 28520, num_iters>(f); \
    test_l1<dtype, 28528, num_iters>(f); \
    test_l1<dtype, 28536, num_iters>(f); \
    test_l1<dtype, 28544, num_iters>(f); \
    test_l1<dtype, 28552, num_iters>(f); \
    test_l1<dtype, 28560, num_iters>(f); \
    test_l1<dtype, 28568, num_iters>(f); \
    test_l1<dtype, 28576, num_iters>(f); \
    test_l1<dtype, 28584, num_iters>(f); \
    test_l1<dtype, 28592, num_iters>(f); \
    test_l1<dtype, 28600, num_iters>(f); \
    test_l1<dtype, 28608, num_iters>(f); \
    test_l1<dtype, 28616, num_iters>(f); \
    test_l1<dtype, 28624, num_iters>(f); \
    test_l1<dtype, 28632, num_iters>(f); \
    test_l1<dtype, 28640, num_iters>(f); \
    test_l1<dtype, 28648, num_iters>(f); \
    test_l1<dtype, 28656, num_iters>(f); \
    test_l1<dtype, 28664, num_iters>(f); \
    test_l1<dtype, 28672, num_iters>(f); \
    test_l1<dtype, 28680, num_iters>(f); \
    test_l1<dtype, 28688, num_iters>(f); \
    test_l1<dtype, 28696, num_iters>(f); \
    test_l1<dtype, 28704, num_iters>(f); \
    test_l1<dtype, 28712, num_iters>(f); \
    test_l1<dtype, 28720, num_iters>(f); \
    test_l1<dtype, 28728, num_iters>(f); \
    test_l1<dtype, 28736, num_iters>(f); \
    test_l1<dtype, 28744, num_iters>(f); \
    test_l1<dtype, 28752, num_iters>(f); \
    test_l1<dtype, 28760, num_iters>(f); \
    test_l1<dtype, 28768, num_iters>(f); \
    test_l1<dtype, 28776, num_iters>(f); \
    test_l1<dtype, 28784, num_iters>(f); \
    test_l1<dtype, 28792, num_iters>(f); \
    test_l1<dtype, 28800, num_iters>(f); \
    test_l1<dtype, 28808, num_iters>(f); \
    test_l1<dtype, 28816, num_iters>(f); \
    test_l1<dtype, 28824, num_iters>(f); \
    test_l1<dtype, 28832, num_iters>(f); \
    test_l1<dtype, 28840, num_iters>(f); \
    test_l1<dtype, 28848, num_iters>(f); \
    test_l1<dtype, 28856, num_iters>(f); \
    test_l1<dtype, 28864, num_iters>(f); \
    test_l1<dtype, 28872, num_iters>(f); \
    test_l1<dtype, 28880, num_iters>(f); \
    test_l1<dtype, 28888, num_iters>(f); \
    test_l1<dtype, 28896, num_iters>(f); \
    test_l1<dtype, 28904, num_iters>(f); \
    test_l1<dtype, 28912, num_iters>(f); \
    test_l1<dtype, 28920, num_iters>(f); \
    test_l1<dtype, 28928, num_iters>(f); \
    test_l1<dtype, 28936, num_iters>(f); \
    test_l1<dtype, 28944, num_iters>(f); \
    test_l1<dtype, 28952, num_iters>(f); \
    test_l1<dtype, 28960, num_iters>(f); \
    test_l1<dtype, 28968, num_iters>(f); \
    test_l1<dtype, 28976, num_iters>(f); \
    test_l1<dtype, 28984, num_iters>(f); \
    test_l1<dtype, 28992, num_iters>(f); \
    test_l1<dtype, 29000, num_iters>(f); \
    test_l1<dtype, 29008, num_iters>(f); \
    test_l1<dtype, 29016, num_iters>(f); \
    test_l1<dtype, 29024, num_iters>(f); \
    test_l1<dtype, 29032, num_iters>(f); \
    test_l1<dtype, 29040, num_iters>(f); \
    test_l1<dtype, 29048, num_iters>(f); \
    test_l1<dtype, 29056, num_iters>(f); \
    test_l1<dtype, 29064, num_iters>(f); \
    test_l1<dtype, 29072, num_iters>(f); \
    test_l1<dtype, 29080, num_iters>(f); \
    test_l1<dtype, 29088, num_iters>(f); \
    test_l1<dtype, 29096, num_iters>(f); \
    test_l1<dtype, 29104, num_iters>(f); \
    test_l1<dtype, 29112, num_iters>(f); \
    test_l1<dtype, 29120, num_iters>(f); \
    test_l1<dtype, 29128, num_iters>(f); \
    test_l1<dtype, 29136, num_iters>(f); \
    test_l1<dtype, 29144, num_iters>(f); \
    test_l1<dtype, 29152, num_iters>(f); \
    test_l1<dtype, 29160, num_iters>(f); \
    test_l1<dtype, 29168, num_iters>(f); \
    test_l1<dtype, 29176, num_iters>(f); \
    test_l1<dtype, 29184, num_iters>(f); \
    test_l1<dtype, 29192, num_iters>(f); \
    test_l1<dtype, 29200, num_iters>(f); \
    test_l1<dtype, 29208, num_iters>(f); \
    test_l1<dtype, 29216, num_iters>(f); \
    test_l1<dtype, 29224, num_iters>(f); \
    test_l1<dtype, 29232, num_iters>(f); \
    test_l1<dtype, 29240, num_iters>(f); \
    test_l1<dtype, 29248, num_iters>(f); \
    test_l1<dtype, 29256, num_iters>(f); \
    test_l1<dtype, 29264, num_iters>(f); \
    test_l1<dtype, 29272, num_iters>(f); \
    test_l1<dtype, 29280, num_iters>(f); \
    test_l1<dtype, 29288, num_iters>(f); \
    test_l1<dtype, 29296, num_iters>(f); \
    test_l1<dtype, 29304, num_iters>(f); \
    test_l1<dtype, 29312, num_iters>(f); \
    test_l1<dtype, 29320, num_iters>(f); \
    test_l1<dtype, 29328, num_iters>(f); \
    test_l1<dtype, 29336, num_iters>(f); \
    test_l1<dtype, 29344, num_iters>(f); \
    test_l1<dtype, 29352, num_iters>(f); \
    test_l1<dtype, 29360, num_iters>(f); \
    test_l1<dtype, 29368, num_iters>(f); \
    test_l1<dtype, 29376, num_iters>(f); \
    test_l1<dtype, 29384, num_iters>(f); \
    test_l1<dtype, 29392, num_iters>(f); \
    test_l1<dtype, 29400, num_iters>(f); \
    test_l1<dtype, 29408, num_iters>(f); \
    test_l1<dtype, 29416, num_iters>(f); \
    test_l1<dtype, 29424, num_iters>(f); \
    test_l1<dtype, 29432, num_iters>(f); \
    test_l1<dtype, 29440, num_iters>(f); \
    test_l1<dtype, 29448, num_iters>(f); \
    test_l1<dtype, 29456, num_iters>(f); \
    test_l1<dtype, 29464, num_iters>(f); \
    test_l1<dtype, 29472, num_iters>(f); \
    test_l1<dtype, 29480, num_iters>(f); \
    test_l1<dtype, 29488, num_iters>(f); \
    test_l1<dtype, 29496, num_iters>(f); \
    test_l1<dtype, 29504, num_iters>(f); \
    test_l1<dtype, 29512, num_iters>(f); \
    test_l1<dtype, 29520, num_iters>(f); \
    test_l1<dtype, 29528, num_iters>(f); \
    test_l1<dtype, 29536, num_iters>(f); \
    test_l1<dtype, 29544, num_iters>(f); \
    test_l1<dtype, 29552, num_iters>(f); \
    test_l1<dtype, 29560, num_iters>(f); \
    test_l1<dtype, 29568, num_iters>(f); \
    test_l1<dtype, 29576, num_iters>(f); \
    test_l1<dtype, 29584, num_iters>(f); \
    test_l1<dtype, 29592, num_iters>(f); \
    test_l1<dtype, 29600, num_iters>(f); \
    test_l1<dtype, 29608, num_iters>(f); \
    test_l1<dtype, 29616, num_iters>(f); \
    test_l1<dtype, 29624, num_iters>(f); \
    test_l1<dtype, 29632, num_iters>(f); \
    test_l1<dtype, 29640, num_iters>(f); \
    test_l1<dtype, 29648, num_iters>(f); \
    test_l1<dtype, 29656, num_iters>(f); \
    test_l1<dtype, 29664, num_iters>(f); \
    test_l1<dtype, 29672, num_iters>(f); \
    test_l1<dtype, 29680, num_iters>(f); \
    test_l1<dtype, 29688, num_iters>(f); \
    test_l1<dtype, 29696, num_iters>(f); \
    test_l1<dtype, 29704, num_iters>(f); \
    test_l1<dtype, 29712, num_iters>(f); \
    test_l1<dtype, 29720, num_iters>(f); \
    test_l1<dtype, 29728, num_iters>(f); \
    test_l1<dtype, 29736, num_iters>(f); \
    test_l1<dtype, 29744, num_iters>(f); \
    test_l1<dtype, 29752, num_iters>(f); \
    test_l1<dtype, 29760, num_iters>(f); \
    test_l1<dtype, 29768, num_iters>(f); \
    test_l1<dtype, 29776, num_iters>(f); \
    test_l1<dtype, 29784, num_iters>(f); \
    test_l1<dtype, 29792, num_iters>(f); \
    test_l1<dtype, 29800, num_iters>(f); \
    test_l1<dtype, 29808, num_iters>(f); \
    test_l1<dtype, 29816, num_iters>(f); \
    test_l1<dtype, 29824, num_iters>(f); \
    test_l1<dtype, 29832, num_iters>(f); \
    test_l1<dtype, 29840, num_iters>(f); \
    test_l1<dtype, 29848, num_iters>(f); \
    test_l1<dtype, 29856, num_iters>(f); \
    test_l1<dtype, 29864, num_iters>(f); \
    test_l1<dtype, 29872, num_iters>(f); \
    test_l1<dtype, 29880, num_iters>(f); \
    test_l1<dtype, 29888, num_iters>(f); \
    test_l1<dtype, 29896, num_iters>(f); \
    test_l1<dtype, 29904, num_iters>(f); \
    test_l1<dtype, 29912, num_iters>(f); \
    test_l1<dtype, 29920, num_iters>(f); \
    test_l1<dtype, 29928, num_iters>(f); \
    test_l1<dtype, 29936, num_iters>(f); \
    test_l1<dtype, 29944, num_iters>(f); \
    test_l1<dtype, 29952, num_iters>(f); \
    test_l1<dtype, 29960, num_iters>(f); \
    test_l1<dtype, 29968, num_iters>(f); \
    test_l1<dtype, 29976, num_iters>(f); \
    test_l1<dtype, 29984, num_iters>(f); \
    test_l1<dtype, 29992, num_iters>(f); \
    test_l1<dtype, 30000, num_iters>(f); \
    test_l1<dtype, 30008, num_iters>(f); \
    test_l1<dtype, 30016, num_iters>(f); \
    test_l1<dtype, 30024, num_iters>(f); \
    test_l1<dtype, 30032, num_iters>(f); \
    test_l1<dtype, 30040, num_iters>(f); \
    test_l1<dtype, 30048, num_iters>(f); \
    test_l1<dtype, 30056, num_iters>(f); \
    test_l1<dtype, 30064, num_iters>(f); \
    test_l1<dtype, 30072, num_iters>(f); \
    test_l1<dtype, 30080, num_iters>(f); \
    test_l1<dtype, 30088, num_iters>(f); \
    test_l1<dtype, 30096, num_iters>(f); \
    test_l1<dtype, 30104, num_iters>(f); \
    test_l1<dtype, 30112, num_iters>(f); \
    test_l1<dtype, 30120, num_iters>(f); \
    test_l1<dtype, 30128, num_iters>(f); \
    test_l1<dtype, 30136, num_iters>(f); \
    test_l1<dtype, 30144, num_iters>(f); \
    test_l1<dtype, 30152, num_iters>(f); \
    test_l1<dtype, 30160, num_iters>(f); \
    test_l1<dtype, 30168, num_iters>(f); \
    test_l1<dtype, 30176, num_iters>(f); \
    test_l1<dtype, 30184, num_iters>(f); \
    test_l1<dtype, 30192, num_iters>(f); \
    test_l1<dtype, 30200, num_iters>(f); \
    test_l1<dtype, 30208, num_iters>(f); \
    test_l1<dtype, 30216, num_iters>(f); \
    test_l1<dtype, 30224, num_iters>(f); \
    test_l1<dtype, 30232, num_iters>(f); \
    test_l1<dtype, 30240, num_iters>(f); \
    test_l1<dtype, 30248, num_iters>(f); \
    test_l1<dtype, 30256, num_iters>(f); \
    test_l1<dtype, 30264, num_iters>(f); \
    test_l1<dtype, 30272, num_iters>(f); \
    test_l1<dtype, 30280, num_iters>(f); \
    test_l1<dtype, 30288, num_iters>(f); \
    test_l1<dtype, 30296, num_iters>(f); \
    test_l1<dtype, 30304, num_iters>(f); \
    test_l1<dtype, 30312, num_iters>(f); \
    test_l1<dtype, 30320, num_iters>(f); \
    test_l1<dtype, 30328, num_iters>(f); \
    test_l1<dtype, 30336, num_iters>(f); \
    test_l1<dtype, 30344, num_iters>(f); \
    test_l1<dtype, 30352, num_iters>(f); \
    test_l1<dtype, 30360, num_iters>(f); \
    test_l1<dtype, 30368, num_iters>(f); \
    test_l1<dtype, 30376, num_iters>(f); \
    test_l1<dtype, 30384, num_iters>(f); \
    test_l1<dtype, 30392, num_iters>(f); \
    test_l1<dtype, 30400, num_iters>(f); \
    test_l1<dtype, 30408, num_iters>(f); \
    test_l1<dtype, 30416, num_iters>(f); \
    test_l1<dtype, 30424, num_iters>(f); \
    test_l1<dtype, 30432, num_iters>(f); \
    test_l1<dtype, 30440, num_iters>(f); \
    test_l1<dtype, 30448, num_iters>(f); \
    test_l1<dtype, 30456, num_iters>(f); \
    test_l1<dtype, 30464, num_iters>(f); \
    test_l1<dtype, 30472, num_iters>(f); \
    test_l1<dtype, 30480, num_iters>(f); \
    test_l1<dtype, 30488, num_iters>(f); \
    test_l1<dtype, 30496, num_iters>(f); \
    test_l1<dtype, 30504, num_iters>(f); \
    test_l1<dtype, 30512, num_iters>(f); \
    test_l1<dtype, 30520, num_iters>(f); \
    test_l1<dtype, 30528, num_iters>(f); \
    test_l1<dtype, 30536, num_iters>(f); \
    test_l1<dtype, 30544, num_iters>(f); \
    test_l1<dtype, 30552, num_iters>(f); \
    test_l1<dtype, 30560, num_iters>(f); \
    test_l1<dtype, 30568, num_iters>(f); \
    test_l1<dtype, 30576, num_iters>(f); \
    test_l1<dtype, 30584, num_iters>(f); \
    test_l1<dtype, 30592, num_iters>(f); \
    test_l1<dtype, 30600, num_iters>(f); \
    test_l1<dtype, 30608, num_iters>(f); \
    test_l1<dtype, 30616, num_iters>(f); \
    test_l1<dtype, 30624, num_iters>(f); \
    test_l1<dtype, 30632, num_iters>(f); \
    test_l1<dtype, 30640, num_iters>(f); \
    test_l1<dtype, 30648, num_iters>(f); \
    test_l1<dtype, 30656, num_iters>(f); \
    test_l1<dtype, 30664, num_iters>(f); \
    test_l1<dtype, 30672, num_iters>(f); \
    test_l1<dtype, 30680, num_iters>(f); \
    test_l1<dtype, 30688, num_iters>(f); \
    test_l1<dtype, 30696, num_iters>(f); \
    test_l1<dtype, 30704, num_iters>(f); \
    test_l1<dtype, 30712, num_iters>(f); \
    test_l1<dtype, 30720, num_iters>(f); \
    test_l1<dtype, 30728, num_iters>(f); \
    test_l1<dtype, 30736, num_iters>(f); \
    test_l1<dtype, 30744, num_iters>(f); \
    test_l1<dtype, 30752, num_iters>(f); \
    test_l1<dtype, 30760, num_iters>(f); \
    test_l1<dtype, 30768, num_iters>(f); \
    test_l1<dtype, 30776, num_iters>(f); \
    test_l1<dtype, 30784, num_iters>(f); \
    test_l1<dtype, 30792, num_iters>(f); \
    test_l1<dtype, 30800, num_iters>(f); \
    test_l1<dtype, 30808, num_iters>(f); \
    test_l1<dtype, 30816, num_iters>(f); \
    test_l1<dtype, 30824, num_iters>(f); \
    test_l1<dtype, 30832, num_iters>(f); \
    test_l1<dtype, 30840, num_iters>(f); \
    test_l1<dtype, 30848, num_iters>(f); \
    test_l1<dtype, 30856, num_iters>(f); \
    test_l1<dtype, 30864, num_iters>(f); \
    test_l1<dtype, 30872, num_iters>(f); \
    test_l1<dtype, 30880, num_iters>(f); \
    test_l1<dtype, 30888, num_iters>(f); \
    test_l1<dtype, 30896, num_iters>(f); \
    test_l1<dtype, 30904, num_iters>(f); \
    test_l1<dtype, 30912, num_iters>(f); \
    test_l1<dtype, 30920, num_iters>(f); \
    test_l1<dtype, 30928, num_iters>(f); \
    test_l1<dtype, 30936, num_iters>(f); \
    test_l1<dtype, 30944, num_iters>(f); \
    test_l1<dtype, 30952, num_iters>(f); \
    test_l1<dtype, 30960, num_iters>(f); \
    test_l1<dtype, 30968, num_iters>(f); \
    test_l1<dtype, 30976, num_iters>(f); \
    test_l1<dtype, 30984, num_iters>(f); \
    test_l1<dtype, 30992, num_iters>(f); \
    test_l1<dtype, 31000, num_iters>(f); \
    test_l1<dtype, 31008, num_iters>(f); \
    test_l1<dtype, 31016, num_iters>(f); \
    test_l1<dtype, 31024, num_iters>(f); \
    test_l1<dtype, 31032, num_iters>(f); \
    test_l1<dtype, 31040, num_iters>(f); \
    test_l1<dtype, 31048, num_iters>(f); \
    test_l1<dtype, 31056, num_iters>(f); \
    test_l1<dtype, 31064, num_iters>(f); \
    test_l1<dtype, 31072, num_iters>(f); \
    test_l1<dtype, 31080, num_iters>(f); \
    test_l1<dtype, 31088, num_iters>(f); \
    test_l1<dtype, 31096, num_iters>(f); \
    test_l1<dtype, 31104, num_iters>(f); \
    test_l1<dtype, 31112, num_iters>(f); \
    test_l1<dtype, 31120, num_iters>(f); \
    test_l1<dtype, 31128, num_iters>(f); \
    test_l1<dtype, 31136, num_iters>(f); \
    test_l1<dtype, 31144, num_iters>(f); \
    test_l1<dtype, 31152, num_iters>(f); \
    test_l1<dtype, 31160, num_iters>(f); \
    test_l1<dtype, 31168, num_iters>(f); \
    test_l1<dtype, 31176, num_iters>(f); \
    test_l1<dtype, 31184, num_iters>(f); \
    test_l1<dtype, 31192, num_iters>(f); \
    test_l1<dtype, 31200, num_iters>(f); \
    test_l1<dtype, 31208, num_iters>(f); \
    test_l1<dtype, 31216, num_iters>(f); \
    test_l1<dtype, 31224, num_iters>(f); \
    test_l1<dtype, 31232, num_iters>(f); \
    test_l1<dtype, 31240, num_iters>(f); \
    test_l1<dtype, 31248, num_iters>(f); \
    test_l1<dtype, 31256, num_iters>(f); \
    test_l1<dtype, 31264, num_iters>(f); \
    test_l1<dtype, 31272, num_iters>(f); \
    test_l1<dtype, 31280, num_iters>(f); \
    test_l1<dtype, 31288, num_iters>(f); \
    test_l1<dtype, 31296, num_iters>(f); \
    test_l1<dtype, 31304, num_iters>(f); \
    test_l1<dtype, 31312, num_iters>(f); \
    test_l1<dtype, 31320, num_iters>(f); \
    test_l1<dtype, 31328, num_iters>(f); \
    test_l1<dtype, 31336, num_iters>(f); \
    test_l1<dtype, 31344, num_iters>(f); \
    test_l1<dtype, 31352, num_iters>(f); \
    test_l1<dtype, 31360, num_iters>(f); \
    test_l1<dtype, 31368, num_iters>(f); \
    test_l1<dtype, 31376, num_iters>(f); \
    test_l1<dtype, 31384, num_iters>(f); \
    test_l1<dtype, 31392, num_iters>(f); \
    test_l1<dtype, 31400, num_iters>(f); \
    test_l1<dtype, 31408, num_iters>(f); \
    test_l1<dtype, 31416, num_iters>(f); \
    test_l1<dtype, 31424, num_iters>(f); \
    test_l1<dtype, 31432, num_iters>(f); \
    test_l1<dtype, 31440, num_iters>(f); \
    test_l1<dtype, 31448, num_iters>(f); \
    test_l1<dtype, 31456, num_iters>(f); \
    test_l1<dtype, 31464, num_iters>(f); \
    test_l1<dtype, 31472, num_iters>(f); \
    test_l1<dtype, 31480, num_iters>(f); \
    test_l1<dtype, 31488, num_iters>(f); \
    test_l1<dtype, 31496, num_iters>(f); \
    test_l1<dtype, 31504, num_iters>(f); \
    test_l1<dtype, 31512, num_iters>(f); \
    test_l1<dtype, 31520, num_iters>(f); \
    test_l1<dtype, 31528, num_iters>(f); \
    test_l1<dtype, 31536, num_iters>(f); \
    test_l1<dtype, 31544, num_iters>(f); \
    test_l1<dtype, 31552, num_iters>(f); \
    test_l1<dtype, 31560, num_iters>(f); \
    test_l1<dtype, 31568, num_iters>(f); \
    test_l1<dtype, 31576, num_iters>(f); \
    test_l1<dtype, 31584, num_iters>(f); \
    test_l1<dtype, 31592, num_iters>(f); \
    test_l1<dtype, 31600, num_iters>(f); \
    test_l1<dtype, 31608, num_iters>(f); \
    test_l1<dtype, 31616, num_iters>(f); \
    test_l1<dtype, 31624, num_iters>(f); \
    test_l1<dtype, 31632, num_iters>(f); \
    test_l1<dtype, 31640, num_iters>(f); \
    test_l1<dtype, 31648, num_iters>(f); \
    test_l1<dtype, 31656, num_iters>(f); \
    test_l1<dtype, 31664, num_iters>(f); \
    test_l1<dtype, 31672, num_iters>(f); \
    test_l1<dtype, 31680, num_iters>(f); \
    test_l1<dtype, 31688, num_iters>(f); \
    test_l1<dtype, 31696, num_iters>(f); \
    test_l1<dtype, 31704, num_iters>(f); \
    test_l1<dtype, 31712, num_iters>(f); \
    test_l1<dtype, 31720, num_iters>(f); \
    test_l1<dtype, 31728, num_iters>(f); \
    test_l1<dtype, 31736, num_iters>(f); \
    test_l1<dtype, 31744, num_iters>(f); \
    test_l1<dtype, 31752, num_iters>(f); \
    test_l1<dtype, 31760, num_iters>(f); \
    test_l1<dtype, 31768, num_iters>(f); \
    test_l1<dtype, 31776, num_iters>(f); \
    test_l1<dtype, 31784, num_iters>(f); \
    test_l1<dtype, 31792, num_iters>(f); \
    test_l1<dtype, 31800, num_iters>(f); \
    test_l1<dtype, 31808, num_iters>(f); \
    test_l1<dtype, 31816, num_iters>(f); \
    test_l1<dtype, 31824, num_iters>(f); \
    test_l1<dtype, 31832, num_iters>(f); \
    test_l1<dtype, 31840, num_iters>(f); \
    test_l1<dtype, 31848, num_iters>(f); \
    test_l1<dtype, 31856, num_iters>(f); \
    test_l1<dtype, 31864, num_iters>(f); \
    test_l1<dtype, 31872, num_iters>(f); \
    test_l1<dtype, 31880, num_iters>(f); \
    test_l1<dtype, 31888, num_iters>(f); \
    test_l1<dtype, 31896, num_iters>(f); \
    test_l1<dtype, 31904, num_iters>(f); \
    test_l1<dtype, 31912, num_iters>(f); \
    test_l1<dtype, 31920, num_iters>(f); \
    test_l1<dtype, 31928, num_iters>(f); \
    test_l1<dtype, 31936, num_iters>(f); \
    test_l1<dtype, 31944, num_iters>(f); \
    test_l1<dtype, 31952, num_iters>(f); \
    test_l1<dtype, 31960, num_iters>(f); \
    test_l1<dtype, 31968, num_iters>(f); \
    test_l1<dtype, 31976, num_iters>(f); \
    test_l1<dtype, 31984, num_iters>(f); \
    test_l1<dtype, 31992, num_iters>(f); \
    test_l1<dtype, 32000, num_iters>(f); \
    test_l1<dtype, 32008, num_iters>(f); \
    test_l1<dtype, 32016, num_iters>(f); \
    test_l1<dtype, 32024, num_iters>(f); \
    test_l1<dtype, 32032, num_iters>(f); \
    test_l1<dtype, 32040, num_iters>(f); \
    test_l1<dtype, 32048, num_iters>(f); \
    test_l1<dtype, 32056, num_iters>(f); \
    test_l1<dtype, 32064, num_iters>(f); \
    test_l1<dtype, 32072, num_iters>(f); \
    test_l1<dtype, 32080, num_iters>(f); \
    test_l1<dtype, 32088, num_iters>(f); \
    test_l1<dtype, 32096, num_iters>(f); \
    test_l1<dtype, 32104, num_iters>(f); \
    test_l1<dtype, 32112, num_iters>(f); \
    test_l1<dtype, 32120, num_iters>(f); \
    test_l1<dtype, 32128, num_iters>(f); \
    test_l1<dtype, 32136, num_iters>(f); \
    test_l1<dtype, 32144, num_iters>(f); \
    test_l1<dtype, 32152, num_iters>(f); \
    test_l1<dtype, 32160, num_iters>(f); \
    test_l1<dtype, 32168, num_iters>(f); \
    test_l1<dtype, 32176, num_iters>(f); \
    test_l1<dtype, 32184, num_iters>(f); \
    test_l1<dtype, 32192, num_iters>(f); \
    test_l1<dtype, 32200, num_iters>(f); \
    test_l1<dtype, 32208, num_iters>(f); \
    test_l1<dtype, 32216, num_iters>(f); \
    test_l1<dtype, 32224, num_iters>(f); \
    test_l1<dtype, 32232, num_iters>(f); \
    test_l1<dtype, 32240, num_iters>(f); \
    test_l1<dtype, 32248, num_iters>(f); \
    test_l1<dtype, 32256, num_iters>(f); \
    test_l1<dtype, 32264, num_iters>(f); \
    test_l1<dtype, 32272, num_iters>(f); \
    test_l1<dtype, 32280, num_iters>(f); \
    test_l1<dtype, 32288, num_iters>(f); \
    test_l1<dtype, 32296, num_iters>(f); \
    test_l1<dtype, 32304, num_iters>(f); \
    test_l1<dtype, 32312, num_iters>(f); \
    test_l1<dtype, 32320, num_iters>(f); \
    test_l1<dtype, 32328, num_iters>(f); \
    test_l1<dtype, 32336, num_iters>(f); \
    test_l1<dtype, 32344, num_iters>(f); \
    test_l1<dtype, 32352, num_iters>(f); \
    test_l1<dtype, 32360, num_iters>(f); \
    test_l1<dtype, 32368, num_iters>(f); \
    test_l1<dtype, 32376, num_iters>(f); \
    test_l1<dtype, 32384, num_iters>(f); \
    test_l1<dtype, 32392, num_iters>(f); \
    test_l1<dtype, 32400, num_iters>(f); \
    test_l1<dtype, 32408, num_iters>(f); \
    test_l1<dtype, 32416, num_iters>(f); \
    test_l1<dtype, 32424, num_iters>(f); \
    test_l1<dtype, 32432, num_iters>(f); \
    test_l1<dtype, 32440, num_iters>(f); \
    test_l1<dtype, 32448, num_iters>(f); \
    test_l1<dtype, 32456, num_iters>(f); \
    test_l1<dtype, 32464, num_iters>(f); \
    test_l1<dtype, 32472, num_iters>(f); \
    test_l1<dtype, 32480, num_iters>(f); \
    test_l1<dtype, 32488, num_iters>(f); \
    test_l1<dtype, 32496, num_iters>(f); \
    test_l1<dtype, 32504, num_iters>(f); \
    test_l1<dtype, 32512, num_iters>(f); \
    test_l1<dtype, 32520, num_iters>(f); \
    test_l1<dtype, 32528, num_iters>(f); \
    test_l1<dtype, 32536, num_iters>(f); \
    test_l1<dtype, 32544, num_iters>(f); \
    test_l1<dtype, 32552, num_iters>(f); \
    test_l1<dtype, 32560, num_iters>(f); \
    test_l1<dtype, 32568, num_iters>(f); \
    test_l1<dtype, 32576, num_iters>(f); \
    test_l1<dtype, 32584, num_iters>(f); \
    test_l1<dtype, 32592, num_iters>(f); \
    test_l1<dtype, 32600, num_iters>(f); \
    test_l1<dtype, 32608, num_iters>(f); \
    test_l1<dtype, 32616, num_iters>(f); \
    test_l1<dtype, 32624, num_iters>(f); \
    test_l1<dtype, 32632, num_iters>(f); \
    test_l1<dtype, 32640, num_iters>(f); \
    test_l1<dtype, 32648, num_iters>(f); \
    test_l1<dtype, 32656, num_iters>(f); \
    test_l1<dtype, 32664, num_iters>(f); \
    test_l1<dtype, 32672, num_iters>(f); \
    test_l1<dtype, 32680, num_iters>(f); \
    test_l1<dtype, 32688, num_iters>(f); \
    test_l1<dtype, 32696, num_iters>(f); \
    test_l1<dtype, 32704, num_iters>(f); \
    test_l1<dtype, 32712, num_iters>(f); \
    test_l1<dtype, 32720, num_iters>(f); \
    test_l1<dtype, 32728, num_iters>(f); \
    test_l1<dtype, 32736, num_iters>(f); \
    test_l1<dtype, 32744, num_iters>(f); \
    test_l1<dtype, 32752, num_iters>(f); \
    test_l1<dtype, 32760, num_iters>(f); \
    test_l1<dtype, 32768, num_iters>(f);
